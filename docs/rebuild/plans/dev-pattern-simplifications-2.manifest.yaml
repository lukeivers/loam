schema_version: 3
amendment:
  # number omitted — schema v3 deprecates amendment.number per
  # AC.DPS1.10 + AC.DPS1.11. Identification by slug only.
  slug: dev-pattern-simplifications-2
  title: "Dev-pattern simplifications #2 — seal-narrative compression (cost-audit recommendation B)"

# BASELINE: ce5d13e — the source-edit commit (pre-apply tip).
#
# Commit ladder pre-apply:
#   57b5b6f — plan-before-code commit (DPS2 sub-plan).
#   ce5d13e — feat(dev-sdlc): synthesizer enrichment + plan_doc_ref in
#             seal commit body + commit-ladder doc + AC.DPS2.* tests.
#   <`loam amend apply` lands NEXT — single merged manifest+apply commit
#    per AC.DPS1.6, since this manifest uses schema_version: 3>
#   <`loam amend seal --plan-doc <abs path>` lands as deterministic
#    seal commit, with synthesized 5-15 line narrative body per
#    AC.DPS2.{1,4} and `Plan doc:` line in the commit message body
#    per AC.DPS2.6>
#   <`loam amend seal --plan-doc` also backfills the §14 SHA register>
#
# Single-component fence — dev-sdlc:
#   - dev-sdlc — synthesizer + commit-message + manifest-schema +
#     conventions doc edits all live under plugins/dev-sdlc/. Tests
#     under plugins/dev-sdlc/tools/loam-amend/tests/test_AC_DPS2_*.py.
#
# AC families locked per docs/rebuild/plans/dev-pattern-simplifications-2.md §4:
#   AC.DPS2.1  — synthesizer emits what-shipped + ACs + smoke + plan-doc.
#   AC.DPS2.2  — ac_count field — additive at v3, rejected at v1/v2.
#   AC.DPS2.3  — smoke_outcome field — additive at v3, rejected at v1/v2.
#   AC.DPS2.4  — synthesized body fits 5-15 lines.
#   AC.DPS2.5  — explicit narrative.body returned verbatim at v3.
#   AC.DPS2.6  — seal commit message body gains "Plan doc:" line at v3.
#   AC.DPS2.7  — seal commit subject unchanged across schemas.
#   AC.DPS2.8  — commit-ladder.md describes both shapes.
#   AC.DPS2.9  — sealed-component-build dispatch template verified.
#   AC.DPS2.10 — all existing manifests validate clean.
#   AC.DPS2.11 — dev-sdlc seal-test stays green.
#   AC.DPS2.12 — existing long-form SEAL_COMMIT.<slug> files unchanged.
#   AC.DPS2.13 — THIS amendment is the v3 first-use validator.
#   AC.DPS2.14 — per-AC tests authored.
#
# Smoke (REALISTIC CONDITION — all 6 dimensions per dispatch text):
#   D1 cold-state — `loam amend apply` on the v3 manifest produces a
#     single merged manifest+apply commit (per AC.DPS1.6); manifest
#     validates with plan_doc_ref + ac_count + smoke_outcome; no
#     amendment-number assigned. Verified by
#     test_AC_DPS2_13_v3_end_to_end_apply_with_new_fields (PASS).
#   D2 steady-state — repeated v3 apply runs idempotent (existing
#     AC.LAE.1 / AC.DPS1.9 invariant). The v3 path inherits this
#     verbatim; no new test needed beyond the existing AC.DPS1.9
#     coverage which still passes.
#   D3 restart — load_manifest is pure (no side-state); validation
#     independent of process state. Verified structurally (the parser
#     reads + validates from disk only).
#   D4 reboot — same as D3.
#   D5 cross-session — existing legacy-format SEAL_COMMIT.<slug> files
#     and v1/v2 manifests still readable. Verified by
#     test_AC_DPS2_10_existing_manifests_validate_clean (108/108 PASS)
#     + the v1/v2 branch in _resolve_narrative_body remains
#     byte-identical to pre-DPS2 (verified by
#     test_AC_DPS2_5_explicit_body_returned_verbatim_at_v3 +
#     test_AC_DPS2_6_seal_commit_message_v1_unchanged).
#   D6 telemetry-floor — apply auto-commit emits the chore(amend)
#     commit shape per AC.LAE.1 + AC.DPS1.6 (the "manifest+apply"
#     verb). Seal commit emits the chore(seals) shape per AC.D-sa.2 +
#     AC.DPS2.6 (Plan doc: line added at v3). Audit-trail floor
#     preserved.
#
# Bookkeeping note: this amendment is the FIRST to author its
# manifest at schema_version: 3 in canonical history. DPS1 deferred
# v3-as-first-use to "the next amendment" by chicken-and-egg
# avoidance; DPS2 is that next amendment. The fact that this manifest
# parses + applies + seals end-to-end is itself the integration
# validation of the schema landed by DPS1 (per dispatch halt-trigger
# #3 — "v3 schema doesn't actually work for THIS amendment's
# manifest → halt + surface (would mean amendment 1 has a defect)").

baseline: "ce5d13e"

plan: docs/rebuild/plans/dev-pattern-simplifications-2.md

# Top-level plan_doc_ref — schema v3 collapsed-narrative pointer.
# `loam amend seal` synthesizes the narrative body from this ref
# plus the optional ac_count + smoke_outcome below.
plan_doc_ref: docs/rebuild/plans/dev-pattern-simplifications-2.md

# DPS2-introduced optional fields, exercised end-to-end on this
# amendment (the first to use them). Per AC.DPS2.{1,2,3,13}.
ac_count: 14
smoke_outcome: "all 6 dimensions exercised"

components:
  - name: dev-sdlc
    seal_test: plugins/dev-sdlc/tests/test_no_sealed_amendments.py
    sidecar: plugins/dev-sdlc/tests/SEAL_COMMIT
    frozen_baseline: false
    extra_allowed_prefixes: []

universal_paths:
  prefixes:
    - docs/rebuild/plans/
  files:
    - CLAUDE.md
    - docs/odd-in-loam.md
    - docs/odd-methodology.md
    - docs/rebuild/STATE.md

narrative:
  target: plugins/dev-sdlc/seals/SEAL_COMMIT.dev-pattern-simplifications-2
  # body omitted — schema v3 with plan_doc_ref synthesizes a 5-15
  # line summary at seal time per AC.DPS2.{1,4}. The summary
  # surfaces title + slug + components + baseline + amendment SHA +
  # plan_doc_ref + ac_count + smoke_outcome.
