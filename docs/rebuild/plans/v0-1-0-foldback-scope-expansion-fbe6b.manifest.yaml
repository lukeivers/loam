schema_version: 1
amendment:
  number: 114
  slug: v0-1-0-foldback-scope-expansion-fbe6b
  title: "FBE.6b — re-run FBE.6 close-cycle against post-FBE.8 canonical HEAD (HOL no-op narrative anchor)"

# BASELINE: a67bfbb — the FBE.6b sub-plan-doc commit (the FBE.6b
# dispatch's canonical HEAD at synth-run-time). FBE.6b carries NO
# source-side delta per AC.FBE.6b.7 (negative AC: zero source/doc
# edits during FBE.6b).
#
# Commit ladder pre-apply:
#   a67bfbb — FBE.6b sub-plan-doc (NEW commit, the BASELINE)
#   <synth ran from a67bfbb producing framework-only 8f57bde>
#   <sweeps + smoke + staging push executed; sweep report written>
#   <seal narrative authored at framework/hands-off-lifecycle/seals/SEAL_COMMIT.v0-1-0-foldback-fbe6b>
#   <this manifest commit lands next>
#   <loam amend apply lands as the next commit>
#   <loam amend seal lands as the seal commit>
#   <parent §8 register backfill commit lands last>
#
# HOL no-op narrative anchor pattern (per FBE.6 + FBE.8 precedent
# for amendments with structural surface that's narrative-only).
# HOL is its own anchor here — the byte-content invariant at HOL
# stays unchanged (frozen_baseline: true) and the seal narrative
# file is the entire structural artefact.
#
# Per parent plan §4 FBE.6.S clause: "Sealed-component fence: NONE
# — this is a sweep + smoke + review amendment with no source-side
# delta. Narrative anchor at framework/hands-off-lifecycle/seals/
# SEAL_COMMIT.v0-1-0-foldback-fbe6b per FBE.6 + FBE.8 precedent."
baseline: "a67bfbb"

plan: docs/rebuild/plans/v0-1-0-foldback-scope-expansion-fbe6b.md

seal_description: "FBE.6b re-run of FBE.6 close-cycle against post-FBE.8 canonical HEAD — verification step for v0.1.0 publish foldback. Re-synth canonical HEAD post-FBE.8 (source a67bfbb → framework-only 8f57bde, advance from FBE.6's 4d105f6); re-run all 8 AC.M11a.* sweeps (all PASS — FBE.6's 4 regressions all closed by FBE.8: pos-amend / loam-amend / docs/rebuild/ / graphiti pyproject all back to zero); EXTENDED smoke via dispatcher's pip install -r install-from-source.txt + loam init <ws> --from <repo> PASSES end-to-end; staging push to lukeivers/loam-staging:main = 8f57bde; reviewer re-run (stranger-perspective + Ruthless Feedback) verifies FBE.8 closures (BLOCKER-FBE6.1 + BLOCKER-FBE6.2 + HIGH-FBE6.1 + HIGH-FBE6.2) all clean BUT surfaces NEW BLOCKER-FBE6b.1 (README + getting-started.md `loam init .` invocation does not match CLI's required --from CANONICAL_SOURCE flag; stranger gets `loam init: error: the following arguments are required: --from` exit 2) + NEW HIGH-FBE6b.1 (pervasive `Amendment #N` source-comment references — 339 occurrences across 95 files; not in AC.M11a.2 banned list; same shape as original review LOW-3 at larger scale). Verdict: FOLDBACK to FBE.9. M12 publish-flip is GATED. Per AC.FBE.6b.7 negative AC + halt-trigger #1, FBE.6b closes WITHOUT silently fixing in-band. Sweep report at <workspace>/.scratch/claude-output/v0-1-0-foldback-fbe6b-sweep-report.md."

# HOL no-op narrative anchor (mirrors FBE.6 + FBE.8 precedent for
# amendments where the structural surface is the seal narrative
# itself; no source-side fence delta to enforce).
components:
  - name: hands-off-lifecycle
    seal_test: framework/hands-off-lifecycle/tests/test_cross_cutting.py
    sidecar: framework/hands-off-lifecycle/tests/SEAL_COMMIT
    frozen_baseline: true
    extra_allowed_prefixes: []

# Universal admissions — per amendment #22 ruling #3.
# `docs/rebuild/plans/` admits the plan-before-code paper trail
# (FBE.6b sub-plan + manifest YAML + parent plan-doc backfill).
# `framework/hands-off-lifecycle/seals/` admits the seal narrative
# file (HOL is the narrative anchor home per parent plan §4 FBE.6.S).
universal_paths:
  prefixes:
    - docs/rebuild/plans/
    - framework/hands-off-lifecycle/seals/
  files:
    - CLAUDE.md
    - docs/odd-in-loam.md
    - docs/odd-methodology.md
    - docs/rebuild/FUTURE_IDEAS.md
    - docs/rebuild/FUTURE_IDEAS_DRAFT.md

narrative:
  target: framework/hands-off-lifecycle/seals/SEAL_COMMIT.v0-1-0-foldback-fbe6b
  body: |
    # FBE.6b narrative-only seal (re-run of FBE.6 close-cycle)

    See the full narrative at the target file (already authored
    pre-apply per AC.FBE.6b.6 + AC.FBE.6b.S) for FBE.6b outcome,
    sweep results (all PASS — no regressions), smoke results
    (PASS end-to-end), reviewer verdict (FOLDBACK to FBE.9 with
    NEW BLOCKER-FBE6b.1 + NEW HIGH-FBE6b.1), and path forward.

    Sub-plan-doc:
    `docs/rebuild/plans/v0-1-0-foldback-scope-expansion-fbe6b.md`.
    Sweep report:
    `<workspace>/.scratch/claude-output/v0-1-0-foldback-fbe6b-sweep-report.md`.
    Status file:
    `<workspace>/.scratch/claude-output/fbe6b-status-2026-05-03.md`.

    BASELINE a67bfbb (the FBE.6b sub-plan-doc commit; FBE.6b's source
    canonical HEAD at synth-run-time). No source-side delta per
    AC.FBE.6b.7 negative AC.
