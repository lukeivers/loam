---
schema_version: 3
amendment:
  # number omitted — schema v3 deprecates amendment.number per
  # AC.DPS1.10 + AC.DPS1.11. Identification by slug only.
  slug: v0-2-5-corrective-c3-install-and-outcome-altitude-ac
  title: "v0.2.5 corrective C3 — install-from-source.txt adds -e ./plugins/dev-sdlc/odd-extractor[synthesis] so a fresh install pulls in the anthropic SDK; new outcome-altitude AC test exercises the production CLI --live path against the real SDK with no monkeypatch (skip-or-pass on ANTHROPIC_API_KEY); F6 dispatch-text drift verified absent in pos-v2 (audit-closure); 5 named ACs + meta-AC for full-suite green"

# BASELINE: <pending> — source-edit feat commit covering install-from-source.txt
# + new test file under plugins/dev-sdlc/odd-extractor/tests/.
#
# Corrective amendment landing C3 fixes surfaced by v0.2.5 HARD smoke RE-RUN
# (RED, F5 BLOCKER + F6 yellow). Master plan §3 v0.2.5 + the dispatch brief
# explicitly authorize "land four artefacts so v0.2.5 HARD smoke re-runs GREEN."
# This manifest carries all four C3 artefacts as a single combined corrective
# per v0.2.5 corrective C1+C2 single-amendment-multi-AC precedent.
#
# Commit ladder pre-apply:
#   064cc2e — v0.2.4 Cycle 3 seal.
#   38a0473 — v0.2.4 Cycle 3 §14 backfill.
#   7f8e8b3 — v0.2.4 master plan §9 Cycle 3 SHA backfill.
#   4f54649 — v0.2.4 SHIPPED rollup.
#   c098b3b — v0.2.5 corrective C1+C2 plan-doc + odd-test-altitude-procedural-fix plan-doc joint.
#   a75e2b0 — v0.2.5 corrective C1+C2 source edits (cli.py + interview.py + tests; rides under odd-test-altitude commit).
#   16d6e50 — odd-test-altitude-procedural-fix manifest+apply (absorbed C1+C2 source edits).
#   a9bc524 — odd-test-altitude-procedural-fix seal (rides v0.2.5 C1+C2 fixes).
#   876bf66 — odd-test-altitude-procedural-fix §14 backfill.
#   8bf8845 — odd-test-altitude STATE entry.
#   e699958 — v0.2.5 corrective C1+C2 §14 backfill + STATE entry + FIDRAFT F3+F4.
#   a43e37d — v0.2.5 C1+C2 §14 SHA self-pin (HEAD).
#   <plan-doc commit lands NEXT (THIS plan-doc + manifest), then
#    source-edit feat commit (BASELINE pinned to that SHA), then
#    `loam amend apply <manifest>` lands as single merged
#    manifest+apply commit per AC.DPS1.6 (v3 schema)>
#   <`loam amend seal --plan-doc <abs path> <manifest>` lands as
#    deterministic seal commit per AC.DPS2.{1,4,6}>
#   <post-seal commit backfills this plan-doc's §14 per AC.D-sa.7>
#
# Cross-component fence — install-from-source + dev-sdlc:
#   - EDIT install-from-source.txt — add new line in Tier J:
#     `-e ./plugins/dev-sdlc/odd-extractor[synthesis]` so a fresh
#     install pulls in odd-extractor editable + the anthropic SDK
#     as part of the standard install path. Preserves the deliberate
#     [synthesis] optional-extra design.
#   - NEW test under plugins/dev-sdlc/odd-extractor/tests/:
#       test_AC_V025_C3_3_cli_live_outcome_altitude.py
#                    — AC.V025-C3.3: cli.main(--live) against
#                      jsts-playwright-app fixture WITHOUT monkeypatch
#                      and WITHOUT pre-arrangement; skips cleanly if
#                      ANTHROPIC_API_KEY unset OR anthropic SDK
#                      unimportable; otherwise asserts objectives.yaml
#                      ≥1 objective + backing-map.yaml exists +
#                      synthesis.yaml model_id != "(none)" AND matches
#                      a real Anthropic model identifier.
#   - NO edits to other source files (production code unchanged;
#     only install-shape + test addition).
#   - NO edits to pyproject.toml [synthesis] extra design (preserved
#     per v0.2.3 Cycle 1 sub-plan-doc §7 method-decision).
#
# Operational (non-commit, AC.V025-C3.2):
#   - pip install anthropic>=0.40 against
#     /opt/homebrew/opt/python@3.13/bin/python3.13.
#   - One-time workstation fix; not a manifest-tracked change.
#
# AC families locked per plan-doc §2:
#   AC.V025-C3.1 — install-from-source.txt synthesis extra (outcome-altitude: false; structural verification).
#   AC.V025-C3.2 — workstation anthropic SDK installed (outcome-altitude: true; real install).
#   AC.V025-C3.3 — outcome-altitude AC test for CLI --live path (outcome-altitude: true; first worked instance of new SKILL).
#   AC.V025-C3.4 — F6 dispatch-text drift fix (outcome-altitude: false; audit closure — drift absent in pos-v2).
#   AC.V025-C3.5 — meta: pre-existing odd-extractor test suite remains green post-fix.
#
# Smoke (REALISTIC CONDITION — applicable dimensions per
# smoke-test-discipline §6 + plan-doc §5):
#   D1 cold-state — fresh tmp workspace; cli.main(--live) against
#     jsts-playwright-app + REAL Anthropic SDK produces non-empty
#     objectives.yaml + backing-map.yaml (or skips cleanly if no
#     API key). Verified by AC.V025-C3.3.
#   D2 steady-state — inherited from v0.2.3 idempotence; not re-verified.
#   D5 cross-session — inherited from v0.2.4; not re-verified.
#   D6 telemetry-floor — structural; no source edits to audit-log paths.
#   D3/D4 — n/a structurally (one-shot CLI).
#   PLUS: full-suite green sweep — AC.V025-C3.5 verifies 815+ pass.
#
# Bookkeeping: corrective amendment outside any v0.2.5 numbered
# cycle ladder (v0.2.5 has no cycles; the release was pure smoke-
# gate). Sealed-content invariant: pre-existing AC.* tests in
# odd-extractor remain green; only one config file edited
# (install-from-source.txt); only one new test file added.

# BASELINE pinned to the source-edit feat commit (v0.2.5 corrective C3).
baseline: "2fd17a6"

plan: docs/rebuild/plans/v0-2-5-corrective-c3-install-and-outcome-altitude-ac.md

# Top-level plan_doc_ref — schema v3 collapsed-narrative pointer.
# `loam amend seal` synthesizes the narrative body from this ref
# plus ac_count + smoke_outcome below.
plan_doc_ref: docs/rebuild/plans/v0-2-5-corrective-c3-install-and-outcome-altitude-ac.md

ac_count: 5
smoke_outcome: "C3.1 install-line added; C3.2 anthropic SDK in workstation Python; C3.3 outcome-altitude test (first SKILL worked instance); C3.4 F6 drift absent; C3.5 odd-extractor green"

components:
  - name: dev-sdlc
    seal_test: plugins/dev-sdlc/tests/test_no_sealed_amendments.py
    sidecar: plugins/dev-sdlc/tests/SEAL_COMMIT
    frozen_baseline: false
    extra_allowed_prefixes: []

# Universal admissions — per amendment #22 ruling #3 + v0.2.5
# corrective C1+C2 precedent. `docs/rebuild/plans/` admits the
# plan-before-code paper trail. `install-from-source.txt` is a
# repo-root file requiring explicit admission.
universal_paths:
  prefixes:
    - docs/rebuild/plans/
  files:
    - CLAUDE.md
    - docs/odd-in-loam.md
    - docs/odd-methodology.md
    - docs/rebuild/STATE.md
    - docs/rebuild/FUTURE_IDEAS_DRAFT.md
    - install-from-source.txt

narrative:
  target: plugins/dev-sdlc/seals/SEAL_COMMIT.v0-2-5-corrective-c3-install-and-outcome-altitude-ac
  # body omitted — schema v3 with plan_doc_ref synthesizes a 5-15
  # line summary at seal time per AC.DPS2.{1,4}.
