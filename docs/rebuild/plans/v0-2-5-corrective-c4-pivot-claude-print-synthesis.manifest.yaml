---
schema_version: 3
amendment:
  # number omitted — schema v3 deprecates amendment.number per
  # AC.DPS1.10 + AC.DPS1.11. Identification by slug only.
  slug: v0-2-5-corrective-c4-pivot-claude-print-synthesis
  title: "v0.2.5 corrective C4-pivot — synthesis routes through `claude -p` subprocess against Claude Max subscription (NO Anthropic SDK; NO API key) per owner ruling Telegram 10194; new ClaudePrintAnthropicShimClient mirrors framework/memory-system claude_print_client subprocess pattern; build_default_anthropic_client body rewritten (symbol preserved for sealed C1+C2 monkeypatch backward-compat); install-from-source.txt [synthesis] line reverted; pyproject.toml [synthesis] extra removed; outcome-altitude tests C3.3+C4.3 pivoted from ANTHROPIC_API_KEY/importorskip to shutil.which('claude'); F8 banding fix folded in (prompt-engineering preserved + first-pass demotion-guard preserved + NEW second-pass demotion-or-drop guard for PLAUSIBLE-no-single-source rows surfaced under live claude -p transport); 7 named ACs + meta-AC; closes both F8 BLOCKER (LLM banding violation) and the direction-pivot (subscription-only constraint)"

# BASELINE: <pending> — source-edit feat commit covering:
#   - NEW plugins/dev-sdlc/odd-extractor/src/loam_odd_extractor/claude_print_synthesis_client.py
#   - EDIT plugins/dev-sdlc/odd-extractor/src/loam_odd_extractor/synthesis.py
#       1. Module docstring updated (claude -p direction).
#       2. build_default_anthropic_client body rewritten — returns
#          ClaudePrintAnthropicShimClient via build_default_synthesis_client
#          (symbol-name preserved for sealed C1+C2 monkeypatch backward-compat).
#       3. _apply_band_demotion_guard preserved verbatim from C4 pre-pivot
#          (rewrites VERIFIED-without-two-sources to PLAUSIBLE).
#       4. NEW _apply_plausible_demotion_or_drop_guard — handles
#          PLAUSIBLE-no-single-source rows surfaced by live LLM (demotes to
#          HYPOTHESISED with rationale-or-code-pattern fallback, drops
#          truly-empty rows). Invoked from _validate_rows after the first-pass.
#       5. Module-level comment + invocation comment updated to name the
#          C4-pivot transport (no Anthropic SDK).
#   - EDIT plugins/dev-sdlc/odd-extractor/src/loam_odd_extractor/completeness.py
#       1. build_default_anthropic_client body rewritten (mirror synthesis).
#   - EDIT plugins/dev-sdlc/odd-extractor/src/loam_odd_extractor/cli.py
#       1. anthropic_client local variable renamed to llm_client at the
#          construction site (the kwarg passed to generate_raw_acs remains
#          anthropic_client= for backward-compat with sealed call sites).
#       2. Comment updated to reference C4-pivot.
#   - EDIT plugins/dev-sdlc/odd-extractor/pyproject.toml
#       1. [synthesis] optional-dependencies entry removed.
#       2. anthropic>=0.40 dep removed.
#       3. Comment updated to point at C4-pivot plan-doc.
#   - EDIT install-from-source.txt
#       1. C3 line `-e ./plugins/dev-sdlc/odd-extractor[synthesis]` reverted
#          to `-e ./plugins/dev-sdlc/odd-extractor` (extra removed).
#       2. Comment block rewritten to reference owner ruling Telegram 10194.
#   - EDIT plugins/dev-sdlc/odd-extractor/tests/test_AC_V025_C3_3_cli_live_outcome_altitude.py
#       1. Drop `import os` + ANTHROPIC_API_KEY env check.
#       2. Drop pytest.importorskip("anthropic").
#       3. Add shutil.which("claude") skip-clean precondition.
#       4. Update docstring + pre-arrangement rubric to name claude -p transport.
#   - EDIT plugins/dev-sdlc/odd-extractor/tests/test_AC_V025_C4_3_cli_live_outcome_altitude_post_fix.py
#       1. Same pivot as C3.3.
#   - EDIT plugins/dev-sdlc/odd-extractor/tests/test_AC_V025_C4_2_demotion_guard.py
#       1. Replace test_AC_V025_C4_2_validate_rows_demoted_row_without_any_evidence_still_raises
#          with test_AC_V025_C4_2_validate_rows_demoted_row_without_any_evidence_is_dropped
#          (second-pass guard's behavior: drop the row instead of raising).
#
# Corrective amendment landing the C4 pivot — supersedes the C4 SDK-direction
# manifest at 48762f5 (which never reached sealed state because of this
# pivot ruling). Master plan §3 v0.2.5 + the C4-pivot dispatch brief
# explicitly authorize this work.
#
# Commit ladder pre-apply (relative to current HEAD):
#   89f97c6 — v0.2.5 corrective C3 seal.
#   b3a5812 — v0.2.5 C3 §14 backfill.
#   189c6db — v0.2.5 C3 STATE entry + FIDRAFT F8 BLOCKER capture.
#   48762f5 — v0.2.5 corrective C4 plan-doc + manifest (SDK-direction; superseded by THIS pivot, manifest never sealed).
#   <plan-doc + manifest commit lands NEXT (THIS plan-doc + manifest), then
#    source-edit feat commit (BASELINE pinned to that SHA), then
#    `loam amend apply <manifest>` lands as single merged
#    manifest+apply commit per AC.DPS1.6 (v3 schema)>
#   <`loam amend seal --plan-doc <abs path> <manifest>` lands as
#    deterministic seal commit per AC.DPS2.{1,4,6}>
#   <post-seal commit backfills this plan-doc's §14 per AC.D-sa.7>
#
# Cross-component fence — install-from-source + dev-sdlc:
#   - 1 NEW source file (claude_print_synthesis_client.py)
#   - 4 EDITED source files (synthesis.py, completeness.py, cli.py,
#     pyproject.toml — under plugins/dev-sdlc/odd-extractor/)
#   - 1 EDITED root config (install-from-source.txt)
#   - 3 EDITED test files (test_AC_V025_C3_3, test_AC_V025_C4_3,
#     test_AC_V025_C4_2 — under plugins/dev-sdlc/odd-extractor/tests/)
#   - NO edits to spec.py validator (validator stays strict per §14
#     method-decision).
#
# AC families locked per plan-doc §2:
#   AC.V025-C4P.1 — Synthesis routes through claude -p (NOT Anthropic SDK).
#   AC.V025-C4P.2 — [synthesis] extra removed from pyproject.toml.
#   AC.V025-C4P.3 — install-from-source.txt revert.
#   AC.V025-C4P.4 — outcome-altitude AC tests pivot to claude binary check
#                   (outcome-altitude: true; HARD per-cycle probe).
#   AC.V025-C4P.5 — F8 banding fix folded in (prompt + first-pass guard
#                   preserved + NEW second-pass guard).
#   AC.V025-C4P.6 — meta: pre-existing odd-extractor test suite remains
#                   green (825 pass + 1 skip + 2 live-pass on workstation).
#   AC.V025-C4P.7 — AC.OBJX.5 methodology refinement documented.
#
# Smoke (REALISTIC CONDITION — applicable dimensions per
# smoke-test-discipline §6 + plan-doc §5):
#   D1 cold-state — fresh tmp workspace; cli.main(--live) against
#     jsts-playwright-app + REAL claude -p subprocess produces
#     non-empty objectives.yaml + backing-map.yaml + synthesis.yaml
#     model_id starting "claude-" (or skips cleanly if no claude
#     binary). Verified by AC.V025-C4P.4 — both C3.3 and C4.3 PASS
#     end-to-end on workstation 2026-05-05.
#   D2 steady-state — inherited from v0.2.3 idempotence; not re-verified
#     under stochastic LLM transport.
#   D5 cross-session — inherited from v0.2.4; not re-verified.
#   D6 telemetry-floor — structural; both demotion-guards add WARN-level
#     log entries but no new audit-log event-kind.
#   D3/D4 — n/a structurally (one-shot CLI).
#   PLUS: full-suite green sweep — AC.V025-C4P.6 verifies
#     825 pass + 1 skip (deselected live tests pass on workstation).
#
# Bookkeeping: corrective amendment outside any v0.2.5 numbered
# cycle ladder. Sealed-content invariant: pre-existing AC.* tests in
# odd-extractor remain green; production source files edited:
# synthesis.py + completeness.py + cli.py (3 files); 1 NEW source file
# (claude_print_synthesis_client.py); 1 build config edit
# (pyproject.toml); 1 root config edit (install-from-source.txt);
# 3 test files edited.

# BASELINE pinned to the source-edit feat commit (v0.2.5 corrective C4-pivot).
# 6a29038 = source-edit feat commit (NEW + EDITED files per the commit
# message). Initial pin set HERE on the same commit as the manifest+plan-doc
# author commit (since the source edit landed BEFORE this manifest commit).
baseline: "6a29038"

plan: docs/rebuild/plans/v0-2-5-corrective-c4-pivot-claude-print-synthesis.md

# Top-level plan_doc_ref — schema v3 collapsed-narrative pointer.
# `loam amend seal` synthesizes the narrative body from this ref
# plus ac_count + smoke_outcome below.
plan_doc_ref: docs/rebuild/plans/v0-2-5-corrective-c4-pivot-claude-print-synthesis.md

ac_count: 7
smoke_outcome: "C4P.1 claude -p shim transport; C4P.2 [synthesis] extra removed; C4P.3 install-from-source revert; C4P.4 outcome-altitude tests pivot to claude-on-PATH (BOTH C3.3+C4.3 PASS live on workstation); C4P.5 F8 fix folded in (prompt + first-pass guard preserved + NEW second-pass guard for PLAUSIBLE-no-single-source); C4P.6 odd-extractor 825/1 green; C4P.7 AC.OBJX.5 refinement documented"

components:
  - name: dev-sdlc
    seal_test: plugins/dev-sdlc/tests/test_no_sealed_amendments.py
    sidecar: plugins/dev-sdlc/tests/SEAL_COMMIT
    frozen_baseline: false
    extra_allowed_prefixes: []

# Universal admissions — per amendment #22 ruling #3 + v0.2.5
# corrective C3 precedent. `docs/rebuild/plans/` admits the
# plan-before-code paper trail. `install-from-source.txt` is a
# repo-root file requiring explicit admission.
universal_paths:
  prefixes:
    - docs/rebuild/plans/
  files:
    - CLAUDE.md
    - docs/odd-in-loam.md
    - docs/odd-methodology.md
    - docs/rebuild/STATE.md
    - docs/rebuild/FUTURE_IDEAS_DRAFT.md
    - install-from-source.txt

narrative:
  target: plugins/dev-sdlc/seals/SEAL_COMMIT.v0-2-5-corrective-c4-pivot-claude-print-synthesis
  # body omitted — schema v3 with plan_doc_ref synthesizes a 5-15
  # line summary at seal time per AC.DPS2.{1,4}.
