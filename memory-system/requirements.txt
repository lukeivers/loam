# graphiti-core 0.28.2 is PINNED — two KuzuDriver bugs are patched in
# src/factory.py. Upgrading requires either removing the patches
# (once upstream lands equivalents) or porting them to the new version.
graphiti-core[anthropic,kuzu]==0.28.2
anthropic>=0.39.0
ollama>=0.4.0
kuzu>=0.11.2
pydantic>=2.0
python-dotenv>=1.0
pyyaml>=6.0
numpy>=1.26
# Amendment #24 (2026-04-22) swapped the transport layer from FastAPI
# + uvicorn to FastMCP (streamable-HTTP). fastapi was removed; uvicorn
# is retained as an explicit pin though it now enters via mcp's
# transitive closure. mcp ships its own Starlette + uvicorn stack for
# the streamable-HTTP transport.
mcp>=1.27
uvicorn[standard]>=0.32
# Amendment #25 (2026-04-22, bge-reranker-swap) adds
# sentence-transformers as a runtime dep. It backs
# graphiti_core.cross_encoder.bge_reranker_client.BGERerankerClient,
# which make_graphiti() now passes explicitly as cross_encoder to
# avoid graphiti-core silently defaulting to OpenAIRerankerClient
# (which requires OPENAI_API_KEY — a billed-API coupling we rejected
# for the LLM path in amendment #8). Pin floor matches graphiti-core
# 0.28.2's declared `sentence-transformers` optional-extra.
sentence-transformers>=3.2.1
# dev / test
pytest>=9.0
pytest-asyncio>=1.0
