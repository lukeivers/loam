"""``loam amend`` — amendment-dispatch tooling subcommand.

The subcommand surface (validate / apply / seal / template / new-plan)
is carried forward from the pre-M1g ``pos-amend`` CLI per M1g
(``docs/plans/oss-v0-1-0-publish-rename-1g.md``); the public
behaviour is unchanged — only the binary + import paths rebrand.
"""
