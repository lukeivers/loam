schema_version: 3
amendment:
  # number: assigned at apply-time per feedback_version_numbers_at_release_time.
  number: null
  slug: frame-kernel-subagent-envelope-cwd-fallback
  title: "frame-kernel SubagentStart envelope cwd-fallback — single-purpose corrective: `bundle.py::parse_envelope` resolves workspace_root from the REAL Claude Code SubagentStart envelope shape. Tier-0 live finding (pos3 watched activation, 2026-06-10): real envelopes carry the common-input `cwd` field, NOT `workspace.project_dir`; 1a's parse_envelope read ONLY workspace.project_dir, so on every REAL dispatch workspace_root=None and all three bundle tiers degraded to placeholders (verified by a probe agent reading its own injected context), while synthetic project_dir envelopes produced the full bundle. The fix mirrors the cwd-fallback already shipped in-fence by 1b's frame_judge.py (priority: workspace.project_dir when present, else cwd). AC.EWR.1 (cwd-only envelope resolves; bundle equivalence; project_dir priority) + AC.EWR.S (outcome-altitude: the REAL cwd-only envelope shape end-to-end through the production hook entry-point yields a NON-degraded microkernel tier against the real on-disk kernel — the real-shape twin of AC.SACH.S). Existing AC.SACH.*/AC.DMP.*/AC.SSFC.* contracts + tests unchanged (fail-soft exit-0 preserved; an envelope with neither field still degrades as before)."

# BASELINE: 6ea60d0 — pre-amendment tip (HEAD at dispatch), per the
# established BASELINE-as-pre-amendment-tip pattern. Surface: the
# workspace-root resolution block in
# framework/frame-kernel/src/loam/frame_kernel/bundle.py::parse_envelope
# (cwd fallback added, mirroring frame_judge.py:370-379) + two new test
# files (AC.EWR.1 / AC.EWR.S) + this plan/manifest under docs/plans/.
baseline: "6ea60d0b87d6b83ceb6e9580eee82c451687ff8d"

plan: docs/plans/frame-kernel-subagent-envelope-cwd-fallback.md
plan_doc_ref: docs/plans/frame-kernel-subagent-envelope-cwd-fallback.md

ac_count: 2
smoke_outcome: "AC.EWR.1 + AC.EWR.S GREEN (6 tests); full frame-kernel suite 68/68 under the venv Python. RED-on-revert verified: AC.EWR.S FAILS against pre-fix bundle.py (degraded microkernel tier on the real cwd-only envelope) and passes post-fix."

seal_description: "frame-kernel SubagentStart envelope cwd-fallback — parse_envelope resolves workspace_root from real envelopes (cwd), all three bundle tiers populate on real dispatches"

# EXTEND the sealed `frame-kernel` component (frozen_baseline: false
# advances the seal baseline — the intended fence-widening of an EXTEND).
# Surface inside the fence: bundle.py + new AC.EWR.* tests. No other
# sealed component's fence widens; frame_judge.py / the hook script /
# the persona memory surface are untouched.
components:
  - name: frame-kernel
    seal_test: framework/frame-kernel/tests/test_no_sealed_amendments.py
    sidecar: framework/frame-kernel/tests/SEAL_COMMIT
    frozen_baseline: false
    extra_allowed_prefixes:
      - framework/frame-kernel/
      - kernel/

# Universal admissions — per amendment #22 ruling #3.
universal_paths:
  prefixes:
    - docs/plans/
    - docs/experiments/
    - docs/design/
    - docs/examples/
  files:
    - CLAUDE.md
    - docs/STATE.md
    - docs/release-roadmap.md
    - docs/release-roadmap-dependency-map.md
    - docs/release-process.md
    - docs/release-versioning-policy.md
    - docs/FUTURE_IDEAS.md
    - docs/FUTURE_IDEAS_DRAFT.md
    - docs/architecture.md
    - docs/components/index.md
    - docs/public-surface-manifest.md
    - README.md
    - install-from-source.txt

narrative:
  target: docs/plans/sealed/frame-kernel-subagent-envelope-cwd-fallback.md
  body: |
    # frame-kernel — SubagentStart envelope cwd-fallback

    Single-purpose corrective to slice 1a's envelope contract. Sealed
    local; rides the next release after v1.4.0 (publish owner-gated).

    THE BUG (Tier-0, pos3 watched activation, 2026-06-10):
    `bundle.py::parse_envelope` resolved workspace_root ONLY from
    `envelope["workspace"]["project_dir"]`. Real Claude Code
    SubagentStart envelopes carry the standard hook common-input `cwd`
    field, not that shape. Result: on every REAL dispatch
    workspace_root=None and ALL THREE bundle tiers (microkernel /
    workstream / memory) degraded to structured placeholders — verified
    by a probe agent reading its own injected context (marker present,
    all tiers `[... unavailable ...]`), while a synthetic
    workspace.project_dir envelope produced the full 6.3KB bundle. The
    injection plumbing + AC.SACH.4 fail-soft contracts worked exactly
    as sealed; the envelope contract was wrong.

    THE FIX: parse_envelope gains the cwd fallback — priority order
    `workspace.project_dir` when present, else `cwd` — mirroring the
    pattern slice 1b already shipped in-fence at
    `frame_judge.py` (workspace-root resolution with the cwd
    common-input fallback). 1a and 1b now agree on the envelope
    contract.

    AC.EWR.1 — an envelope carrying ONLY `cwd` (the real observed
    shape) resolves workspace_root; the composed bundle is identical to
    the project_dir-envelope bundle for the same root; a both-present
    envelope prefers workspace.project_dir.

    AC.EWR.S (outcome-altitude) — the REAL envelope shape (cwd-only)
    driven end-to-end through the production hook entry-point
    (`subagent_start_context.py` subprocess, the AC.SACH.S pattern)
    yields an injected additionalContext whose microkernel tier is
    NON-degraded against the real on-disk `kernel/loam-microkernel.md`.
    This is the regression test the original AC.SACH.S probe missed —
    that probe's synthetic envelope used the project_dir shape, which
    is why the contract gap shipped unobserved.

    Unchanged: fail-soft exit-0 (AC.SACH.4) — an envelope with NEITHER
    field still degrades exactly as before; task-text handling;
    frame_judge.py; the hook script; all existing AC.SACH.* /
    AC.DMP.* / AC.SSFC.* tests pass untouched.
