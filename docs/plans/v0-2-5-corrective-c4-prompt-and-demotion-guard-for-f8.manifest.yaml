---
schema_version: 3
amendment:
  # number omitted — schema v3 deprecates amendment.number per
  # AC.DPS1.10 + AC.DPS1.11. Identification by slug only.
  slug: v0-2-5-corrective-c4-prompt-and-demotion-guard-for-f8
  title: "v0.2.5 corrective C4 — synthesis prompt-engineering strengthens VERIFIED two-source rule + demotion-guard normalizes VERIFIED-without-two-sources to PLAUSIBLE before validator (validator stays strict); AC.OBJX.5 methodology refined (band-rule violations demote-able; structural malformation still raises); 5 named ACs + meta-AC for full-suite green; closes F8 BLOCKER from v0.2.5 corrective C3"

# BASELINE: <pending> — source-edit feat commit covering synthesis.py
# (prompt strengthen + demotion-guard) + 2 new test files under
# plugins/dev-sdlc/odd-extractor/tests/.
#
# Corrective amendment landing the F8 fix surfaced by v0.2.5 corrective C3
# (the C3.3 outcome-altitude AC test caught the LLM producing VERIFIED-banded
# objectives without two-source evidence; Pydantic validator correctly rejects;
# entire synthesis stage exits 2; AC.HARD.1-5 cascade-fail). Master plan §3
# v0.2.5 + the C4 dispatch brief explicitly authorize "prompt-engineering as
# primary + demotion-guard as safety net".
#
# Commit ladder pre-apply (relative to C3 outcome):
#   89f97c6 — v0.2.5 corrective C3 seal.
#   b3a5812 — v0.2.5 C3 §14 backfill.
#   189c6db — v0.2.5 C3 STATE entry + FIDRAFT F8 BLOCKER capture (HEAD).
#   <plan-doc commit lands NEXT (THIS plan-doc + manifest), then
#    source-edit feat commit (BASELINE pinned to that SHA), then
#    `loam amend apply <manifest>` lands as single merged
#    manifest+apply commit per AC.DPS1.6 (v3 schema)>
#   <`loam amend seal --plan-doc <abs path> <manifest>` lands as
#    deterministic seal commit per AC.DPS2.{1,4,6}>
#   <post-seal commit backfills this plan-doc's §14 per AC.D-sa.7>
#
# Cross-component fence — dev-sdlc only:
#   - EDIT plugins/dev-sdlc/odd-extractor/src/loam_odd_extractor/synthesis.py
#       1. Strengthen _SYSTEM_PROMPT BANDING RULE — add explicit
#          instruction: "if you cannot supply tests AND
#          (readme_excerpts OR design_doc_refs), band as PLAUSIBLE,
#          NEVER VERIFIED".
#       2. Add helper _apply_band_demotion_guard (or inline pass
#          inside _validate_rows) — for each VERIFIED-banded row
#          missing both readme_excerpts AND design_doc_refs, demote
#          to PLAUSIBLE before per-row Pydantic validation; log the
#          demotion at WARN level naming the objective_id.
#       3. Add `import logging` + module-level logger if not present.
#   - NEW test plugins/dev-sdlc/odd-extractor/tests/test_AC_V025_C4_2_demotion_guard.py
#                    — AC.V025-C4.2: feed _validate_rows a payload
#                      with VERIFIED-banded row missing two sources;
#                      assert demotion to PLAUSIBLE + log entry +
#                      validator passes.
#   - NEW test plugins/dev-sdlc/odd-extractor/tests/test_AC_V025_C4_3_cli_live_outcome_altitude_post_fix.py
#                    — AC.V025-C4.3: cli.main(--live) against
#                      jsts-playwright-app fixture WITHOUT monkeypatch
#                      and WITHOUT pre-arrangement; skips cleanly if
#                      ANTHROPIC_API_KEY unset OR anthropic SDK
#                      unimportable; otherwise asserts clean exit +
#                      ≥1 objective + backing-map.yaml exists.
#   - NO edits to spec.py validator (validator stays strict per
#     §14 method-decision).
#   - NO edits to other source files.
#
# AC families locked per plan-doc §2:
#   AC.V025-C4.1 — Synthesis system prompt strengthened (outcome-altitude: false; structural verification).
#   AC.V025-C4.2 — Demotion-guard at parsing layer (outcome-altitude: false; unit test).
#   AC.V025-C4.3 — Outcome-altitude AC test post-fix (outcome-altitude: true; HARD per-cycle probe).
#   AC.V025-C4.4 — meta: pre-existing odd-extractor test suite remains green post-fix.
#   AC.V025-C4.5 — AC.OBJX.5 methodology refinement documented (audit-trail).
#
# Smoke (REALISTIC CONDITION — applicable dimensions per
# smoke-test-discipline §6 + plan-doc §5):
#   D1 cold-state — fresh tmp workspace; cli.main(--live) against
#     jsts-playwright-app + REAL Anthropic SDK produces non-empty
#     objectives.yaml + backing-map.yaml (or skips cleanly if no
#     API key). Verified by AC.V025-C4.3.
#   D2 steady-state — inherited from v0.2.3 idempotence; not re-verified.
#   D5 cross-session — inherited from v0.2.4; not re-verified.
#   D6 telemetry-floor — structural; demotion-guard adds a WARN-level
#     log entry but no new audit-log event-kind.
#   D3/D4 — n/a structurally (one-shot CLI).
#   PLUS: full-suite green sweep — AC.V025-C4.4 verifies 815+ pass.
#
# Bookkeeping: corrective amendment outside any v0.2.5 numbered
# cycle ladder (v0.2.5 has no cycles; the release was pure smoke-
# gate). Sealed-content invariant: pre-existing AC.* tests in
# odd-extractor remain green; only one production source file
# edited (synthesis.py); two new test files added.

# BASELINE pinned to the source-edit feat commit (v0.2.5 corrective C4).
# Initial pin to HEAD-pre-build; re-pinned to the source-edit feat commit
# in a follow-up baseline-pin commit (per C3 precedent).
baseline: "189c6db"

plan: docs/plans/v0-2-5-corrective-c4-prompt-and-demotion-guard-for-f8.md

# Top-level plan_doc_ref — schema v3 collapsed-narrative pointer.
# `loam amend seal` synthesizes the narrative body from this ref
# plus ac_count + smoke_outcome below.
plan_doc_ref: docs/plans/v0-2-5-corrective-c4-prompt-and-demotion-guard-for-f8.md

ac_count: 5
smoke_outcome: "C4.1 prompt strengthened; C4.2 demotion-guard normalizes VERIFIED→PLAUSIBLE; C4.3 outcome-altitude post-fix test; C4.4 odd-extractor green; C4.5 AC.OBJX.5 methodology refined"

components:
  - name: dev-sdlc
    seal_test: plugins/dev-sdlc/tests/test_no_sealed_amendments.py
    sidecar: plugins/dev-sdlc/tests/SEAL_COMMIT
    frozen_baseline: false
    extra_allowed_prefixes: []

# Universal admissions — per amendment #22 ruling #3 + v0.2.5
# corrective C1+C2/C3 precedent. `docs/plans/` admits the
# plan-before-code paper trail.
universal_paths:
  prefixes:
    - docs/plans/
  files:
    - CLAUDE.md
    - docs/odd-in-loam.md
    - docs/odd-methodology.md
    - docs/STATE.md
    - docs/FUTURE_IDEAS_DRAFT.md

narrative:
  target: plugins/dev-sdlc/seals/SEAL_COMMIT.v0-2-5-corrective-c4-prompt-and-demotion-guard-for-f8
  # body omitted — schema v3 with plan_doc_ref synthesizes a 5-15
  # line summary at seal time per AC.DPS2.{1,4}.
