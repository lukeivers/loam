schema_version: 3
amendment:
  # number omitted — schema v3 deprecates amendment.number per
  # AC.DPS1.10 + AC.DPS1.11. Identification by slug only.
  slug: v0-1-9-cycle-3-skills-and-cleanup
  title: "v0.1.9 Cycle 3 — 6 dev-sdlc SKILLs second pass + audit-allowlist cleanup; single-component fence on plugins/dev-sdlc/"

# BASELINE: <pending> — source-edit feat commit covering this Cycle 3
# scope under plugins/dev-sdlc/:
#   skills/seal-narrative-writer/SKILL.md (NEW)
#   skills/plan-docs-author/SKILL.md (NEW)
#   skills/hook-violation-recovery/SKILL.md (NEW)
#   skills/component-scaffold-author/SKILL.md (NEW)
#   skills/graceful-fallthrough-with-detection/SKILL.md (NEW)
#   skills/loam-amend-status-quick/SKILL.md (NEW)
#   tools/loam-mode/tests/test_partition_references.py (EDIT —
#     shrink KNOWN_CROSS_MODE_DEBT from 5 to 1 entry; refresh comment)
#   tests/test_AC_SKILLS_DSDLC2_1_seal_narrative_writer_skill_present.py (NEW)
#   tests/test_AC_SKILLS_DSDLC2_2_plan_docs_author_skill_present.py (NEW)
#   tests/test_AC_SKILLS_DSDLC2_3_hook_violation_recovery_skill_present.py (NEW)
#   tests/test_AC_SKILLS_DSDLC2_4_component_scaffold_author_skill_present.py (NEW)
#   tests/test_AC_SKILLS_DSDLC2_5_graceful_fallthrough_with_detection_skill_present.py (NEW)
#   tests/test_AC_SKILLS_DSDLC2_6_loam_amend_status_quick_skill_present.py (NEW)
#   tests/test_AC_SKILLS_DSDLC2_7_all_twelve_skills_discovered.py (NEW)
#
# Cycle 3 is the FINAL cycle of v0.1.9. After Cycle 3 seals, the
# release-level SOFT smoke gate (master plan §5) runs. If green, the
# v0.1.9 release-level rollup (STATE.md + roadmap §8 + eric-final §2)
# backfills SHIPPED status. Tag is local-only (NOT pushed) until Luke
# gates.
#
# Commit ladder pre-apply:
#   790807d — v0.1.9 Cycle 1 seal.
#   2f154c8 — Cycle 1 §14 backfill.
#   3162d2f — Cycle 1 §9 backfill.
#   48a4758 — Cycle 2 plan-doc.
#   17d02ca — Cycle 2 source-edit BASELINE.
#   68859d9 — Cycle 2 manifest+apply.
#   0dc557e — Cycle 2 seal.
#   a61d4ff — Cycle 2 §14 backfill.
#   2a2a839 — Cycle 2 §9 backfill (most recent commit on pos-v2 branch).
#   <plan-doc commit lands NEXT (THIS plan-doc + manifest), then
#    source-edit feat commit (BASELINE pinned to that SHA), then
#    `loam amend apply <manifest>` lands as single merged
#    manifest+apply commit per AC.DPS1.6 (v3 schema)>
#   <`loam amend seal --plan-doc <abs path> <manifest>` lands as
#    deterministic seal commit per AC.DPS2.{1,4,6}>
#   <post-seal commit backfills this plan-doc's §14 + master plan §9
#    + roadmap §8 + eric-final-delivery §2 per AC.D-sa.7>
#
# Single-component fence — dev-sdlc:
#   - Six new SKILL.md packages at plugins/dev-sdlc/skills/<name>/SKILL.md.
#   - Seven new pytest files at plugins/dev-sdlc/tests/.
#   - One edit to plugins/dev-sdlc/tools/loam-mode/tests/test_partition_references.py
#     (KNOWN_CROSS_MODE_DEBT shrink + comment refresh).
#
# AC families locked per plan-doc §4:
#   AC.SKILLS-DSDLC2.1 — seal-narrative-writer SKILL.md.
#   AC.SKILLS-DSDLC2.2 — plan-docs-author SKILL.md.
#   AC.SKILLS-DSDLC2.3 — hook-violation-recovery SKILL.md.
#   AC.SKILLS-DSDLC2.4 — component-scaffold-author SKILL.md.
#   AC.SKILLS-DSDLC2.5 — graceful-fallthrough-with-detection SKILL.md.
#   AC.SKILLS-DSDLC2.6 — loam-amend-status-quick SKILL.md.
#   AC.SKILLS-DSDLC2.7 — All 12 dev-sdlc SKILLs auto-discoverable.
#   AC.SKILLS-DSDLC2.8 — Regression test per SKILL (≥7 new tests).
#   AC.AUDIT-CLEANUP.1 — KNOWN_CROSS_MODE_DEBT shrunk to 1 entry.
#   AC.AUDIT-CLEANUP.2 — test_AC_F3_always_loaded_no_dev_refs passes.
#   AC.AUDIT-CLEANUP.3 — Source-file scan of removed pairs returns 0
#                          matches.
#
# Smoke (REALISTIC CONDITION — all 6 dimensions per
# smoke-test-discipline §6 + plan-doc §6):
#   D1 cold-state — fresh canonical workspace shows all 12 dev-sdlc
#     SKILLs in `/` menu. Verified by
#     test_AC_SKILLS_DSDLC2_7_all_twelve_skills_discovered.py walking
#     plugins/dev-sdlc/skills/. Audit-allowlist shrunk to 1 entry.
#   D2 steady-state — SKILLs survive across N (≥5) sessions; allowlist
#     test stable. Inherited from filesystem persistence + deterministic
#     test logic.
#   D3 restart — process restart preserves SKILL discovery. Inherited
#     from filesystem persistence (no long-running daemon).
#   D4 reboot — macOS reboot equivalent — filesystem state survives.
#   D5 cross-session — SKILLs visible after /clear. Inherited from
#     Anthropic native discovery + v0.1.7 Cycle 3 mechanism.
#   D6 telemetry-floor — SKILL discovery events emit per layered-skill
#     audit-trail floor (inherited from v0.1.7 Cycle 3).
#   PLUS: full-suite green sweep — at least 501 dev-sdlc parent tests
#     at HEAD e4512b9 (Cycle 5 seal) all pass post-Cycle-3; halt +
#     surface on any regression. Cycle 3 adds ~7 new test functions;
#     expected post-cycle test count ≥508.
#   PLUS: release-level SOFT smoke gate (master plan §5) runs AFTER
#     Cycle 3 seals — separate from this amendment cycle's smoke.
#     v0.1.9 release-level rollup (STATE.md + roadmap §8 + eric-final
#     §2) backfills only after release-level smoke passes.
#
# Bookkeeping note: this is the ELEVENTH amendment to use schema v3
# (DPS1 / DPS2 / v0.1.8 Cycles 1-5 / v0.1.9 Cycles 1-2 / THIS =
# v0.1.9 Cycle 3, the final cycle of v0.1.9).
# v3 fields exercised: plan_doc_ref + ac_count + smoke_outcome at the
# manifest level; single-component sweep at seal time.

# BASELINE pinned to the source-edit feat commit. Updated post-
# source-commit by the build agent.
baseline: "d8e3f01"

plan: docs/rebuild/plans/v0-1-9-cycle-3-skills-and-cleanup.md

# Top-level plan_doc_ref — schema v3 collapsed-narrative pointer.
# `loam amend seal` synthesizes the narrative body from this ref
# plus ac_count + smoke_outcome below.
plan_doc_ref: docs/rebuild/plans/v0-1-9-cycle-3-skills-and-cleanup.md

ac_count: 11
smoke_outcome: "All 6 dims (D1 12-SKILL + 1-entry allowlist; D2/D3/D4 filesystem; D5 native discovery; D6 audit-floor); 71 cycle tests green; release-level SOFT smoke runs post-seal"

components:
  - name: dev-sdlc
    seal_test: plugins/dev-sdlc/tests/test_no_sealed_amendments.py
    sidecar: plugins/dev-sdlc/tests/SEAL_COMMIT
    frozen_baseline: false
    extra_allowed_prefixes: []

# Universal admissions — per amendment #22 ruling #3 + v0.1.8 Cycle 5
# + v0.1.9 Cycles 1-2 precedent. `docs/rebuild/plans/` admits the
# plan-before-code paper trail. No `odd-methodology.md` edit required
# (Cycle 3 ships SKILLs + an allowlist trim, not methodology). STATE.md
# / roadmap / eric-final rollup happens post-release-level-smoke per
# plan-doc §9.
universal_paths:
  prefixes:
    - docs/rebuild/plans/
  files:
    - CLAUDE.md
    - docs/odd-in-loam.md
    - docs/odd-methodology.md
    - docs/rebuild/STATE.md

narrative:
  target: plugins/dev-sdlc/seals/SEAL_COMMIT.v0-1-9-cycle-3-skills-and-cleanup
  # body omitted — schema v3 with plan_doc_ref synthesizes a 5-15
  # line summary at seal time per AC.DPS2.{1,4}. The summary
  # surfaces title + slug + components + baseline + amendment SHA +
  # plan_doc_ref + ac_count + smoke_outcome.
