schema_version: 1
amendment:
  number: 148
  slug: loam-bafi-stale-test-retire
  title: "Retire stale `start-project`-absence assertion in Batch A's outcome-altitude test (post-A-PROMOTE re-correction; single-assertion deletion)"

# BASELINE: current main HEAD `120c16d` (`docs(plans): record amendment
# #147 commit SHAs in method-decision register`). Verified Tier-0 this
# turn via `git rev-parse HEAD`. The predecessor seal of load-bearing
# interest is A-PROMOTE `389dac7` (which restored the `start-project`
# reference in docs/architecture.md, semantically reversing
# D-BAFI.START-PROJECT and making the BAFI test assertion at lines
# 71-75 stale-RED).
#
# Commit ladder (expected):
#   <plan-doc + manifest commit (this manifest)>
#   <test-edit commit — delete lines 71-75 of plugins/loam-skills/
#    tests/test_AC_BAFI_S_post_fix_state.py + add audit-trail comment
#    per AC.BAFISTR.RETIRE + AC.BAFISTR.AUDIT>
#   <`loam amend apply` auto-commit>
#   <`loam amend seal --plan-doc docs/plans/loam-bafi-stale-test-
#    retire.md --allow-untracked-globs 'docs/plans/promote-multi-
#    channel-extractor-and-iteration-loop-family.md'` deterministic
#    seal commit; T1.4 archives this plan-doc + manifest to
#    docs/plans/sealed/ via the post-#134 plan_archive.py integration
#    (Strategy 1 match on full slug). §14 auto-backfill via #141's
#    decoupled path.>
#
# Single-component fence (loam-skills — the only component whose tree
# is touched, via plugins/loam-skills/tests/test_AC_BAFI_S_post_fix_
# state.py). No universal_paths entries beyond docs/ (plan-doc +
# manifest archival to docs/plans/sealed/ on T1.4).
#
# AC families per docs/plans/loam-bafi-stale-test-retire.md §4:
#   AC.BAFISTR.RETIRE — the `assert "start-project" not in body`
#     assertion at lines 71-75 of test_AC_BAFI_S_post_fix_state.py
#     is removed; the function's other 4 assertions remain
#     unmodified in substance.
#   AC.BAFISTR.AUDIT — an audit-trail comment or docstring paragraph
#     inside the function names the retirement reason (substrings
#     `A-PROMOTE` or `389dac7` AND `D-BAFI.START-PROJECT` present).
#   AC.BAFISTR.S — outcome-altitude smoke: full
#     test_AC_BAFI_S_post_fix_state.py file passes 5/5; loam-skills
#     seal-test (test_no_sealed_amendments.py) passes; RED-on-
#     mutation proof via temporary re-introduction of the deleted
#     assertion.
baseline: "120c16dfc20bc6db16e4e52580acba8f5b597f78"
# Verified Tier-0 this turn: `git rev-parse HEAD` returns the full
# 40-char SHA above. Build agent re-walks at apply-time per the
# walk-forward discipline (#142 D-PASH.BASELINE-WALK); this baseline
# is the plan-time pin. No `chore(amend-fixup):` commits expected
# between A-PROMOTE seal `389dac7` (immediate predecessor) and HEAD
# `120c16d` (the post-A-PROMOTE §14 SHA-backfill commit).

plan: docs/plans/loam-bafi-stale-test-retire.md

seal_description: |
  Retire the single stale `assert "start-project" not in body`
  assertion at lines 71-75 of plugins/loam-skills/tests/test_AC_BAFI_
  S_post_fix_state.py::test_AC_BAFI_ARCH_skills_section_reflects_
  current_reality. The assertion encoded Batch A (amendment #145)
  decision D-BAFI.START-PROJECT, which removed the `start-project`
  reference from docs/architecture.md. A-PROMOTE-START-PROJECT
  (amendment #147, seal `389dac7`) semantically reversed that decision
  by promoting the SKILL to discoverable subdirectory shape AND
  restoring the architecture.md reference with refined wording naming
  the subdirectory shape + auto-symlink mechanism. From A-PROMOTE's
  seal forward, the assertion is stale-RED in canonical.

  A-PROMOTE plan-doc §10 D-SPDISC.BAFI-TEST-COLLISION ratified path
  (a) at plan-author time — "builder updates the BAFI test as part of
  this amendment's source-edit commit (preferred — single semantic
  correction)" — but at build-time, A-PROMOTE's builder honored a
  separate dispatcher directive ("don't touch sealed Batch A test")
  and surfaced the staleness rather than modifying. This retire
  amendment closes that surfaced finding as a separate follow-on,
  preserving the semantic correctness ratified at A-PROMOTE plan-
  author time.

  AC.BAFISTR.RETIRE — surgical deletion of lines 71-75 only (Option A
  from the dispatcher brief). Option B (whole-function deletion) was
  rejected per Tier-0 evidence at plan-author time: full-file pytest
  (4 passed, 1 failed) confirmed the function's other 4 assertions
  (stale-framing absence + loam-skills presence + dev-sdlc presence
  + _symlink_plugin_skills presence) are all load-bearing properties
  of current docs/architecture.md. Whole-function deletion would
  silently drop 4 still-valid AC.BAFI.ARCH guards.

  AC.BAFISTR.AUDIT — audit-trail comment inside the function naming
  the retirement reason (A-PROMOTE seal `389dac7` + D-BAFI.START-
  PROJECT). In-file context for readers who don't load the seal
  narrative; marginal cost, marginal benefit, on-shape with the
  amendment's purpose.

  AC.BAFISTR.S — outcome-altitude smoke: full test_AC_BAFI_S_post_
  fix_state.py file passes 5/5 against canonical docs/architecture.
  md (production-altitude filesystem-walk read, no pre-arranged
  state). Mutation-proof via temporary re-introduction at build-time.

  Five plan-author halt-and-surface findings (plan §10) all surfaced
  + autonomous-decision-recorded per `feedback_test_against_
  operational_objective_before_escalating`: F1 (Option A wins over
  Option B per Tier-0 evidence); F2 (AC.BAFISTR.AUDIT recommended
  but builder may collapse if scope-tightness preference wins);
  F3 (no widening required — only one BAFI test function affected);
  F4 (sealed Batch A AC.BAFI.ARCH text NOT modified — A-PROMOTE's
  seal narrative is the canonical audit surface); F5 (seal-time
  `--allow-untracked-globs` admission per dispatcher direction
  unrelated to scope).

  Test-only amendment; no behavior change for any production code
  path; no source-code edits. Composes with A-PROMOTE (closes the
  surfaced finding) + Batch A (removes a now-stale assertion that
  was correct at Batch A's seal time). Sub-cycle-time amendment
  (estimated 8-12 min AI-time per `feedback_duration_estimation_
  rubric`).

components:
  - name: loam-skills
    seal_test: plugins/loam-skills/tests/test_no_sealed_amendments.py
    sidecar: plugins/loam-skills/tests/SEAL_COMMIT
    frozen_baseline: false
    extra_allowed_prefixes: []
    # Single-component fence — plugins/loam-skills/tests/test_AC_BAFI_
    # S_post_fix_state.py is the only file touched outside docs/.
    # Sidecar currently reads `d5b022fd4439bad4afecb3f18e6f57188c4aaddb`
    # (the A-PROMOTE plan-doc commit, per the walk-forward discipline
    # captured by A-PROMOTE's seal); advances to this amendment's seal
    # SHA at apply time.

universal_paths:
  prefixes:
    - "docs/"        # docs/plans/loam-bafi-stale-test-retire.md +
                     # docs/plans/loam-bafi-stale-test-retire.manifest.yaml
                     # (this plan-doc + manifest archival to
                     # docs/plans/sealed/ at T1.4).
  files: []

narrative:
  target: docs/plans/sealed/loam-bafi-stale-test-retire.md
  body: |
    Retire the single stale `assert "start-project" not in body`
    assertion at lines 71-75 of plugins/loam-skills/tests/test_AC_
    BAFI_S_post_fix_state.py::test_AC_BAFI_ARCH_skills_section_
    reflects_current_reality. The assertion encoded Batch A
    (amendment #145) decision D-BAFI.START-PROJECT, which removed
    the `start-project` reference from docs/architecture.md.
    A-PROMOTE-START-PROJECT (amendment #147, seal `389dac7`)
    semantically reversed that decision by promoting the SKILL to
    discoverable subdirectory shape AND restoring the architecture.
    md reference with refined wording naming the subdirectory shape
    + auto-symlink mechanism. From A-PROMOTE's seal forward, the
    assertion was stale-RED in canonical; this amendment closes the
    surfaced finding A-PROMOTE's builder raised by Option A —
    surgical deletion of the single stale assertion, preserving the
    function's 4 other still-load-bearing assertions.

    AC.BAFISTR.RETIRE — surgical deletion of lines 71-75 only. The
    function's other 4 assertions (stale-framing absence, loam-
    skills presence, dev-sdlc presence, _symlink_plugin_skills
    presence) remain unmodified in substance; full-file pytest at
    plan-author time confirmed they are all load-bearing properties
    of current docs/architecture.md. Whole-function deletion
    (Option B from the dispatcher brief) was rejected per the
    evidence.

    AC.BAFISTR.AUDIT — audit-trail comment inside the function names
    the retirement reason (A-PROMOTE seal `389dac7` + D-BAFI.START-
    PROJECT). In-file context for readers who don't load the seal
    narrative.

    AC.BAFISTR.S — outcome-altitude smoke: full test_AC_BAFI_S_post_
    fix_state.py file passes 5/5 against canonical docs/architecture.
    md (production-altitude filesystem-walk read, no pre-arranged
    state). RED-on-mutation: temporarily re-introduce the deleted
    assertion + observe RED + re-apply deletion.

    Five plan-author halt-and-surface findings (plan §10) all
    surfaced + autonomous-decision-recorded: F1 (Option A wins per
    Tier-0 evidence); F2 (AUDIT comment recommended, builder may
    collapse); F3 (no widening — only one function affected); F4
    (sealed Batch A AC text NOT modified); F5 (seal-time `--allow-
    untracked-globs` admission per dispatcher direction).

    Test-only amendment; no behavior change for any production code
    path; no source-code edits. Composes with A-PROMOTE (closes
    surfaced finding) + Batch A (removes now-stale assertion).
    Sub-cycle-time amendment.
