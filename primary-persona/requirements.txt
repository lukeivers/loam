# Runtime deps (brief §"Permitted dependencies"):
#   Python stdlib (pathlib, asyncio, uuid, sqlite3, dataclasses)
#   + pydantic, pyee, opentelemetry-api, opentelemetry-sdk, PyYAML
# No other third-party library is imported by the primary-persona runtime.
pydantic>=2
pyee>=11
opentelemetry-api>=1.25
opentelemetry-sdk>=1.25
PyYAML>=6
# Test infrastructure (dev-only):
pytest>=8
pytest-asyncio>=0.23
