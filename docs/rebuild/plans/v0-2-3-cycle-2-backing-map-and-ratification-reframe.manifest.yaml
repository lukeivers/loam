---
schema_version: 3
amendment:
  # number omitted — schema v3 deprecates amendment.number per
  # AC.DPS1.10 + AC.DPS1.11. Identification by slug only.
  slug: v0-2-3-cycle-2-backing-map-and-ratification-reframe
  title: "v0.2.3 Cycle 2 — Backing-implementation map + ratification reframe; new backing_map.py module + populate_backing_map heuristic-pre-filter + LLM-pass classifier; objective-altitude ratification factory + apply path; v1→v2 ratification-state schema migration; PM-side altitude-tagged provenance; single-component fence on plugins/dev-sdlc/odd-extractor/"

# BASELINE: <pending> — source-edit feat commit covering new module
# at plugins/dev-sdlc/odd-extractor/src/loam_odd_extractor/backing_map.py
# + extending spec.py with BackingMap/BackingMapEntry/EvidenceRowRef/
# OrphanRow/ObjectiveRatificationAction models + extending ratify.py
# (parallel objective-altitude factories + apply path; v1 paths
# preserved unchanged) + extending ratification_state.py
# (RatificationStateV2 with altitude_index + pending_targets;
# schema_version=2; auto-migration on read with v1 backup) +
# extending generate.py (post-synthesis backing-map population call)
# + extending verify.py (per-objective backing-row counts +
# orphan-row section in contract-draft.md) + extending
# observability.py (1 backing-map + 12 ratification altitude×action
# event_kinds) + extending cli.py (objective-altitude ratify routing)
# + per-AC tests + integration smoke + 6 fixtures (3 backing-map +
# 3 ratification).
#
# Cycle 2 is the second of three cycles in v0.2.3. Cycle 3 follows:
#   - Cycle 3: PR-safety + continuous-watch reframe (release SOFT
#              smoke gate). Two-component fence: pr-safety/ (PRIMARY)
#              + odd-extractor incremental*.py (secondary). Gate
#              consumes objectives + backing-map; watch flags
#              objective-altitude coverage shift (AC.PRSGOBJ.* +
#              AC.WATCHOBJ.*).
# After Cycle 3 seals + SOFT smoke green, the v0.2.3 SHIPPED rollup
# commit lands on canonical, backfilling STATE.md + roadmap §8 +
# odd-rebuild master plan §3 v0.2.3 row + master plan §9 per-cycle
# SHA register. Tag deferred until v0.2.5 ship gate.
#
# Commit ladder pre-apply:
#   ada74e1 — v0.2.2 grounding propagation apply.
#   5eda09d — v0.2.2 seal.
#   ebca7dc — v0.2.2 §14 backfill.
#   35155fd — v0.2.3 master plan + cycle decomposition.
#   1e20037 — v0.2.3 Cycle 1 manifest+apply (multi-source objective
#              synthesis).
#   9b9f87c — v0.2.3 Cycle 1 seal.
#   66de327 — v0.2.3 Cycle 1 §14 backfill.
#   914809d — v0.2.3 master plan §9 Cycle 1 SHA backfill.
#   <plan-doc commit lands NEXT (THIS plan-doc + manifest), then
#    source-edit feat commit (BASELINE pinned to that SHA), then
#    `loam amend apply <manifest>` lands as single merged
#    manifest+apply commit per AC.DPS1.6 (v3 schema)>
#   <`loam amend seal --plan-doc <abs path> <manifest>` lands as
#    deterministic seal commit per AC.DPS2.{1,4,6}>
#   <post-seal commit backfills this plan-doc's §14 + master plan §9
#    Cycle 2 SHA row per AC.D-sa.7>
#
# Single-component fence — plugins/dev-sdlc/odd-extractor/:
#   - NEW module under plugins/dev-sdlc/odd-extractor/src/
#     loam_odd_extractor/:
#       backing_map.py         — populate_backing_map orchestrator;
#                                 heuristic pre-filter (domain-keyword
#                                 substring + kind=test weighting +
#                                 per-language conventions; top-K=8) +
#                                 LLM-pass classifier scoring per
#                                 EVAL_DIMENSIONS (file-path-relevance
#                                 / symbol-name-relevance / domain-
#                                 match / outcome-shape-match) +
#                                 orphan classification + cost-band
#                                 enforcement ($0.05–$2.00; default
#                                 $0.50 ceiling) + halt-trigger on
#                                 >200 candidate pairs after pre-filter.
#   - EDITS to existing modules (additive only):
#       spec.py        — extend with new Pydantic/dataclass models
#                        (BackingMap + BackingMapEntry + EvidenceRowRef
#                        + OrphanRow + ObjectiveRatificationAction);
#                        existing models unchanged.
#       ratify.py      — extend with promote_objective / demote_objective
#                        / edit_objective / reject_objective factories
#                        + analogous constraint + capability factories
#                        + apply_objective_ratification_action apply
#                        path + altitude-tagged provenance in
#                        enqueue_ratification_batch. v0.1.8 BandedAC
#                        path PRESERVED unchanged (parallel paths).
#       ratification_state.py — extend with RatificationStateV2
#                                (altitude_index + pending_targets);
#                                bump _RATIFICATION_STATE_SCHEMA_VERSION
#                                = 2; auto-migrate v1 files on read
#                                (atomic backup + v2 write); v1 dataclass
#                                + accessors preserved.
#       generate.py    — additive: post-synthesis backing-map
#                        population call; state.yaml gets
#                        state.artefacts["backing_map"].
#       verify.py      — additive: render "Backing-implementation map"
#                        section + orphan section in contract-draft.md
#                        per AC.BACKMAP.4.
#       observability.py — additive event_kinds (backing_map_populated
#                          + 12 ratification_<altitude>_<action> kinds);
#                          existing schema_version=1 preserved.
#       cli.py         — additive: `loam odd-extract ratify` routes to
#                        objective/constraint/capability altitude when
#                        contract has typed lists (Cycle 1 shape); v1
#                        BandedAC path remains for legacy contracts.
#       __init__.py    — additive exports of new module entry-points
#                        + new Pydantic/dataclass models.
#   - NEW tests under plugins/dev-sdlc/odd-extractor/tests/:
#       test_AC_BACKMAP_1_models.py
#       test_AC_BACKMAP_2_population.py
#       test_AC_BACKMAP_3_persistence.py
#       test_AC_BACKMAP_4_coverage_report.py
#       test_AC_BACKMAP_5_forward_compat.py
#       test_AC_BACKMAP_6_synthetic_shapes.py
#       test_AC_BACKMAP_7_audit_log.py
#       test_AC_OBJRAT_1_factories_and_apply.py
#       test_AC_OBJRAT_2_promotion_gate.py
#       test_AC_OBJRAT_3_demotion.py
#       test_AC_OBJRAT_4_audit_log.py
#       test_AC_OBJRAT_5_constraint_capability.py
#       test_AC_OBJRAT_6_state_migration.py
#       test_AC_OBJRAT_7_pm_provenance.py
#       test_AC_OBJRAT_8_synthetic_batches.py
#       test_objective_ratification_integration.py    (full pipeline e2e)
#   - NEW fixtures under plugins/dev-sdlc/odd-extractor/tests/fixtures/:
#       backing-map/tight-1-to-1/         — 3 objectives + 5 evidence
#                                            rows (clean per-objective
#                                            mapping).
#       backing-map/loose-multi-row/      — 2 objectives + 12 evidence
#                                            rows (multi-row backing +
#                                            orphans).
#       backing-map/no-evidence-hypothesised/ — 4 mixed-band objectives
#                                                + 6 mostly-orphan rows.
#       objective-ratification/all-plausible/ — 5 P-band objectives +
#                                                populated backing-map.
#       objective-ratification/mixed-bands/   — V/P/H-band objectives
#                                                + mixed backing-map.
#       objective-ratification/edge-cases/    — P→V-without-backing-
#                                                blocked + capability-
#                                                with-H-served-objective.
#
# AC families locked per plan-doc §3:
#   AC.BACKMAP.1 — BackingMap + BackingMapEntry + EvidenceRowRef +
#                   OrphanRow Pydantic models (id regexes + role
#                   validators).
#   AC.BACKMAP.2 — populate_backing_map heuristic pre-filter + LLM-pass
#                   classifier (top-K=8; STRONG/WEAK/NONE per
#                   EVAL_DIMENSIONS; cost-band $0.05–$2.00; halt at >200
#                   pre-filter pairs).
#   AC.BACKMAP.3 — Persistence at backing-map.yaml (atomic write;
#                   schema_version=1; round-trip; generate-stage
#                   state.yaml artefact).
#   AC.BACKMAP.4 — Coverage report in contract-draft.md (per-objective
#                   backing-row counts + first-3 path:line previews +
#                   orphan section).
#   AC.BACKMAP.5 — Forward-compat for v0.2.4/v0.2.5 (OrphanRow.reason
#                   enum extensible; unmatched_objective_ids gap-signal).
#   AC.BACKMAP.6 — Component tests against 3 backing-map fixtures
#                   (tight-1-to-1; loose-multi-row;
#                   no-evidence-hypothesised).
#   AC.BACKMAP.7 — Audit-log per population (backing_map_populated
#                   event_kind; SOC-2 floor).
#   AC.OBJRAT.1  — Objective-altitude factory functions + apply path
#                   (promote/demote/edit/reject_objective +
#                   apply_objective_ratification_action; v1 BandedAC
#                   path preserved).
#   AC.OBJRAT.2  — PLAUSIBLE→VERIFIED on objective requires
#                   explicit_yes + backing_evidence_cited resolving to
#                   STRONG row OR test-asserting-outcome row pinned to
#                   repo_sha; apply-path defense-in-depth.
#   AC.OBJRAT.3  — V→P demotion: no explicit_yes, no backing required;
#                   prior backing carried in audit-log.
#   AC.OBJRAT.4  — Audit-log per ratification action (12 new
#                   ratification_<altitude>_<action> event_kinds);
#                   pm_audit_path cross-reference.
#   AC.OBJRAT.5  — Constraint + capability ratification (parallel
#                   altitudes; constraints no backing required;
#                   capability serves-validator at apply blocks
#                   dangling/H-band).
#   AC.OBJRAT.6  — Substrate preservation: RatificationStateV2 extends
#                   v1; schema_version=2; auto-migrate on read with
#                   atomic backup; v1 callers transparent.
#   AC.OBJRAT.7  — PM-side decision-queue.yaml altitude-tagged
#                   provenance (additive provenance string;
#                   PM-side schema unchanged; persona-side response
#                   router parses altitude).
#   AC.OBJRAT.8  — Component tests against 3 ratification fixtures
#                   (all-plausible; mixed-bands; edge-cases).
#
# Smoke (REALISTIC CONDITION — applicable dimensions per
# smoke-test-discipline §6 + plan-doc §4):
#   D1 cold-state — synthetic banded contract + backing-map →
#     populate_backing_map produces backing-map.yaml + audit-log;
#     ratification batch enqueues objective-altitude prompts; promote
#     with explicit_yes + STRONG backing accepted; promote without
#     backing refused. Verified by test_objective_ratification_integration.py
#     + per-AC tests.
#   D2 steady-state — re-run populate on unchanged objectives +
#     evidence-rows is idempotent (skip when same objective_count +
#     total_evidence_rows); re-run ratify on already-fully-ratified
#     workspace is idempotent. Verified by integration test
#     re-invocation variant.
#   D3 restart — mid-ratification kill -TERM; resume from PM-side
#     record_response audit + ratification-state pending_targets.
#     Inherited from v0.1.8 substrate; no new D3-specific code.
#   D5 cross-session — backing-map.yaml + ratification-state.yaml v2 +
#     audit-log files survive /clear; v1→v2 migration survives session
#     boundary. Verified by manifest-roundtrip + state-migration tests.
#   D6 telemetry-floor — backing_map_populated +
#     ratification_<altitude>_<action> (12 kinds) all present per run;
#     SOC-2 audit-trail floor (Decision P). Verified by
#     test_AC_BACKMAP_7 + test_AC_OBJRAT_4.
#   D4 reboot — n/a structurally (odd-extract is invoked-on-demand,
#     not a daemon; filesystem state survives reboot trivially per
#     v0.1.8 + Cycle 1 precedent).
#   PLUS: full-suite green sweep — pre-Cycle-2 plugins/dev-sdlc/
#     odd-extractor/ + framework/cost-governance/ + framework/
#     per-project-pm/ tests at HEAD (914809d or successor) all pass
#     post-Cycle-2; halt + surface on any regression.
#
# Bookkeeping note: this is the v0.2.3 second cycle (immediate
# successor to v0.2.3 Cycle 1 via apply 1e20037 / seal 9b9f87c /
# §14 backfill 66de327 + master plan §9 backfill 914809d). v3 fields
# exercised: plan_doc_ref + ac_count + smoke_outcome at the manifest
# level; single-component sweep at seal time. Universal-paths admit
# docs/rebuild/plans/ for plan-doc paper trail only — Cycle 2 ships
# zero docs/ edits beyond the plan-doc itself (banding-rule extension
# was Cycle 1's deliverable; Cycle 2's deliverables are all under
# plugins/dev-sdlc/odd-extractor/).

# BASELINE pinned to the source-edit feat commit. Updated post-
# source-commit by the build agent.
baseline: "27111ed"

plan: docs/rebuild/plans/v0-2-3-cycle-2-backing-map-and-ratification-reframe.md

# Top-level plan_doc_ref — schema v3 collapsed-narrative pointer.
# `loam amend seal` synthesizes the narrative body from this ref
# plus ac_count + smoke_outcome below.
plan_doc_ref: docs/rebuild/plans/v0-2-3-cycle-2-backing-map-and-ratification-reframe.md

ac_count: 15
smoke_outcome: "D1+D2+D5+D6 exercised; D3 inherited (PM batch API + state.yaml resume); D4 n/a; 3 backing-map + 3 ratification fixtures; full-suite green sweep"

components:
  - name: dev-sdlc
    seal_test: plugins/dev-sdlc/tests/test_no_sealed_amendments.py
    sidecar: plugins/dev-sdlc/tests/SEAL_COMMIT
    frozen_baseline: false
    extra_allowed_prefixes: []

# Universal admissions — per amendment #22 ruling #3 + Cycle 1
# precedent.
# `docs/rebuild/plans/` admits the plan-before-code paper trail.
# Cycle 2 ships zero non-plan docs/ edits (banding-rule extension was
# Cycle 1's deliverable; methodology doc unchanged at Cycle 2).
universal_paths:
  prefixes:
    - docs/rebuild/plans/
  files:
    - CLAUDE.md
    - docs/odd-in-loam.md
    - docs/odd-methodology.md
    - docs/rebuild/STATE.md
    - plugins/dev-sdlc/docs/odd-methodology.md

narrative:
  target: plugins/dev-sdlc/seals/SEAL_COMMIT.v0-2-3-cycle-2-backing-map-and-ratification-reframe
  # body omitted — schema v3 with plan_doc_ref synthesizes a 5-15
  # line summary at seal time per AC.DPS2.{1,4}. The summary
  # surfaces title + slug + components + baseline + amendment SHA +
  # plan_doc_ref + ac_count + smoke_outcome.
