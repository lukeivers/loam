"""Operational scripts for the orchestrator.

Not imported by runtime code. Run as modules:

    python -m pos_orchestrator_scripts.install_launchd
    python -m pos_orchestrator_scripts.measure_launchd

(These live outside the installed package; treat them as standalone
tools invoked from the repo root or via direct path.)
"""
