# FUTURE_IDEAS_DRAFT.md — no-overhead idea capture

This is a draft surface for *every* improvement idea about pos-v2 — Luke's or the assistant's — captured at point-of-occurrence with no overhead. Sibling to `FUTURE_IDEAS.md` but distinct in lifecycle:

- `FUTURE_IDEAS.md` is **curated**: each idea has a designated number, a rationale section, and a relationship to other ideas.
- `FUTURE_IDEAS_DRAFT.md` is **no-overhead capture**: append a brief bullet with rationale (~3–5 lines) the moment an idea surfaces. No numbering, no curation, no ordering.

**Lifecycle.** During the initial phase of pos-v2 development, entries accumulate. After the initial phase concludes, a daily rigor reviews the draft:

- Incidentally-actioned items get removed (the work happened, the idea served its purpose).
- No-longer-relevant items get pruned.
- Graduate-worthy items get promoted to `FUTURE_IDEAS.md` with full curation.
- Others get dropped or restated.

**Convention.** Agents (background or main-session) **surface to chat** — they do not write to this file directly. The parent (or owner) appends. This avoids file-write races when multiple agents run in parallel, and keeps the parent in the loop on every captured idea.

**Why this exists.** Asymmetric findings, deferred decisions, anti-pattern observations, and feature ideas are valuable signals. Without a destination, they drift away in chat or get lost in memory churn. `FUTURE_IDEAS.md` is too weighty for impromptu capture; this file is the lower-friction destination.

---

## Entries

- **Commit-message templates per category.** Patterns like `feat(<comp>): ... — amendment #N`, `chore(seals): ... — <comp> at <sha>`, `docs(plans): record amendment #N commit SHAs`, etc. are hand-crafted each time. A small template library + a `pos-amend commit-msg <category> <vars>` helper would mechanise the prose and reduce drift.

- **`pos-amend log-decisions <plan> <key>=<value>...`** (stretch). Mechanise §14 method-decision register's mostly-deterministic subsections — test counts, dependents-cleared list, file-touched manifests — while leaving D-build.x prose to the builder. Marginal value vs. cost; flag only.

- **`personas/primary/` at canonical root cleanup.** Apr 18 mtime, untracked, predates the persona-setup amendments. Decide: gitignore `personas/` at repo root, commit a working-state sentinel, or delete the stale directory. Currently noise in `git status`. **Cause identified by #42 build agent (2026-04-25):** some test in workspace-bootstrap's full-suite invokes `run_first_run_scaffold` without `workspace_root` set; the `_resolve_workspace_root` walk-up heuristic falls through to the canonical repo root, materialising `personas/primary/` there. Fix is workspace-bootstrap-test-side: tests should always pass an explicit `workspace_root` (tmp dir).

- **#20 empirical finding: parallel doc-edit + dev-discipline-build is safe.** `feedback_serialize_amendment_builds` may be over-broad — the rule applies specifically to sealed-component-amendment-build pairs racing on `pos-amend`, not generic git activity. Tighten the memory to clarify scope.

- **Stale `error_code` + `remediation` fields in completed `first-run.state`.** Cosmetic from the gen-2 timeout → gen-3 success transition; `_advance_state` doesn't clear them on completion. Flagged in #3 audit. Cosmetic only.

- **Phase-5 confirmation sentence "twelve components".** Actual install is 13 components (telegram-interface auto-discovered). Hardcoded string in `_confirmation_sentence()`. Cosmetic.

- **`/effort` confirmation message bug.** `/effort auto` says "Effort level set to max" but actual state is `xhigh` (which is correct behaviour). UI-only mismatch in the confirmation prose. File via `/feedback`.

- **Dynamic `/effort auto` (feature request).** Luke's intuition that "auto" should scale per task, not statically reset to model default. Not a Claude Code feature today; file as upstream feature request.

- **AC text precision sweep.** Opportunistic batch of post-seal AC tightenings (similar to AC37.5, AC40.1) — when an AC pins specific vocabulary that turns out to be method-not-objective, tighten the AC. Could batch across multiple sealed amendments in one doc-only sweep.

- **Integration-test methodology gap on SQLite file inspection.** Bare `stat()` on the main `.sqlite` file mid-WAL can mislead — the size doesn't reflect committed data until WAL checkpoints. Future integration-test fixtures should sample sibling `-wal`/`-shm` files AND open-then-close a `sqlite3` connection before size-checking. The 0-byte Finding 2 was caused by this methodology gap, not a real bug.

- **Pos-amend `--bare` mode use case.** `claude --bare --settings .claude/settings.json` could be useful for clean experiments / scripted scenarios where the full pos-v2 harness shouldn't load. Document the recipe somewhere if it becomes a recurring need.

- **Template engine: one-pass substitution authoring discipline.** The dispatch-template engine's `{{var}}` substitution doesn't recursively expand defaults — a default containing `{{OTHER_VAR}}` renders the literal placeholder. Caught early in #25 build (the dispatch template's `PRIME_OBJECTIVE_FRAMING` default contained `{{AC_PREFIX}}`). Worth documenting in the dispatch-template authoring guide alongside the future skills wrapping.

- **Template engine: package-data declaration for wheel form.** Templates root resolves via `__file__` parents — works for editable installs (the only mode pos-amend ships in today). If pos-amend ever lands in PyPI / wheel form, templates need `package-data` declaration in `pyproject.toml`. Not blocking; noted for the eventual packaging story.

- **`pos-amend new-plan <slug>` orchestration.** Plan-doc skeleton has 13 required vars — that's a lot to author by hand for every plan. A `pos-amend new-plan <slug>` orchestration that scaffolds the vars-file with empty defaults (or pre-fills `TITLE` / `AC_PREFIX` from CLI args) would be a high-leverage follow-up — bigger leverage than the D-3c skills wrapping. Surfaced by #25 build.

- **Template `description` frontmatter doubles as `list` one-liner.** The introspection-surface frontmatter the engine requires gives `pos-amend template list` its descriptions for free; useful pattern when memory-doc / commit-message families land.

- **G-activation-first dissolves D.** If sub-plan G (shared host-level memory-graphiti instance + workspace-keying via group_id) activates first at multi-workspace reactivation time, sub-plan D (per-workspace memory-graphiti port auto-allocation) becomes moot — one shared instance means no port-collision problem. Worth flagging at reactivation-time triage so we don't redundantly build D before G. Surfaced by doc-update agent 2026-04-25 (captured as G's D-G.4).

- **Lazy-projection trigger ↔ amendment #32 session-start gate composition.** When sub-plan #17 (heavy-b-phase-α/β/γ-migration) reactivates as the lazy-projection job triggered by dev_intent=yes, the cheapest available attach point is amendment #32's session-start gate — same lifecycle event, contract is already loaded at that point, one read + one dispatch decision. Method-level note for #17's future builder. Surfaced by doc-update agent 2026-04-25 (captured as #17 D-build.6).

- **C may activate as "audit + cleanup" rather than "migrate" when multi-workspace lands.** D-MASTER.2's owner-revised mirror of `~/.claude/` (global + workspace-override) collapsed C's migration burden; if any of the resolver pattern is partially in place by reactivation time, C's scope shifts from "migrate state files" to "audit existing layout for compliance + close gaps." Worth the triage at reactivation. Surfaced by doc-update agent 2026-04-25.

- **Cross-mode reference debt: `memory-system/launchd/README.md` references dev-only path.** F's AC.F3 reference scanner found `memory-system/launchd/README.md` (an always-loaded artefact) references `docs/rebuild/components/true-first-run/research.md` (a dev-only artefact). Editing the README would breach F's sealed-component fence (memory-system is sealed; F is dev-discipline). F captured as `KNOWN_CROSS_MODE_DEBT` allowlist so AC.F3 passes; allowlist must shrink to empty when fixed. **Two resolution paths:** (a) future memory-system amendment scrubs the cross-ref (preferred — minimal partition surface); (b) partition relaxes locked ruling 4 to carve out `memory-system/launchd/` from sealed-component coverage (worse — undermines partition's structural simplicity). Surfaced by #44 (sub-plan F build).

- **AC text borderline cases not tightened in #17's sweep.** The post-#45 AC sweep (commit `177a4a9`) tightened 4 ACs (AC.A2, AC.A3, AC.E4, AC.45.4) but flagged three borderline cases left as-is: (1) AC.A1 + AC.A6 + AC.A7 + AC.B5 carry borderline method vocabulary (specific OTel event names, specific `Literal` types) — debatable whether to tighten; the names ARE contracts THIS amendment authors. (2) F's AC.F1 names the `always_loaded`/`dev_only` partition keys — borderline because the names are now load-bearing across F + B integration. Worth re-evaluating in a future precision sweep; cosmetic only for now.

- **`source_commit` backfill pass for Phase γ records.** The #17 Phase γ projection populated 99 amendment-AC records but left `source_commit` field None — reading per-amendment seal SHAs reliably across 25 plans is brittle (every plan's §14 has a different layout). A follow-on backfill pass that walks `git log --grep` for each amendment's seal commit could populate these post-hoc. Going forward, the live `pos-amend seal` cycle populates `source_commit` automatically (AC.D-mig.4 verified); only the historical Phase γ records need backfill. Surfaced by #17 build.

- **AC anchor regex is conservative — tightening could yield 100-200 more records.** The Phase β/γ extractor's regex catches H2/H3 + bullet-form ACs but misses prose-style criteria. ~17 placeholders out of 25 plans + 21 components — not bad, but trims the headcount substantially. A regex-tuning pass would surface materially more ACs (estimate 100-200 additional records). Worth doing once the projection is in active use and the missing records become visibly missed. Surfaced by #17 build.

- **Test `conftest` collision pattern across `tools/<x>/tests/` dirs.** Adding tests in two `tools/<x>/tests/` directories with the same module names triggers a pytest cross-tree collision when run together (resolved in #17 by dropping `__init__.py` + using fixtures instead of cross-test imports). Worth capturing as a tooling pattern — either in the dispatch template's "Cycle mechanics" section or a separate pytest-conventions doc. Surfaced by #17 build.

---

*New entries appended to bottom; review-and-graduate happens post-initial-phase.*

- **`pos-amend apply` regex misses in-function literals.** Captured 2026-04-26 by amendment #50 build agent. The apply-regex doesn't reach `allowed_prefixes` literals declared inside function bodies (vs module-level), causing misses on test_AC_M_S surface coverage. Worked around in #50 via a corrective commit (`6c90b9c`) that hand-widened the literal. Future improvement: extend the regex (or switch to AST parse) to reach in-function bindings, OR document the limitation so dispatchers know to flag it. Adjacent to other pos-amend automation gaps.

- **loam-mode F-register references pre-D.1 path `tools/loam-mode/`** (should be `framework/tools/loam-mode/`). Captured 2026-04-28 by amendment #67 build agent. `framework/tools/loam-mode/tests/test_F_S_seal_diff.py::test_AC_F_S_allowed_surfaces_register_is_complete` — pre-existing on canonical HEAD `39cfbb1`. loam-mode is not sealed (touching it falls under tools/ universal admission); fix is a one-line F-register update + maintainer hygiene. Small.

- **`pos-amend` plan-§14 backfill regex too narrow — rejects `## §14.` form.** Captured 2026-04-28 by amendment #67 build agent. Plan-doc convention permits `## §14.` (section-sign prefix); the backfill regex `^## 14[.\s]` only matches the no-prefix form. Required normalising commit `59c9dfd` mid-build. Suggestion: relax regex to `^## §?\s*14[.\s]` so plan authors can use either form without breaking automation. Small.

- **D.1 byte-content-match sample is brittle to legitimate post-D.1 edits.** Captured 2026-04-28 by amendment #67 build agent. The sample list (in hands-off-lifecycle's H/L test that asserts D.1's git-mv discipline produced byte-identical content under the rename) relies on hand-curated SHAs that go stale when ANY future amendment edits a file in the sample. D.4 (#65) legitimately edited `framework/workspace-bootstrap/pyproject.toml` to add the `pos-new-workspace` entry-point AFTER D.1 captured its byte-match SHA; the test was silently failing but uncaught because no amendment between D.4 and #67 ran H/L's full pytest. Resolution in #67: replace pyproject.toml with `errors.py` (a leaf untouched since D.1) in the sample. Better long-term: rebuild the sample as "files in `framework/<comp>/` whose `git log --since=<D.1-seal>` is empty" (computed at test time, not authored). Methodological refinement; worth research+plan when next H/L amendment crosses the test.

- **D5 launchd plist test is timing-flaky under load.** Captured 2026-04-28 by amendment #67 build agent. `test_D5_1_memory_graphiti_scaffold_plist_reaches_health_200` failed with connection-refused during the seal-sweep first pass; passed on standalone re-run. Likely launchd race under load. Suggestion: longer deadline + retry-loop tolerance, OR separate launchd-bound tests into a slow lane that doesn't share load with the seal sweep. Cosmetic / dev-ergonomics.

- **`pos-publish-framework-only` pre-push hook not yet installed on canonical.** Captured 2026-04-28 by amendment #67 build agent. The synthesis tool exists (`framework/tools/pos-publish-framework-only/`); the integration into git's pre-push event is left for a follow-on. D4 admitted both pre-push hook AND `make publish-framework-only` target as the binding mechanism; the tool itself is operator-invokable already, so the `framework-only` branch is currently advanced manually (or via Makefile target). Pre-push hook installation is a one-line `.git/hooks/pre-push` edit on canonical's local clone — not portable across maintainers without a setup script. Sequenced: install on Luke's clone whenever convenient.

- **GitHub remote default branch is `main`, not `pos-v2`.** Captured 2026-04-28 by amendment #67 build agent. `git remote -v` shows `origin → https://github.com/lukeivers/ivers-corp.git` with default `main`. Strangers cloning default-target `main`, not `pos-v2`. AC.SFR.5 tests against the local `pos-v2` branch; on GitHub the assumption needs revisiting. Composes with the fresh-checkout-of-canonical-from-GitHub follow-on (#3 in the D-cutover queue): when canonical publishes `framework-only` to GitHub, the bootstrap should also handle the case where the user expects `pos-v2` rather than the GitHub default. Not blocking #67 but a config-time concern.

- **`pos-amend seal --plan-doc` SHA-backfill heuristic captures the wrong amendment commit when a corrective fix lands between feat and seal.** Captured 2026-04-28 by amendment #68 build agent. Auto-§14 backfill recorded the corrective-fix SHA (`45e8fbc`) as α's "amendment commit" rather than the actual feature commit (`d1eca61`); required manual refinement in a follow-on doc commit (`139eab3`). Suggestion: walk back to the most recent commit whose subject contains `(amendment #N` matching the manifest's amendment number, OR have the manifest declare `feat_commit:` explicitly. Composes with the prior capture about plan-§14 regex narrowness — both are pos-amend automation gaps surfaceable by a single research-and-fix sweep.

- **L's AC.O.1 doc-vs-impl mismatch — "fourteen" sections vs eleven actually shipped.** Captured 2026-04-28 by amendment #68 build agent. L's plan §4 prose claims fourteen named sections (six traits + eight rules); the actual sealed implementation + L's `test_AC_O_1_named_section_count_is_eleven` carry eleven (five traits + six rules). α composed against the eleven that exist (correct). Tiny doc-prose-vs-implementation mismatch in L's plan §4. Per the loose-AC tightening rule (memory feedback), tighten the plan §4 prose to match the eleven that actually shipped — small doc-only edit, opportunistic batch with the next L-touching amendment.

- **Fresh-checkout-of-canonical-pos-v2 from GitHub must work standalone (D-migration follow-on).** Captured 2026-04-28 by pos3-sync recovery agent (relocated from pos3/framework's dirty FIDRAFT). From Luke's Telegram concern post-pos3-cutover: when canonical pos-v2 is published to GitHub, a `git clone` of canonical alone (NOT cloning into a pos3-shaped workspace) must produce a usable repo. **Concern:** the symlinks created at pos3 root during cutover (CLAUDE.md → framework/CLAUDE.md, docs/odd-* → ../framework/docs/odd-*, docs/rebuild/* → ../../framework/docs/rebuild/*, .scratch/.mcp.json/.pos → workspace/...) are pos3-workspace-shape — they don't apply to canonical itself, since canonical's repo IS the framework (it doesn't have its own `framework/` subdir wrapping it). Canonical's clone is fine in the dev sense (CLAUDE.md is at root, docs/ is at root). But a fresh-clone validation is owed: clone canonical to a tmp path, run pos-new-workspace (or whatever the documented onboarding flow is for first-time users) against it, verify everything works without manual symlink adjustments. Composes with the GitHub-default-branch entry above and amendment #67's reader fall-through. Either way, the test is: a stranger pulling pos-v2 from GitHub succeeds at first-run without primary-persona handholding. Not blocking; pos3 is already cut over and functional. Capture as a build-time validation amendment after single-framework + symlink-discovery follow-ons land.

- **`pos-sync` misleading error message on dirty working tree.** Captured 2026-04-28 by pos3-sync recovery agent during the post-#68 sync attempt. When pos-sync runs against a dirty workspace where the dirty path overlaps with the would-be ff target, the error message reads `workspace has commits ahead of canonical; falling back to merge + LLM resolver` then `merge failed but no conflicted paths` — misleading because the real cause is uncommitted local changes blocking the ff. Suggestion: pre-flight a `git diff --name-only HEAD` check; if non-empty, report `dirty working tree blocks ff-only` with the offending paths before attempting merge. Composes with the next entry (main-session-write-prevention) — both are about the same wrong-tree-write failure class; one prevents at edit time, the other improves diagnostics when prevention fails.

- **Research-doc factual verification CDC.** Captured 2026-04-28 by amendment #69 build agent. Research §6 of the AC.M.S redesign claimed `odd-methodology.md` §10 carries the per-invariant convention; empirical inspection at build time showed `odd-methodology.md` has only §10 ("Where this fits") — the per-invariant convention lives in `odd-in-pos.md` §10.3 alone. The research-author agent likely conflated "ODD docs name the convention" with "both ODD docs name it." Worth tightening research-author dispatches with an explicit "verify section anchors exist before naming them" or grep-the-doc CDC. Composes with the existing dispatch-template authoring-discipline FUTURE_IDEAS entries.

- **Direction-B (test-without-implementation) refusal — distinct surface from A3.** Captured 2026-04-28 by amendment #71 (A3) build agent. ODD §3.3 (one criterion per behaviour) implies a symmetric direction to A3's impl-without-test refusal. A3 ships only direction A per programme D2 lock; direction B is named placeholder. If tests-authored-without-backing-implementation becomes a real failure mode (tests passing because they happen to pass without exercising any real code), the right surface is a `Stop` hook or `pos-amend apply --dry-run` extension — NOT another PreToolUse refusal (that would refuse the very "test before source" sequence ODD §4 admits as the re-extension pattern). Composes with: pos-amend automation gaps already captured.

- **Audit-log rotation for gate hooks (A2 + A3 + future A4).** Captured 2026-04-28 by amendment #71 (A3) build agent. A2's `objective-binding-gate.log` and A3's `tdd-guard.log` ship append-only NDJSON with no rotation. A2 inherited the deferral; A3 inherits A2. When logs grow past ~10 MB the cost of replaying through downstream consumers starts to hurt. A rotation amendment (size-based or per-amendment-window-based) becomes worthwhile when first downstream consumer (e.g. dispatch-staleness verification gate from FIDRAFT) goes live.

- **Test-deletion gate.** Captured 2026-04-28 by amendment #71 (A3) build agent. A3 fires on Edit/Write/MultiEdit so it doesn't see test deletion via Bash `rm`. A test-deletion-for-existing-AC gate is out of A3's surface but a real failure class — if a test for AC X gets deleted, the gate's invariant (test-pinned-to-AC) silently breaks. A4-adjacent territory: the Bash gate could check `git status --porcelain` on PostToolUse for deleted test files corresponding to ACs the manifest still binds, OR a separate amendment authors a delete-specific guard.

- **`framework/orchestrator/tests/__init__.py` collection naming collision.** Captured 2026-04-28 by amendment #71 (A3) build agent. Running `pytest framework/hands-off-lifecycle/tests/ framework/objective-tracker/tests/` together fails with `ModuleNotFoundError: No module named 'tests.test_AC29_health_workspace_probe'` because two unrelated `tests/` packages share the path-import-mode default. Workaround: run pytest per-component-root individually. Fix: `conftest.py` / `pytest.ini` rootdir config sweep, or rename test packages to be component-namespaced. Composes with the existing pos-amend automation FIDRAFT entries.

- **`framework/<comp>/src/__init__.py` byte-content invariant blocks legitimate public-API additions.** Captured 2026-04-28 by amendment #74 build agent. The d1 byte-content test (`framework/hands-off-lifecycle/tests/test_d1_byte_content_match.py`) pins `framework/primary-persona/src/__init__.py`'s SHA to its D-migration baseline. Any amendment that adds a new public API to primary-persona faces a forced choice: (a) edit `__init__.py` to add the export (which breaks the byte-content test, and updating the SHA in hands-off-lifecycle/tests/ requires editing OUTSIDE the amendment's seal-diff fence), or (b) hide the new API as a submodule import (callers do `from primary_persona.dispatch_wrapper import NewACSpec`). Amendment #74 chose (b). Long-term, (a) is the right shape — public APIs should be top-level — but the byte-content test's pin pattern needs to be ODD-§4 retired or selectively-amended for legitimate post-D-migration public-API additions. Composes with the existing AC.M.S structural-brittleness FIDRAFT entry — both are the same class of "frozen-window invariant doesn't anticipate normal forward evolution."

- **Stale editable installs in component venvs survive D-shape cutover.** Captured 2026-04-28 by amendment #74 build agent. Multiple component venvs (objective-tracker, primary-persona) carried `__editable__.<pkg>-<version>.pth` files pointing at `/Users/lukeivers/ivers-corp-pos-v2/<comp>/src` — the legacy pre-D-cutover paths (no `framework/` prefix). The pyproject.toml + import path resolution still happen to work because the new layout matches what `package-dir = {"<pkg>" = "src"}` expects against the CWD-of-install, but the .pth-recorded MAPPING dict carries a stale absolute path. Workaround during amendment #74's build: re-run `pip install -e ../<dep>` for each dep. **Fix candidates:** (i) workspace-bootstrap automates `pip install -e .` across every component venv on D-shape transitions; (ii) a `pos-doctor` command checks for stale editable mappings; (iii) document the manual remediation in CLAUDE.dev.md so the next post-cutover build agent doesn't re-discover it. Low-risk; small shape; high frequency-of-pain-when-it-bites.

- **Cross-component substrate helpers extraction.** Captured 2026-04-28 by amendment #75 build agent. AC.TFN.3 ("single shared helper") would cleanly land via `from _gate_helpers import now_iso_microsecond_z` in `objective_tracker/src/store.py`, but the objective-tracker test suite doesn't have hands-off-lifecycle's hooks dir on sys.path. Worked around with one-line mirror + cross-component invariant test. Future amendment: (a) extract substrate helpers into `framework/_substrate-helpers/` shared by both packages, OR (b) add sys.path bootstrap in objective-tracker's conftest. Asymmetric leverage — eliminates a recurring "duplicate helpers across components" pattern.

- **`time` import audit across framework — dead-import detection.** Captured 2026-04-28 by amendment #75 build agent. Removing #75's `_wait_until_next_iso_second` mitigation exposed two `import time` statements that became dead. Future amendment auditing for unused imports across framework (or adding a ruff rule) catches this class structurally. Composes with the structural-enforcement programme as a Lens-1 leveraged dev-discipline gate.

- **Format-string drift static check between `_gate_helpers.now_iso_microsecond_z` and `objective_tracker.store._now_iso_microsecond_z`.** Captured 2026-04-28 by amendment #75 build agent. AC.TFN.3 + AC.TFN.6 catch the byte-equality empirically, but it's implicit. A static-analysis test grep'ing both source files for the literal `"%Y-%m-%dT%H:%M:%S.%fZ"` and asserting both occurrences exist would catch drift if either side updates without the other. Composes with the cross-component substrate helpers extraction entry above (extraction would eliminate the duplicate entirely).

- **`disable_iso_second_wait` shim cleanup follow-on.** Captured 2026-04-28 by amendment #75 build agent. 5 test files still call this through `_helpers_dsa.py` after #75 turned the wait into a no-op. Future cleanup amendment removes calls + shim entirely per `feedback_loose_AC_text_fix_AC_not_implementation` (substrate change makes the wait unnecessary; loose-AC tightening removes residual scaffolding).

- **Telegram-only channel + pause-on-outage as structural-enforcement amendment.** Captured 2026-04-29 by main session. Owner locked two operating rules: (1) Telegram is the only user-visible communication channel when configured — replies route through `mcp__plugin_telegram_telegram__reply` whenever a chat_id is known for the session, terminal output is for diagnostics only; (2) if a Telegram reply call fails (network, MCP down, auth expired) the persona must NOT kill in-flight background work but must NOT launch any new agent / bash / dispatch — surface the outage in-terminal once, idle, resume on next successful reply. Interim shape: rule lives in `~/.claude/CLAUDE.md` "Channel rules" + persona prompts that observe it. **Structural-enforcement candidate (Lens 1 leverage):** (a) PreToolUse hook on Agent + Bash (+ all dispatch-shaped tools) that probes Telegram health (lightweight call — `mcp__plugin_telegram_telegram__token_usage` or a no-op react with retry+timeout); refuses if unreachable. (b) Stop hook contributor that re-checks at end-of-turn so a recovered Telegram resumes work without persona awareness needed. (c) Workspace contract field `primary_channel` (already a FUTURE_IDEAS_DRAFT entry — graduates as Idea 25 in the cleanup proposal) provides the structured surface this amendment hangs off. Composes with: A4 structural-enforcement programme; the workspace-default-channel slot; the SessionStart corpus-inlining gate (#73). Sequenced after the Idea-25 graduation amendment lands the contract field, since the hook reads `primary_channel` from it.
