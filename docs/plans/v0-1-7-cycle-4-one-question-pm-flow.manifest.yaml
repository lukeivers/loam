schema_version: 1
amendment:
  number: 131
  slug: v0-1-7-cycle4-one-question-pm-flow
  title: "v0.1.7 Cycle 4 — one-question-at-a-time PM-enforced surfacing flow + v0.1.7 release close"

# BASELINE: bcef0fb — the source-edit commit (pre-apply tip).
#
# Commit ladder pre-apply:
#   bcf699a — v0.1.7 Cycle 3 seal (layered-skill discovery).
#   660f05a — Cycle 4 sub-plan-doc (NEW commit; THIS plan).
#   bcef0fb — feat(v0.1.7): Cycle 4 source-edit BASELINE.
#   <this manifest commit lands next>
#   <loam amend apply lands; auto-commit per v0.1.2 item 6>
#   <loam amend seal lands as deterministic seal commit>
#
# Single-component fence — v0.1.7 Cycle 4:
#   - per-project-pm — extends Cycle 2's queue + API with the
#     deferred user-facing surfacing flow:
#       errors.py (PendingResponseError class added),
#       state.py (RecordedResponse dataclass + is_audit_block_trigger
#         property on both SurfacedQuestion + RecordedResponse;
#         StateOfWorld extended with pending_response_for field),
#       loader.py (state.yaml schema extended w/ pending_response_for,
#         backward-compat with Cycle 2 state files),
#       runtime.py (record_response, surface_next_questions_batch,
#         blocking enforcement on batch API only; surface_next_question
#         Cycle 2 contract preserved verbatim per plan §5 Surface #8),
#       __init__.py (re-exports),
#       docs/design.md §11 + §12 (one-question flow + audit-block
#         composition cross-reference),
#       README.md (Cycle 4 surfaces section replaces deferred section),
#       tests/ — 8 NEW AC test files for AC.QSURF.{1..8}.
#
# AC families locked per docs/plans/v0-1-7-cycle-4-one-question-pm-flow.md §4:
#   AC.QSURF.1 — onboarding_mode forces 1 per batch call regardless of n
#                / max_questions_per_turn (8 tests).
#   AC.QSURF.2 — non-onboarding permits batched surfacing per
#                max_questions_per_turn; 5 tests.
#   AC.QSURF.3 — onboarding hard test (5 turns × 1 per turn × 5 enqueued);
#                4 tests including structural-determinism repeated runs.
#   AC.QSURF.4 — record_response audit-log entry shape + linkage;
#                4 tests.
#   AC.QSURF.5 — require_owner_response blocks subsequent surfacings;
#                PendingResponseError carries pending_question +
#                surfaced_audit_path; 6 tests.
#   AC.QSURF.6 — record_response API; idempotency on duplicate;
#                rejects empty / missing path / wrong event_kind;
#                accepts relative path; 9 tests.
#   AC.QSURF.7 — every PM-mediated surfacing AND response writes an
#                audit-log entry; SOC-2 audit-trail floor; 5 tests.
#   AC.QSURF.8 — composition with audit-block-on-telegram SKILL via
#                is_audit_block_trigger property + design.md §12
#                cross-reference; 9 tests.
#
# Smoke (REALISTIC CONDITION — all 6 dimensions per plan §6):
#   D1 cold-state, D2 steady-state, D3 restart, D4 reboot,
#   D5 cross-session, D6 telemetry-floor — all exercised; HARD gate
#   for v0.1.7 release close per Decision R.
#
# Release-level row backfill (STATE.md / roadmap §8 / eric-final §2)
# lands as a follow-on doc-only commit per plan §3 — they are universal-
# admitted prefixes but kept off the seal-fence diff for clean
# component-boundary signal.
baseline: "bcef0fb"

plan: docs/plans/v0-1-7-cycle-4-one-question-pm-flow.md

components:
  - name: per-project-pm
    seal_test: framework/per-project-pm/tests/test_no_sealed_amendments.py
    sidecar: framework/per-project-pm/tests/SEAL_COMMIT
    frozen_baseline: false
    extra_allowed_prefixes: []

# Universal admissions — per amendment #22 ruling #3.
universal_paths:
  prefixes:
    - docs/plans/
  files:
    - CLAUDE.md
    - docs/odd-in-loam.md
    - docs/odd-methodology.md
    - docs/FUTURE_IDEAS.md
    - docs/FUTURE_IDEAS_DRAFT.md
    - docs/STATE.md

narrative:
  target: framework/per-project-pm/seals/SEAL_COMMIT.v0-1-7-cycle4-one-question-pm-flow
  body: |
    # v0.1.7 Cycle 4 — one-question-at-a-time PM-enforced surfacing
    flow — apply ladder

    2026-05-04. v0.1.7 Cycle 4 (final cycle of v0.1.7) per
    `docs/plans/v0-1-7-cycle-4-one-question-pm-flow.md` and
    parent `docs/plans/v0-1-7-personas-pm-layered-skills.md`
    §5 AC.QSURF.* + §6 Cycle 4. Closes v0.1.7 (Cycles 1+2+3+4 sealed).

    Sub-plan: `docs/plans/v0-1-7-cycle-4-one-question-pm-flow.md`.

    Scope (per sub-plan §3 + §4 + §7): single-component fence on
    `framework/per-project-pm/` — extends Cycle 2's component with
    the deferred user-facing surfacing flow. Cycle 2 (sealed at
    `73505f0`) shipped the queue + API primitive; Cycle 4 wires the
    one-question-at-a-time discipline + response tracking + blocking
    enforcement + audit-block-on-telegram SKILL composition.

    AC families:

      - AC.QSURF.1 — onboarding_mode flag toggles enforcement.
        With onboarding_mode=True, surface_next_questions_batch()
        returns exactly 1 question per call regardless of n or
        max_questions_per_turn. With onboarding_mode=False, returns
        up to min(n, max_questions_per_turn, len(queue)).
      - AC.QSURF.2 — non-onboarding-mode permits batched surfacing.
        With max_questions_per_turn=3 + n=3, batch returns 3; queue
        advances by 3; audit-log records 3 surface_question entries
        with FIFO order preserved; per-batch queue_position fields.
      - AC.QSURF.3 — onboarding hard test. 5 simulated turns × 1
        question per turn × 5 enqueued questions, structurally
        deterministic (assertion `len(returned) == 1` per turn — never
        randomly passes). Repeated-run determinism verified.
      - AC.QSURF.4 — audit-log records each surfacing AND each
        response with full provenance. record_response audit entry
        carries: schema_version, event_kind=record_response,
        timestamp, pm_handle, response_text, surfaced_audit_path
        (relative to PM dir), surfaced_question_text, responded_at.
        NNNN counter shared across event_kinds within (pm-name, UTC
        date). Linkage round-trippable.
      - AC.QSURF.5 — require_owner_response=True blocks subsequent
        surfacings via surface_next_questions_batch (NOT via
        surface_next_question — Cycle 2 contract preserved per plan
        §5 Surface #8 fallback). PendingResponseError carries
        pending_question + surfaced_audit_path. With
        require_owner_response=False, no blocking. Blocking does NOT
        partial-surface.
      - AC.QSURF.6 — record_response API. Returns RecordedResponse;
        writes audit entry; clears pending_response_for; idempotent
        on duplicate (returns prior recorded response without dup
        write); rejects empty text; rejects missing path; rejects
        wrong event_kind on link; accepts relative path.
      - AC.QSURF.7 — PM-mediated dispatches log per audit-trail floor
        (SOC-2 — Eric synthesis Decision P alignment). Every
        surfacing + every response writes one audit entry. Every
        entry carries pm_handle + ISO 8601 timezone-aware timestamp
        + schema_version=1.
      - AC.QSURF.8 — composition with audit-block-on-telegram SKILL.
        SurfacedQuestion + RecordedResponse expose
        `is_audit_block_trigger` property (returns True at Cycle 4
        unconditionally; forward-compat extension point). design.md
        §12 cross-references the SKILL by exact path
        (`plugins/loam-skills/skills/audit-block-on-telegram/`) and
        names the "decision was made" trigger condition. Test
        verifies (a) property exposes True on both classes,
        (b) design-note carries path + trigger string, (c) SKILL.md
        actually exists at named path AND contains trigger string,
        (d) property docstrings reference both SKILL name +
        trigger string.

    Method-level choices (builder's call per ODD §1.1):

      - Blocking enforcement on `surface_next_questions_batch` only;
        `surface_next_question` (Cycle 2 API) preserved verbatim per
        plan §5 Surface #8 fallback. The Cycle 2 contract is
        load-bearing — 64 Cycle 2 tests pass unchanged after Cycle 4
        runtime extension. The structural one-question-at-a-time
        discipline lives on the batch API; the single-question API
        remains the "low-discipline" surface.
      - `pending_response_for` field added to state.yaml schema
        (vs separate file). Backward-compat: Cycle 2 state.yaml
        loads as `pending_response_for=None`.
      - record_response audit entry stores `surfaced_audit_path` as
        a path RELATIVE to PM dir (portable if workspace is moved).
      - Idempotency on duplicate record_response: scan audit-log/ for
        existing record_response entry whose surfaced_audit_path
        matches; on hit, return prior RecordedResponse without
        writing a duplicate entry (audit-log is the source of truth).
      - `is_audit_block_trigger` is a `@property` (not always-True
        field) on both event classes. Forward-compat: future cycles
        may gate on event metadata; the property mechanism is the
        extension point.
      - Batch with require_owner_response=True surfaces ONE question
        then stops (subsequent surfacings would be blocked anyway);
        next call raises PendingResponseError. Predictable shape;
        no partial state.
      - PendingResponseError carries pending_question +
        surfaced_audit_path on the exception body so the caller can
        re-prompt the user without a second loader call.
      - Tests = 8 NEW files (one per AC.QSURF.{1..8}); 50 new tests;
        all pass; cumulative per-project-pm count 114 tests
        (64 Cycle 2 + 50 Cycle 4) all green.
      - Public API surface re-exported via `__init__.py` for
        `from loam.per_project_pm import RecordedResponse,
        PendingResponseError`.

    Out of scope (deferred to v0.2.0+):

      - Persona-side flow integration in primary-persona — Cycle 4
        ships the PM-side surfacing API; the primary-persona's
        call-site that invokes
        `host.per_project_pm.runtime_for(...).surface_next_questions_batch(...)`
        before relaying to the user is a primary-persona-component
        change outside Cycle 4 fence. Lands at v0.2.0+ alongside
        auto-creation per parent plan §7.
      - OTEL emission for production-stake mode — Cycle 4 ships the
        audit primitive; OTEL transport is v0.2.0+.
      - Auto-creation of skills by PM (Eric synthesis Decision H,
        v0.2.0).
      - PM ratification queue mechanics + domain-batched AC
        surfacing (v0.2.0).

    Predecessor commits:
      - 3aa20dd — v0.1.7 Cycle 1 seal (5 subagent personas + symlink
        registration).
      - 73505f0 — v0.1.7 Cycle 2 seal (per-project PM NEW component;
        queue + API).
      - bcf699a — v0.1.7 Cycle 3 seal (layered-skill discovery +
        collision rules).
      - 660f05a — Cycle 4 sub-plan-doc.
      - bcef0fb — feat(v0.1.7): Cycle 4 source-edit BASELINE.

    BASELINE bcef0fb — the source-edit commit. Single-component fence
    on `framework/per-project-pm/`. Sidecar advances to the v0.1.7
    Cycle 4 seal SHA at apply time.

    With Cycle 4 sealed, v0.1.7 ships in full: 5 subagent personas
    discoverable in `<workspace>/.claude/agents/` (Cycle 1) +
    per-project PM with queue + state-of-world + decision-surfacing
    primitive (Cycle 2) + plugin skills auto-symlinked into
    `<workspace>/.claude/skills/` with workspace-local override
    semantics (Cycle 3) + one-question-at-a-time PM-enforced
    surfacing flow with audit-block-on-telegram SKILL composition
    (Cycle 4). Release-level row backfill (STATE.md / roadmap §8 /
    eric-final §2) lands as a follow-on doc-only commit.

    DO NOT push tags — v0.1.7 sits as a local release until Luke
    gates and the migration question resolves (per parent dispatch
    §Bookkeeping).
