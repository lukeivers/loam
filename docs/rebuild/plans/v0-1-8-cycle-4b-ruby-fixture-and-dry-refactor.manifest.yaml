---
schema_version: 3
amendment:
  # number omitted — schema v3 deprecates amendment.number per
  # AC.DPS1.10 + AC.DPS1.11. Identification by slug only.
  slug: v0-1-8-cycle-4b-ruby-fixture-and-dry-refactor
  title: "v0.1.8 Cycle 4b — canonical ruby-rails-payment fixture + Ruby e2e ratification + DRY refactor of repo_sha/slugify/heuristic-helpers into lang/_common/ (single-component fence: dev-sdlc/odd-extractor)"

# BASELINE: <pending> — source-edit feat commit covering the
# canonical ruby-rails-payment fixture (5–10 routes + 3 ActiveRecord
# models with callbacks/concerns/polymorphic + 2 Sidekiq jobs +
# ≥10 RSpec specs + Apache-2.0 LICENSE + README), the JsTs fixture
# Apache-2.0 LICENSE addition, the DRY refactor (move
# resolve_repo_sha + slugify + file_slug + make_inferred_banded_ac
# into lang/_common/ + migrate recognizer import sites + retain
# compat shim at _ast_utils), and ~25 new tests across
# AC.FIXTURES.{2, 3-ruby, 4-ruby, 5-ruby} + AC.DRY.{1..4}.
#
# Cycle 4b is the residue cycle from Cycle 4a's plan-author-time
# 4a/4b split (master plan §7.9 + Cycle 4 dispatch's halt-trigger
# explicit authorization). Cycle 4a shipped the JS/TS adapter +
# JsTs fixture + JsTs e2e smoke. Cycle 4b ships:
#   - canonical ruby-rails-payment fixture (AC.FIXTURES.2).
#   - Ruby e2e ratification end-to-end pin (AC.FIXTURES.4 ruby).
#   - LICENSE for both fixtures (AC.FIXTURES.5 ruby + jsts).
#   - DRY refactor of repo_sha + slugify + heuristic-helper
#     boilerplate into lang/_common/ (RF surfaced in Cycle 4a §10
#     #6).
#
# Behaviour preservation: 304 odd-extractor tests + 71 dev-sdlc
# parent tests verified passing at HEAD 67dd302 (Cycle 4a seal)
# at plan-author 2026-05-04 — required green sweep post-refactor.
# Halt trigger: any prior-cycle test regression post-refactor.
#
# Commit ladder pre-apply:
#   1c2c478 — v0.1.8 master plan (Cycle 0 predecessor).
#   c1abda1 — v0.1.8 Cycle 1 seal.
#   774d465 — Cycle 1 §9 backfill.
#   4865028 — v0.1.8 Cycle 2 seal.
#   cbde592 — Cycle 2 §9 backfill.
#   236fdcd — Cycle 2 §9 backfill (continued).
#   3b4fe9b — Cycle 3 plan-doc.
#   f1c762c — Cycle 3 source-edit BASELINE.
#   2a64b23 — Cycle 3 manifest+apply.
#   6711dd7 — Cycle 3 seal.
#   cfee099 — Cycle 3 §9 backfill.
#   17f32a9 — v0.1.8 master plan reroute (Cycle 4 Python → JS/TS).
#   5ed881d — Cycle 4a plan-doc.
#   c2356a6 — Cycle 4a source-edit BASELINE.
#   07033d3 — Cycle 4a manifest+apply.
#   67dd302 — Cycle 4a seal.
#   79f6960 — Cycle 4a §9 backfill.
#   180e2b1 — master plan §9 + 4b residue row backfill.
#   <plan-doc commit lands NEXT, then source-edit feat commit, then
#    `loam amend apply` lands as single merged manifest+apply commit
#    per AC.DPS1.6 (v3 schema)>
#   <`loam amend seal --plan-doc <abs path>` lands as deterministic
#    seal commit per AC.DPS2.{1,4,6}>
#
# Single-component fence — dev-sdlc/odd-extractor:
#   - canonical ruby-rails-payment fixture at
#     plugins/dev-sdlc/odd-extractor/tests/fixtures/ruby-rails-payment/
#     (NEW; co-exists with Cycle 3's synthetic-rails fixture).
#     ≥18 files: 3 controllers, 3 models with callbacks/concerns/
#     polymorphic, 2+ concerns under app/models/concerns/, 2 Sidekiq
#     jobs, 3 migrations, ≥10 RSpec specs, Gemfile, README (SYNTHETIC
#     banner), config/routes.rb (5–10 routes), Apache-2.0 LICENSE.
#   - jsts-playwright-app LICENSE addition (Apache-2.0; closes
#     AC.FIXTURES.5 jsts portion).
#   - DRY refactor: lang/_common/ NEW subpackage exposes
#       _common/repo_sha.py (resolve_repo_sha)
#       _common/slugs.py (slugify, file_slug)
#       _common/heuristic_helpers.py (make_inferred_banded_ac
#         constructor for HYPOTHESISED-band ACs)
#     lang/{ruby,jsts}/repo_sha.py DELETED (single source via
#     _common/).
#     lang/{ruby,jsts}/_ast_utils.py keep compat shim re-export
#     for slugify + file_slug (zero-cost backcompat).
#     All recognizer modules under lang/{ruby,jsts}/recognizers/
#     migrate to import from .._common.slugs directly (canonical
#     path; not via the shim).
#     lang/{ruby,jsts}/heuristic_inferences.py refactored to
#     call make_inferred_banded_ac() instead of hand-rolling
#     Evidence(kind="inference", ...) — per-language regex tables
#     and orchestration unchanged.
#   - SliceDriftError + aggregate_slice_results stay where they
#     are (lang/ruby/slicer.py + lang/jsts/slicer.py re-export);
#     deferred to Cycle 5+ per Surface #5.
#   - odd-methodology.md §12 small note: lang/_common/ is canonical
#     shared-symbol home for future adapters (universal admission).
#   - tests under tests/lang/ruby/ for fixture/e2e (~14 new) +
#     tests/lang/_common/ NEW subdir for DRY (~10 new) + 1
#     top-level test for AC.FIXTURES.5 (~25 total new).
#
# AC families locked per plan-doc §4:
#   AC.FIXTURES.2 — canonical ruby-rails-payment realistic fixture
#                   (5–10 routes + 3 ActiveRecord models w/
#                   callbacks/concerns/polymorphic + 2 Sidekiq
#                   jobs + ≥10 RSpec specs + Apache-2.0 LICENSE +
#                   README/Gemfile/migrations/routes.rb).
#   AC.FIXTURES.3 (ruby-only portion) — End-to-end smoke band
#                  distribution against canonical fixture (≥3
#                  VERIFIED + ≥5 PLAUSIBLE + ≥2 HYPOTHESISED).
#   AC.FIXTURES.4 (ruby-only portion) — Eric-ratification e2e
#                  pin against canonical fixture (mirror Cycle 3's
#                  test_AC_RAILS_5_eric_ratification_pin.py shape).
#   AC.FIXTURES.5 (ruby + jsts portions) — both fixtures committed
#                  real repos with permissive LICENSE (Apache-2.0).
#   AC.DRY.1 — _common/repo_sha.py exposes resolve_repo_sha;
#               lang/{ruby,jsts}/repo_sha.py DELETED;
#               adapter.py imports from .._common.repo_sha.
#   AC.DRY.2 — _common/slugs.py exposes slugify + file_slug;
#               re-export at _ast_utils.py (compat shim).
#   AC.DRY.3 — _common/heuristic_helpers.py exposes
#               make_inferred_banded_ac; both adapters'
#               heuristic_inferences.py refactored to call it.
#   AC.DRY.4 — recognizer import sites migrated to .._common.slugs
#               (not via shim).
#
# Smoke (REALISTIC CONDITION — applicable dimensions per
# smoke-test-discipline §6 + plan-doc §6):
#   D1 cold-state — fresh tmp workspace + ruby-rails-payment fixture
#     → loam odd-extract (live, --budget-cents 5000) → banded
#     contract draft with band-tagged AC table at <!-- ACS_TABLE_HERE -->;
#     ≥3 VERIFIED + ≥5 PLAUSIBLE + ≥2 HYPOTHESISED. Verified by
#     test_smoke_d1_cold_state_canonical.py.
#   D2 steady-state — n/a structurally (one-shot CLI). Idempotency
#     variant: 5 extractions byte-identical (modulo timestamps).
#     Verified by test_smoke_d2_idempotency_canonical.py.
#   D3 restart — n/a structurally (no long-running process).
#   D4 reboot — n/a structurally (same as D3).
#   D5 cross-session — partial extraction state survives subprocess
#     boundary; resume completes remaining stages. Verified by
#     test_smoke_d5_cross_session_canonical.py.
#   D6 telemetry-floor — full extraction writes extraction_start +
#     4× stage_complete + ≥1× slice_complete + ≥6× recognizer_finding
#     + extraction_end audit-log entries; schema v1 preserved;
#     filenames monotonic NNNN.yaml. Verified by
#     test_smoke_d6_telemetry_floor_canonical.py.
#   PLUS: full-suite green sweep — 304 odd-extractor + 71 dev-sdlc
#     parent tests (375 baseline at HEAD 67dd302) all pass
#     post-refactor; halt + surface on any regression. The DRY
#     refactor is supposed to be behaviour-preserving.
#
# Bookkeeping note: this is the SEVENTH amendment to use schema v3
# (DPS1 / DPS2 / Cycle 1 / Cycle 2 / Cycle 3 / Cycle 4a / THIS =
# Cycle 4b). v3 fields exercised: plan_doc_ref + ac_count +
# smoke_outcome at the manifest level; single-component sweep at
# seal time.

baseline: "<pending — source-edit feat commit SHA fills here>"

plan: docs/rebuild/plans/v0-1-8-cycle-4b-ruby-fixture-and-dry-refactor.md

# Top-level plan_doc_ref — schema v3 collapsed-narrative pointer.
# `loam amend seal` synthesizes the narrative body from this ref
# plus ac_count + smoke_outcome below.
plan_doc_ref: docs/rebuild/plans/v0-1-8-cycle-4b-ruby-fixture-and-dry-refactor.md

ac_count: 8
smoke_outcome: "D1+D2-idempotency+D5+D6 exercised against canonical ruby-rails-payment fixture; D3/D4 n/a per smoke-test-discipline §6 (one-shot CLI); full-suite green sweep — 375 prior-cycle tests pass post-refactor (verifying behaviour preservation)"

components:
  - name: dev-sdlc
    seal_test: plugins/dev-sdlc/tests/test_no_sealed_amendments.py
    sidecar: plugins/dev-sdlc/tests/SEAL_COMMIT
    frozen_baseline: false
    extra_allowed_prefixes: []

# Universal admissions — per amendment #22 ruling #3 + Cycle 3 +
# Cycle 4a precedent. `docs/rebuild/plans/` admits the
# plan-before-code paper trail. `plugins/dev-sdlc/docs/odd-
# methodology.md` is a small Cycle 4b extension target (§12 note
# referencing lang/_common/ as the canonical shared-symbol home;
# mirrors Cycle 3+4a precedent).
universal_paths:
  prefixes:
    - docs/rebuild/plans/
  files:
    - CLAUDE.md
    - docs/odd-in-loam.md
    - docs/odd-methodology.md
    - docs/rebuild/STATE.md

narrative:
  target: plugins/dev-sdlc/seals/SEAL_COMMIT.v0-1-8-cycle-4b-ruby-fixture-and-dry-refactor
  # body omitted — schema v3 with plan_doc_ref synthesizes a 5-15
  # line summary at seal time per AC.DPS2.{1,4}. The summary
  # surfaces title + slug + components + baseline + amendment SHA +
  # plan_doc_ref + ac_count + smoke_outcome.
