schema_version: 3
amendment:
  slug: session-clear-safety-r2-owner-pending-lifecycle
  title: "session-`/clear` safety R2 — objective-tracker owner-pending lifecycle (sub-amendment 1 of 3). Additive `ObjectiveStatus.owner_pending` widening (amendment-38 additive precedent, default-preserving, D8 round-trip): represents 'work shipped, owner decision pending' — distinct from `active` (in progress) and from the terminal set `{achieved, abandoned}` (closed). `active→owner_pending` + `owner_pending→{active,achieved,abandoned}` legal transitions added; no pre-R2 transition removed/altered. Production runtime API `ObjectiveTracker.mark_owner_pending`. NOT terminal, NOT default-in-flight — its own category so the session-start digest never collapses it into done. Build-cycle AC scope AC.SCS-R2.{1,3,S} (AC.SCS-R2.2 STAGED to sub-amendment 2/R1 per D-SCS.4 — its digest-surface outcome is Tier-0-verified structurally unsatisfiable in R2's objective-tracker-only fence; digest lives in primary-persona/tracker_context.py). R2 is the hard input to R1: R1's priority-ordered digest cannot rank-by-owner-pending until owner-pending is representable. Parent plan: session-clear-safety-tracker-register-and-first-run-update-parity.md §7 sub-amendment 1."

# BASELINE captured at the source-edit commit (per established precedent;
# the seal-diff window is BASELINE..SEAL_COMMIT). Backfilled below to the
# feat(...) source-edit commit SHA after it lands.
baseline: "a96f698"

plan: docs/plans/session-clear-safety-tracker-register-and-first-run-update-parity.md
plan_doc_ref: docs/plans/session-clear-safety-tracker-register-and-first-run-update-parity.md

ac_count: 3
smoke_outcome: "AC.SCS-R2.{1,3,S} GREEN: owner_pending representable via mark_owner_pending, distinct from active+terminal, queryable; pre-R2 records/transitions/D8 unchanged; seal-diff in objective-tracker fence."

# Single-component fence — sub-amendment 1 (R2) seal anchor is
# framework/objective-tracker/ (the additive owner-pending lifecycle
# state lands here). extra_allowed_prefixes empty — R2 touches no other
# component. AC.SCS-R2.2 is NOT in this fence (D-SCS.4 staged it to R1).
components:
  - name: objective-tracker
    seal_test: framework/objective-tracker/tests/test_no_sealed_amendments.py
    sidecar: framework/objective-tracker/tests/SEAL_COMMIT
    frozen_baseline: false
    extra_allowed_prefixes: []

# Universal admissions — per amendment #22 ruling #3.
universal_paths:
  prefixes:
    - docs/plans/
    - docs/experiments/
    - docs/design/
    - docs/examples/
  files:
    - CLAUDE.md
    - install-from-source.txt
    - docs/odd-in-pos.md
    - docs/odd-methodology.md
    - docs/FUTURE_IDEAS.md
    - docs/FUTURE_IDEAS_DRAFT.md
    - docs/release-roadmap.md
    - docs/release-roadmap-dependency-map.md
    - docs/STATE.md
    - docs/release-process.md
    - docs/release-versioning-policy.md
    - docs/architecture.md
    - docs/components/index.md
    - docs/public-surface-manifest.md
    - README.md

narrative:
  target: framework/objective-tracker/seals/SEAL_COMMIT.session-clear-safety-r2-owner-pending-lifecycle
  body: |
    # session-`/clear` safety R2 — objective-tracker owner-pending lifecycle

    Sub-amendment 1 of 3. Sealed local; awaiting dispatcher dogfood
    publish per ASK-FIRST.

    The objective-tracker gained an additive `ObjectiveStatus`
    lifecycle value `owner_pending` — "work shipped, owner decision
    pending" — distinct from `active` (in progress) and from the
    terminal set `{achieved, abandoned}` (closed). This is the single
    distinction a hand-maintained RESUME-STATE file existed to carry:
    that a shipped item is awaiting the owner's call, not done. Added
    `active→owner_pending` and `owner_pending→{active,achieved,
    abandoned}` legal transitions plus the production runtime API
    `ObjectiveTracker.mark_owner_pending`; no pre-R2 transition was
    removed or altered (additive — amendment-38 widening precedent,
    default-preserving, D8 round-trip). owner_pending is NOT terminal
    and NOT default-in-flight: its own category so the session-start
    digest never collapses it into "done" (R2.2 surfaces it in R1's
    primary-persona digest fence per D-SCS.4). R2 is the hard input to
    R1 — the priority-ordered digest cannot rank-by-owner-pending until
    owner-pending is representable. AC.SCS-R2.{1,3,S} green against real
    test output; full objective-tracker suite (incl. amendment-38
    backward-compat + D8 round-trip) unchanged. SHA register +
    method-decision record in the parent plan-doc §14.
