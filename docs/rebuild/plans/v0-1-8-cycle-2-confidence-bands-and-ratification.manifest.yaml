schema_version: 3
amendment:
  # number omitted — schema v3 deprecates amendment.number per
  # AC.DPS1.10 + AC.DPS1.11. Identification by slug only.
  slug: v0-1-8-cycle-2-confidence-bands-and-ratification
  title: "v0.1.8 Cycle 2 — confidence bands + ratification workflow (two-component fence: dev-sdlc/odd-extractor + framework/per-project-pm)"

# BASELINE: <to be set to the source-edit commit SHA when build lands>
#
# Commit ladder pre-apply:
#   1c2c478 — v0.1.8 master plan + cycle decomposition (predecessor).
#   c1abda1 — v0.1.8 Cycle 1 seal (odd-extractor scaffolding).
#   774d465 — master plan §9 backfill for Cycle 1 SHAs.
#   <sub-plan-doc commit lands first; THIS plan>
#   <feat(...): Cycle 2 source-edit BASELINE — two-component edit
#     covering plugins/dev-sdlc/odd-extractor/ + framework/per-project-pm/>
#   <`loam amend apply` lands NEXT — single merged manifest+apply
#    commit per AC.DPS1.6 (v3 schema)>
#   <`loam amend seal --plan-doc <abs path>` lands as deterministic
#    seal commit, with synthesized 5-15 line narrative body per
#    AC.DPS2.{1,4} + `Plan doc:` line per AC.DPS2.6>
#
# Two-component fence — serialized per feedback_serialize_amendment_builds:
#   - dev-sdlc — primary fence; plugins/dev-sdlc/odd-extractor/ adds
#     bands.py (Pydantic ConfidenceBand + Evidence + BandedAC),
#     ratify.py (RatificationAction dataclass + factory funcs +
#     apply_ratification_action), ratification_state.py
#     (ratification-state.yaml schema + loader); extends spec.py
#     re-exports, verify.py band-tagged markdown injection at
#     <!-- ACS_TABLE_HERE --> anchor, cli.py with `ratify` sub-verb,
#     __init__.py re-exports, README.md ratification-workflow section.
#     Tests: 7 per-AC test files (AC.BANDS.{1..7}) + 2 smoke
#     (cross-session resume + steady-state idempotency) + 1 fixture
#     (synthetic-banded-contract.{md,yaml}). Plus odd-methodology.md
#     §11 extension (universal-admitted top-level doc).
#   - per-project-pm — secondary fence; thin helper module
#     ratification.py (RatificationBatch.from_contract_draft +
#     enqueue) re-exports. NO changes to existing PMRuntime / state /
#     loader / contract / contribution. README.md adds Cycle-2-of-
#     v0.1.8 ratification-batches subsection. Tests: 1 PM-side
#     integration test (test_AC_BANDS_PM_integration.py).
#
# AC families locked per docs/rebuild/plans/v0-1-8-cycle-2-confidence-bands-and-ratification.md §4:
#   AC.BANDS.1 — Banded AC schema (ConfidenceBand enum + Evidence
#                + BandedAC Pydantic; round-trip via model_dump()
#                through dict shape).
#   AC.BANDS.2 — Per-band evidence requirements via model_validator
#                (VERIFIED→test+repo_sha; PLAUSIBLE→source+citations;
#                HYPOTHESISED→inference+rationale).
#   AC.BANDS.3 — odd-methodology.md §11 extension carries band
#                semantics + evidence-field names + ratification
#                CLI verb (content-grep test).
#   AC.BANDS.4 — `loam odd-extract ratify <draft>` CLI sub-verb +
#                PM-mediated batch (--pm-name, --workspace-root,
#                --json, --resume flags).
#   AC.BANDS.5 — PLAUSIBLE→VERIFIED requires explicit_yes=True per
#                Decision I; silent promotion refused; demotion
#                ungated (asymmetric).
#   AC.BANDS.6 — Audit log per ratification action via existing
#                write_audit_entry primitive; new event_kind values
#                ratification_promote/demote/edit/reject/resume.
#                pm_audit_path cross-references PM-side record_response.
#   AC.BANDS.7 — RatificationBatch.from_contract_draft + enqueue
#                via PMRuntime.enqueue_decision; provenance format
#                odd-extract:{extraction_id}:{ac_id}; one-question-
#                at-a-time per Decision Q.
#
# Smoke (REALISTIC CONDITION — applicable dimensions per
# smoke-test-discipline §6 + plan-doc §6):
#   D1 cold-state — fresh tmp workspace + synthetic banded fixture →
#     ratify → audit log entries observable. Verified by
#     test_AC_BANDS_4 + test_AC_BANDS_7.
#   D2 steady-state — n/a structurally (one-shot CLI). Idempotency
#     variant: 5 promotions in sequence are idempotent; queue
#     bounded; completed_actions monotonic. Verified by
#     test_ratification_steady_state.py.
#   D3 restart — n/a structurally (no long-running process).
#   D4 reboot — n/a structurally (same as D3).
#   D5 cross-session — partial ratification batch resumable across
#     fresh-process boundary via ratification-state.yaml +
#     `--resume`. Verified by test_ratification_resume_cross_session.py.
#   D6 telemetry-floor — every ratification action writes one
#     audit-log entry; pm_audit_path cross-references PM-side
#     record_response entries. SOC-2 audit-trail floor (Decision P).
#     Verified by test_AC_BANDS_6_audit_log_per_action.py.
#
# Bookkeeping note: this is the THIRD amendment to use schema v3
# (DPS1 = first; DPS2 = second; v0.1.8 Cycle 1 = third single-component
# v3 build; THIS = first multi-component v3 build). v3 fields
# exercised: plan_doc_ref + ac_count + smoke_outcome at the manifest
# level; multi-`components:` cross-component sweep at seal time.

baseline: "08256cf"

plan: docs/rebuild/plans/v0-1-8-cycle-2-confidence-bands-and-ratification.md

# Top-level plan_doc_ref — schema v3 collapsed-narrative pointer.
# `loam amend seal` synthesizes the narrative body from this ref
# plus ac_count + smoke_outcome below.
plan_doc_ref: docs/rebuild/plans/v0-1-8-cycle-2-confidence-bands-and-ratification.md

ac_count: 7
smoke_outcome: "D1+D2-idempotency+D5+D6 exercised; D3/D4 n/a per smoke-test-discipline §6 (one-shot CLI)"

components:
  - name: dev-sdlc
    seal_test: plugins/dev-sdlc/tests/test_no_sealed_amendments.py
    sidecar: plugins/dev-sdlc/tests/SEAL_COMMIT
    frozen_baseline: false
    extra_allowed_prefixes: []
  - name: per-project-pm
    seal_test: framework/per-project-pm/tests/test_no_sealed_amendments.py
    sidecar: framework/per-project-pm/tests/SEAL_COMMIT
    frozen_baseline: false
    extra_allowed_prefixes: []

# Universal admissions — per amendment #22 ruling #3.
# `docs/rebuild/plans/` admits the plan-before-code paper trail
# (sub-plan + manifest + master-plan §9 backfill).
# `plugins/dev-sdlc/docs/odd-methodology.md` is the AC.BANDS.3 §11
# extension target — not at the top-level docs/ path; must be admitted
# explicitly because the dev-sdlc seal-test treats plugins/dev-sdlc/
# as the primary fence and the doc edit IS within fence already, but
# explicit admission documents the intent for SOC-2-style audit
# readers.
universal_paths:
  prefixes:
    - docs/rebuild/plans/
  files:
    - CLAUDE.md
    - docs/odd-in-loam.md
    - docs/odd-methodology.md
    - docs/rebuild/STATE.md

narrative:
  target: plugins/dev-sdlc/seals/SEAL_COMMIT.v0-1-8-cycle-2-confidence-bands-and-ratification
  # body omitted — schema v3 with plan_doc_ref synthesizes a 5-15
  # line summary at seal time per AC.DPS2.{1,4}. The summary
  # surfaces title + slug + components + baseline + amendment SHA +
  # plan_doc_ref + ac_count + smoke_outcome.
