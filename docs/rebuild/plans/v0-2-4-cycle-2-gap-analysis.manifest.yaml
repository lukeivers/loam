---
schema_version: 3
amendment:
  # number omitted — schema v3 deprecates amendment.number per
  # AC.DPS1.10 + AC.DPS1.11. Identification by slug only.
  slug: v0-2-4-cycle-2-gap-analysis
  title: "v0.2.4 Cycle 2 — Gap analysis (two-category GapInventory over augmented objectives + backing-map + adapter evidence-rows); Gap + GapInventory Pydantic shapes (two-category Literal, STRONG/WEAK confidence orthogonal to objective banding) + analyze_gaps() pure function over (augmented_objectives, backing_map, evidence_rows) with same-file orphan-cluster collapse + confidence rule (STRONG = V/P + empty-backing OR non-test orphan; WEAK = HYPOTHESISED OR test/config-only orphan) + persistence at <workspace>/.loam/extractions/<repo-id>/gap-inventory.yaml + 3 additive audit-log event_kinds (gap_analysis_start / gap_inventory_persisted / gap_analysis_end) + CLI `loam odd-extract gaps <workspace>` + forward-compat Gap.negative_alignment_evidence field (null at v0.2.4 for v0.2.6+ negative-alignment) + component tests on 4 synthetic fixtures (clean / category-a-only / category-b-only / mixed); single-component fence on plugins/dev-sdlc/odd-extractor/"

# BASELINE: <pending> — source-edit feat commit covering new module at
# plugins/dev-sdlc/odd-extractor/src/loam_odd_extractor/gap_analysis.py
# + extending spec.py with Gap + GapInventory + GapSummary Pydantic
# models + extending observability.py (3 new event_kinds for
# gap-analysis actions) + additive cli.py subcommand `loam odd-extract
# gaps` + per-AC tests + 4 gap-analysis fixtures.
#
# Cycle 2 is the second of three cycles in v0.2.4. Cycle 1 sealed at
# d42ace9 (completeness interview + augmented-objectives.yaml).
# Cycle 3 follows: build-next + persona surface + SOFT smoke
# (release SOFT smoke gate). PRIMARY plugins/dev-sdlc/odd-extractor/.
# New build_next.py + CLI subcommand for persona pull; ranked-
# candidate output with rationale; informative not prescriptive
# (AC.BLDNXT.* + AC.PERSONA-PULL.*).
# After Cycle 3 seals + SOFT smoke green, the v0.2.4 SHIPPED rollup
# commit lands on canonical, backfilling STATE.md + roadmap §8 +
# odd-rebuild master plan §3 v0.2.4 row + master plan §9 per-cycle
# SHA register. Tag deferred until v0.2.5 ship gate per Luke
# 2026-05-05.
#
# Commit ladder pre-apply:
#   50b5385 — v0.2.3 SHIPPED rollup. Cycle 3 seal f78bb36.
#   f230333 — v0.2.4 master plan-doc (trim discipline applied).
#   36ca3e2 — v0.2.4 Cycle 1 sub-plan-doc + manifest.
#   e1a4239 — v0.2.4 Cycle 1 amendment apply (BASELINE 89f3933).
#   d42ace9 — v0.2.4 Cycle 1 seal.
#   <plan-doc commit lands NEXT (THIS plan-doc + manifest), then
#    source-edit feat commit (BASELINE pinned to that SHA), then
#    `loam amend apply <manifest>` lands as single merged
#    manifest+apply commit per AC.DPS1.6 (v3 schema)>
#   <`loam amend seal --plan-doc <abs path> <manifest>` lands as
#    deterministic seal commit per AC.DPS2.{1,4,6}>
#   <post-seal commit backfills this plan-doc's §14 + master plan §9
#    Cycle 2 SHA row per AC.D-sa.7>
#
# Single-component fence — plugins/dev-sdlc/odd-extractor/:
#   - NEW module under plugins/dev-sdlc/odd-extractor/src/
#     loam_odd_extractor/:
#       gap_analysis.py    — pure-function gap analysis;
#                             analyze_gaps(*, augmented_objectives,
#                             backing_map, evidence_rows,
#                             extraction_id) -> GapInventory; helper
#                             _classify_confidence() per AC.GAPAN.4;
#                             same-file orphan-cluster collapse;
#                             deterministic + idempotent.
#   - EDITS to existing modules (additive only):
#       spec.py        — extend with Gap Pydantic model (gap_id +
#                        category Literal + confidence Literal +
#                        objective_id Optional + evidence_rows +
#                        rationale + negative_alignment_evidence
#                        Optional forward-compat field) + GapSummary
#                        nested model + GapInventory container model
#                        (schema_version=1; no-duplicate-gap_id +
#                        summary-aggregation model_validators).
#       observability.py — extend event_kinds (3 new kinds for
#                          gap-analysis actions:
#                          gap_analysis_start /
#                          gap_inventory_persisted /
#                          gap_analysis_end); structured payload uses
#                          existing estimate field (no schema-version
#                          bump).
#       cli.py         — additive subcommand `loam odd-extract gaps
#                        <workspace>` reading augmented-objectives.yaml
#                        + backing-map.yaml + evidence-rows.yaml from
#                        the workspace's extraction-dir; missing-
#                        predecessor halt with exit code 2 +
#                        actionable message; idempotent re-run.
#       __init__.py    — additive exports of new module entry-point +
#                        new Pydantic models.
#   - NEW tests under plugins/dev-sdlc/odd-extractor/tests/:
#       test_AC_GAPAN_1_gap_model.py
#       test_AC_GAPAN_2_inventory_model.py
#       test_AC_GAPAN_3_analyze_gaps.py
#       test_AC_GAPAN_4_confidence_rule.py
#       test_AC_GAPAN_5_persistence.py
#       test_AC_GAPAN_6_audit_log.py
#       test_AC_GAPAN_7_cli.py
#       test_AC_GAPAN_8_forward_compat.py
#       test_AC_GAPAN_9_integration.py
#   - NEW fixtures under plugins/dev-sdlc/odd-extractor/tests/
#     fixtures/gap-analysis/:
#       clean/             — 3 VERIFIED objectives + full STRONG
#                             backing + all rows claimed → empty
#                             GapInventory.
#       category-a-only/   — 1 PLAUSIBLE empty-backing + 1
#                             HYPOTHESISED no-rows → 2 category-a
#                             Gaps (1 STRONG + 1 WEAK).
#       category-b-only/   — 3 VERIFIED full-STRONG-backing + 5
#                             unclaimed rows (3 production same-file
#                             → 1 STRONG orphan; 2 test rows → 1
#                             WEAK orphan) → 2 category-b Gaps.
#       mixed/             — Eric-shape (synthetic-of-shape, NOT real
#                             repo paths): PLAUSIBLE security
#                             objective with WEAK rows only +
#                             production-code orphan + HYPOTHESISED
#                             no-rows + test-only orphan → 4 Gaps
#                             spanning all (category × confidence)
#                             cells; calibration anchor for AC.GAPAN.4
#                             non-degenerate halt.
#
# AC families locked per plan-doc §4:
#   AC.GAPAN.1   — Gap Pydantic shape (gap_id regex + category
#                   Literal + confidence Literal + objective_id
#                   Optional + evidence_rows + rationale +
#                   negative_alignment_evidence forward-compat).
#   AC.GAPAN.2   — GapInventory container Pydantic (schema_version=1
#                   + GapSummary nested + no-duplicate-gap_id +
#                   summary-matches-gaps validators).
#   AC.GAPAN.3   — analyze_gaps pure function over (augmented_
#                   objectives, backing_map, evidence_rows,
#                   extraction_id); same-file orphan-cluster collapse;
#                   no I/O; deterministic; no LLM call.
#   AC.GAPAN.4   — Confidence rule (STRONG/WEAK) deterministic over
#                   typed substrate; halt-on-degenerate (100%-STRONG
#                   or 100%-WEAK on mixed/ fixture).
#   AC.GAPAN.5   — Persistence at <workspace>/.loam/extractions/
#                   <repo-id>/gap-inventory.yaml; atomic tmp+rename;
#                   idempotent on no-change (skip-write semantics
#                   excluding analyzed_at).
#   AC.GAPAN.6   — Audit-log event_kinds (3 new kinds; structured
#                   payload via existing estimate field; no
#                   schema-version bump).
#   AC.GAPAN.7   — CLI subcommand `loam odd-extract gaps <workspace>`;
#                   missing-predecessor halt with exit code 2 +
#                   actionable message; stdout summary (per-category
#                   + per-confidence counts + top-3 gap_ids).
#   AC.GAPAN.8   — Forward-compat Gap.negative_alignment_evidence
#                   field (None at v0.2.4); model_dump(exclude_none=
#                   True) keeps YAML clean; v0.2.6+ migration safe.
#   AC.GAPAN.9   — Component tests against 4 synthetic fixtures
#                   (clean + category-a-only + category-b-only +
#                   mixed); full pipeline e2e per fixture.
#
# Smoke (REALISTIC CONDITION — applicable dimensions per
# smoke-test-discipline §6 + plan-doc §1):
#   D1 cold-state — `loam odd-extract gaps <fresh-workspace>`
#     (predecessors set up via Cycle 1 + v0.2.3 substrate) produces
#     gap-inventory.yaml + audit-log entries + stdout summary.
#     Verified by test_AC_GAPAN_9_integration.py.
#   D5 cross-session — gap-inventory.yaml + audit-log persist at
#     <workspace>/.loam/extractions/<repo-id>/; survive /clear.
#     Idempotent re-run produces byte-identical content (sans
#     analyzed_at). Verified by test_AC_GAPAN_5_persistence.py +
#     test_AC_GAPAN_7_cli.py.
#   D6 telemetry-floor — all 3 new event_kinds present per run; SOC-2
#     audit-trail floor (Decision P). Verified by
#     test_AC_GAPAN_6_audit_log.py.
#   D2 steady-state — re-run gap-analysis on unchanged inputs is
#     idempotent (no-change skip-write per AC.GAPAN.5).
#   D3 restart — inherited; analyze_gaps is a pure function; mid-run
#     kill → re-run from scratch reproduces identical output (no
#     persistent in-flight state to recover).
#   D4 reboot — n/a structurally (odd-extract is invoked-on-demand,
#     not a daemon; filesystem state survives reboot trivially per
#     v0.1.8 / v0.2.3 / Cycle 1 precedent).
#   PLUS: full-suite green sweep — pre-Cycle-2 plugins/dev-sdlc/
#     odd-extractor/ + framework/cost-governance/ +
#     framework/per-project-pm/ tests at HEAD (Cycle 1 seal d42ace9
#     or successor) all pass post-Cycle-2; halt + surface on any
#     regression.
#
# Bookkeeping note: this is the v0.2.4 second cycle (immediate
# successor to Cycle 1 seal d42ace9 + v0.2.4 master plan at f230333
# + Cycle 1 sub-plan-doc at 36ca3e2). v3 fields exercised:
# plan_doc_ref + ac_count + smoke_outcome at the manifest level;
# single-component sweep at seal time. Universal-paths admit
# docs/rebuild/plans/ for plan-doc paper trail.

# BASELINE pinned to the source-edit feat commit. Updated post-
# source-commit by the build agent.
baseline: "df6727684832f624841cc7f017aefccd2de0751f"

plan: docs/rebuild/plans/v0-2-4-cycle-2-gap-analysis.md

# Top-level plan_doc_ref — schema v3 collapsed-narrative pointer.
# `loam amend seal` synthesizes the narrative body from this ref
# plus ac_count + smoke_outcome below.
plan_doc_ref: docs/rebuild/plans/v0-2-4-cycle-2-gap-analysis.md

ac_count: 9
smoke_outcome: "D1+D5+D6 exercised; D2+D3 inherited (pure-function idempotence + no in-flight state); D4 n/a; 4 gap-analysis fixtures (clean + category-a-only + category-b-only + mixed); full-suite green sweep"

components:
  - name: dev-sdlc
    seal_test: plugins/dev-sdlc/tests/test_no_sealed_amendments.py
    sidecar: plugins/dev-sdlc/tests/SEAL_COMMIT
    frozen_baseline: false
    extra_allowed_prefixes: []

# Universal admissions — per amendment #22 ruling #3 + Cycle 3 +
# Cycle 4a/4b precedent + v0.1.9 Cycle 1 precedent + v0.2.0 Cycle 1
# precedent + v0.2.1 Cycle 1 precedent + v0.2.3 Cycle 1 precedent +
# v0.2.4 Cycle 1 precedent. `docs/rebuild/plans/` admits the
# plan-before-code paper trail.
universal_paths:
  prefixes:
    - docs/rebuild/plans/
  files:
    - CLAUDE.md
    - docs/odd-in-loam.md
    - docs/odd-methodology.md
    - docs/rebuild/STATE.md
    - plugins/dev-sdlc/docs/odd-methodology.md

narrative:
  target: plugins/dev-sdlc/seals/SEAL_COMMIT.v0-2-4-cycle-2-gap-analysis
  # body omitted — schema v3 with plan_doc_ref synthesizes a 5-15
  # line summary at seal time per AC.DPS2.{1,4}. The summary
  # surfaces title + slug + components + baseline + amendment SHA +
  # plan_doc_ref + ac_count + smoke_outcome.
