schema_version: 3
amendment:
  # number: assigned at apply-time per feedback_version_numbers_at_release_time.
  number: null
  slug: frame-kernel-real-dispatch-memory-tier
  title: "frame-kernel real-dispatch memory tier — task-text derivation + standing decision floor. Tier-0 probe findings (2026-06-10, two real claude -p runs with a SubagentStart capture hook): real envelopes carry ONLY the documented common six fields (no prompt/task/description), so parse_envelope's task_text was empty by construction and the memory tier degraded to its unavailable-marker on every real dispatch; the envelope's transcript_path points at the PARENT session's transcript which at fire time already contains the dispatching turn's real user message (mechanism (a) timing-feasible), while the in-flight Task tool_use is NOT yet flushed (a tool_use scan would be empty or stale — rejected, D-RDM.1). THE FIX (D-RDM.2, layered): (a) parse_envelope derives task_text from the transcript's last real user message (envelope fields keep first priority; tail-bounded read; reuses frame_judge's record parsing) feeding the EXISTING gated retrieval unchanged; (b) when no task text is derivable the memory tier renders a query-less STANDING FLOOR — open + recent ruled decision records WHOLE via the sealed decision_ledger read surface, within the existing injection budget — so dispatches always carry the load-bearing rulings. AC.RDM.1 (derivation + priority + skip + fail-soft) + AC.RDM.2 (standing floor; byte-identical degraded markers with no ledger) + AC.RDM.S (outcome-altitude: production hook subprocess, real-shape envelope + transcript fixture, planted ruled record reaches the injected memory tier). Existing AC.SACH./AC.DMP./AC.SSFC./AC.EWR. contracts + tests unchanged; fail-soft exit-0 preserved; primary-persona untouched (imported read-only as today)."

# BASELINE: c09328fb — pre-amendment tip (HEAD at dispatch). Surface:
# framework/frame-kernel/src/loam/frame_kernel/bundle.py (task-text
# transcript fallback in parse_envelope + standing-floor branch in the
# memory tier) + three new test files (AC.RDM.1 / AC.RDM.2 / AC.RDM.S)
# + this plan/manifest under docs/plans/.
baseline: "c09328fb6bba333a4904217aca9c5bc5149b158f"

plan: docs/plans/frame-kernel-real-dispatch-memory-tier.md
plan_doc_ref: docs/plans/frame-kernel-real-dispatch-memory-tier.md

ac_count: 3
smoke_outcome: "AC.RDM.1 + AC.RDM.2 + AC.RDM.S GREEN; full frame-kernel suite green under the venv Python. RED-on-revert verified on the new ACs against pre-fix bundle.py."

seal_description: "frame-kernel real-dispatch memory tier — task_text derived from the parent transcript's last user message + query-less standing decision floor; the bundle's memory tier populates on real SubagentStart envelopes"

# EXTEND the sealed `frame-kernel` component (frozen_baseline: false
# advances the seal baseline — the intended fence-widening of an EXTEND).
# Surface inside the fence: bundle.py + new AC.RDM.* tests. No other
# sealed component's fence widens; frame_judge.py / the hook script /
# the persona memory + ledger surfaces are imported, never edited.
components:
  - name: frame-kernel
    seal_test: framework/frame-kernel/tests/test_no_sealed_amendments.py
    sidecar: framework/frame-kernel/tests/SEAL_COMMIT
    frozen_baseline: false
    extra_allowed_prefixes:
      - framework/frame-kernel/
      - kernel/

# Universal admissions — per amendment #22 ruling #3.
universal_paths:
  prefixes:
    - docs/plans/
    - docs/experiments/
    - docs/design/
    - docs/examples/
  files:
    - CLAUDE.md
    - docs/STATE.md
    - docs/release-roadmap.md
    - docs/release-roadmap-dependency-map.md
    - docs/release-process.md
    - docs/release-versioning-policy.md
    - docs/FUTURE_IDEAS.md
    - docs/FUTURE_IDEAS_DRAFT.md
    - docs/architecture.md
    - docs/components/index.md
    - docs/public-surface-manifest.md
    - README.md
    - install-from-source.txt

narrative:
  target: docs/plans/sealed/frame-kernel-real-dispatch-memory-tier.md
  body: |
    # frame-kernel — real-dispatch memory tier

    Follow-on to the envelope cwd-fallback corrective (sealed c39de619):
    that cycle made the microkernel tier populate on real dispatches;
    THIS cycle makes the MEMORY tier populate on them too.

    THE GAP (Tier-0, probe captures 2026-06-10): real SubagentStart
    envelopes carry only the documented common six fields — no
    prompt/task/description — so `parse_envelope`'s task_text was empty
    by construction, no memory query ever ran, and every real dispatch
    saw `[memory unavailable — no live store or query]`.

    THE EVIDENCE (two real `claude -p` runs, isolated via
    loam-spawn-isolation, each dispatching a Task subagent under a
    SubagentStart capture hook snapshotting the transcript at fire
    time): the envelope's transcript_path points at the PARENT
    session's transcript; at fire time it already contains the
    dispatching turn's real user message; the in-flight Task tool_use
    is NOT yet flushed (so a tool_use scan would read nothing — or,
    worse, a PREVIOUS dispatch's prompt once one flushes).

    THE FIX, layered (D-RDM.1 / D-RDM.2):

    (a) task-text derivation — `parse_envelope` falls back from the
    envelope fields (kept as first priority, forward-compat) to the
    transcript's LAST REAL USER MESSAGE: the current ask the dispatch
    serves, the same seed the parent's own per-turn retrieval uses.
    Tail-bounded read; record parsing reused from frame_judge. The
    derived text feeds the EXISTING gated retrieval unchanged.

    (b) standing decision floor — when no task text is derivable at
    all, the memory tier injects the workspace's open + recent ruled
    decision records WHOLE (sealed decision_ledger read surface),
    newest-first, within the existing injection budget — dispatches
    always carry the load-bearing rulings even without relevance
    matching. With no ledger the tier degrades byte-identically to the
    pre-cycle markers.

    AC.RDM.1 — derivation + envelope-field priority + synthetic-record
    skips + fail-soft. AC.RDM.2 — the floor, whole records, budget,
    byte-identical no-ledger degradation. AC.RDM.S (outcome-altitude) —
    the production hook entry-point as a subprocess, real-shape
    envelope + real-shape transcript fixture, a planted RULED record
    relevant to the fixture's user message reaches the injected
    memory tier.

    Unchanged: fail-soft exit-0 (AC.SACH.4); the workstream tier (its
    real-dispatch placeholder is an absent-state-file condition, not an
    envelope bug — triaged in plan §2); frame_judge.py; the hook
    script; primary-persona (imported read-only); all existing
    frame-kernel tests pass untouched.
