---
schema_version: 3
amendment:
  # number omitted — schema v3 deprecates amendment.number per
  # AC.DPS1.10 + AC.DPS1.11. Identification by slug only.
  slug: v0-2-0-cycle-2-auto-skill-creation
  title: "v0.2.0 Cycle 2 — Persona-driven skill capture (auto-creation MVP) + workspace-config flag + design note; two-component fence on plugins/loam-skills/ + framework/workspace-bootstrap/"

# BASELINE: <pending> — source-edit feat commit covering the new
# skill-capture-proposal SKILL package + the manifest-flag extension
# in workspace-bootstrap + the new design note + per-AC tests.
#
# Cycle 2 is the second of two cycles in v0.2.0. Cycle 1 sealed at
# 6fef2f1 (apply faff84e). After Cycle 2 seals, the v0.2.0 release-
# level SOFT smoke gate runs (master plan §5 + Decision R). Tag is
# local-only (NOT pushed) until Luke gates the release.
#
# Commit ladder pre-apply:
#   9022df1 — v0.1.9 SHIPPED rollup (predecessor; v0.1.9 release).
#   7c0f87b — v0.2.0 master plan + cycle decomposition.
#   6fef2f1 — v0.2.0 Cycle 1 seal commit.
#   11ebc76 — v0.2.0 Cycle 1 SHA backfill.
#   <plan-doc commit lands NEXT (THIS plan-doc + manifest), then
#    source-edit feat commit (BASELINE pinned to that SHA), then
#    `loam amend apply <manifest>` lands as single merged
#    manifest+apply commit per AC.DPS1.6 (v3 schema)>
#   <`loam amend seal --plan-doc <abs path> <manifest>` lands as
#    deterministic seal commit per AC.DPS2.{1,4,6}>
#   <post-seal commit backfills this plan-doc's §14 + master plan §9
#    per AC.D-sa.7>
#
# Two-component fence (intentional exception per master plan §3
# Cycle 2 fence locked):
#   PRIMARY — plugins/loam-skills/:
#     - NEW skill at plugins/loam-skills/skills/skill-capture-proposal/
#         SKILL.md  — the SKILL package codifying the persona's auto-
#                     capture workflow.
#     - EDITS to existing test files (additive — extend EXPECTED_SKILLS
#                                     8→9):
#         tests/test_AC_LSK_1_skill_packages_present.py
#         tests/test_AC_LSK_2_frontmatter_well_formed.py
#         tests/test_AC_LSK_3_body_content_shape.py
#     - NEW tests under plugins/loam-skills/tests/:
#         test_AC_SKILLCAP_1_skill_package.py
#         test_AC_SKILLCAP_2_explicit_request_trigger.py
#         test_AC_SKILLCAP_3_repeated_invocation_trigger.py
#         test_AC_SKILLCAP_4_ask_and_answer_trigger.py
#         test_AC_SKILLCAP_5_proposal_draft_workflow.py
#         test_AC_SKILLCAP_6_pm_ratification_gate.py
#         test_AC_SKILLCAP_8_cooldown_semantics.py
#         test_AC_SKILLCAP_9_per_week_budget.py
#         test_AC_SKILLCAP_10_hard_cap.py
#         test_AC_SKILLCAP_11_design_note_present.py
#         test_AC_SKILLCAP_12_audit_log_shape.py
#         test_AC_SKILLCAP_13_self_contained.py
#   SECONDARY — framework/workspace-bootstrap/:
#     - EDIT to src/loam/workspace_bootstrap/manifest.py — add
#       enable_auto_skill_capture: bool = False field + load_manifest
#       validator (additive; mirrors safety_profile shape).
#     - NEW tests under framework/workspace-bootstrap/tests/:
#         test_AC_SKILLCAP_7_enable_flag_default_false.py
#         test_AC_SKILLCAP_7_enable_flag_field_pinned.py
#   TERTIARY (universal admission):
#     - NEW docs/design/auto-skill-capture-shape.md — the design note.
#
# AC families locked per plan-doc §4:
#   AC.SKILLCAP.1  — `skill-capture-proposal` SKILL package present +
#                     well-formed (frontmatter + body 6-section + loam-
#                     pattern reference + graceful-degradation).
#   AC.SKILLCAP.2  — Trigger 1 explicit-request named in body (phrase-
#                     list ≥3 + immediate-draft semantic).
#   AC.SKILLCAP.3  — Trigger 2 repeated-invocation named (≥3 threshold
#                     + structural-overlap heuristic + session-scoped).
#   AC.SKILLCAP.4  — Trigger 3 ask-and-answer named (3+ exchanges +
#                     non-dev emphasis + session-scoped).
#   AC.SKILLCAP.5  — Proposal draft workflow (.scratch/ path + 6-
#                     section template + evidence header).
#   AC.SKILLCAP.6  — User-ratification via PM (Y/N/R + one-question-
#                     at-a-time + Y-write semantics).
#   AC.SKILLCAP.7  — `enable_auto_skill_capture` workspace-config
#                     flag (default false; bool-only; fail-closed).
#   AC.SKILLCAP.8  — Cool-down semantics (14d post-N + cooldowns.yaml
#                     + suppression check).
#   AC.SKILLCAP.9  — Per-week budget (≤3/rolling-7d + budget.yaml +
#                     reset semantic).
#   AC.SKILLCAP.10 — Hard-cap (20 workspace-local SKILLs +
#                     promotion-rubric pointer).
#   AC.SKILLCAP.11 — Design note present at docs/design/auto-skill-
#                     capture-shape.md (7 named sections + universal-
#                     tier + ratification framing + v0.2.x roadmap +
#                     Eric grounding).
#   AC.SKILLCAP.12 — Audit-log shape (.loam/skill-capture/audit-log/
#                     + 6 event-kinds + SOC-2 reference).
#   AC.SKILLCAP.13 — End-to-end smoke / SKILL self-contained at
#                     component-level; release-level at v0.2.0
#                     SOFT gate.
#
# Smoke (REALISTIC CONDITION — applicable dimensions per
# smoke-test-discipline §6 + plan-doc §7):
#   D1 cold-state — fresh checkout; pytest in plugins/loam-skills/
#     tests/ + framework/workspace-bootstrap/tests/ green; SKILL.md
#     present + well-formed; manifest field default false.
#   D2 idempotency — pytest twice produces identical results;
#     load_manifest equal Manifest objects on same file.
#   D3 restart — n/a structurally (static artefacts; no daemon).
#   D4 reboot — n/a structurally (filesystem state survives).
#   D5 cross-session — `/` menu exposes 9 SKILLs after Cycle 2 lands
#     (Anthropic-native discovery for the new base-loam SKILL);
#     workspace-local SKILL discovery side covered at v0.2.0 release-
#     level smoke (master plan §5 D5).
#   D6 telemetry-floor — SKILL body documents 6 event-kinds + audit-
#     log directory; AC.SKILLCAP.12 test asserts presence; release-
#     level fires-end-to-end at master plan §5 D6.
#   PLUS: pre-flight v0.1.7 Cycle 3 layered-skill discovery test
#     passes (test_AC_LAYERED_2_skill_symlink_registration.py); halt
#     + surface if broken.
#   PLUS: full-suite green sweep — pre-Cycle-2 loam-skills + workspace-
#     bootstrap tests at HEAD 6fef2f1 all pass post-Cycle-2; halt +
#     surface on any regression.
#
# Bookkeeping note: this is the post-Cycle-1 v3-schema build (immediate
# successor to v0-2-0-cycle-1-continuous-watch at 6fef2f1). v3 fields
# exercised: plan_doc_ref + ac_count + smoke_outcome at the manifest
# level; cross-component sweep at seal time covers BOTH primary and
# secondary fence components.

# BASELINE pinned to the source-edit feat commit. Updated post-
# source-commit by the build agent.
baseline: "<pending>"

plan: docs/rebuild/plans/v0-2-0-cycle-2-auto-skill-creation.md

# Top-level plan_doc_ref — schema v3 collapsed-narrative pointer.
# `loam amend seal` synthesizes the narrative body from this ref
# plus ac_count + smoke_outcome below.
plan_doc_ref: docs/rebuild/plans/v0-2-0-cycle-2-auto-skill-creation.md

ac_count: 13
smoke_outcome: "D1 + D2-idempotency + D5 + D6 exercised; D3/D4 n/a per smoke-test-discipline §6 (static artefacts); pre-flight v0.1.7 Cycle 3 layered-skill discovery test passes; full-suite green sweep across loam-skills + workspace-bootstrap; release-level smoke at v0.2.0 SOFT gate per master plan §5"

components:
  - name: loam-skills
    seal_test: plugins/loam-skills/tests/test_no_sealed_amendments.py
    sidecar: plugins/loam-skills/tests/SEAL_COMMIT
    frozen_baseline: false
    extra_allowed_prefixes: []
  - name: workspace-bootstrap
    seal_test: framework/workspace-bootstrap/tests/test_no_sealed_amendments.py
    sidecar: framework/workspace-bootstrap/tests/SEAL_COMMIT
    frozen_baseline: false
    extra_allowed_prefixes: []

# Universal admissions — per amendment #22 ruling #3 + Cycle 1
# precedent + Cycle 2 design-note co-shipping.
# `docs/rebuild/plans/` admits the plan-before-code paper trail.
# `docs/design/` admits the new auto-skill-capture-shape.md design note.
universal_paths:
  prefixes:
    - docs/rebuild/plans/
    - docs/design/
  files:
    - CLAUDE.md
    - docs/odd-in-loam.md
    - docs/odd-methodology.md
    - docs/rebuild/STATE.md

narrative:
  target: framework/workspace-bootstrap/seals/SEAL_COMMIT.v0-2-0-cycle-2-auto-skill-creation
  # body omitted — schema v3 with plan_doc_ref synthesizes a 5-15
  # line summary at seal time per AC.DPS2.{1,4}. The summary
  # surfaces title + slug + components + baseline + amendment SHA +
  # plan_doc_ref + ac_count + smoke_outcome.
  # target placement matches v0-1-7-cycle-3 precedent (the prior
  # cross-cutting amendment touching loam-skills via the layered-
  # skill discovery cycle); workspace-bootstrap/seals/ has the
  # established seal-narrative subdirectory while loam-skills uses
  # the SEAL_COMMIT.notes append-only convention for its own seal-
  # cycles.
