# Amendment #68 — Claude-Code-corpus prompt-spine + seed docs (α)

2026-04-28. Amendment α of the four-amendment Claude-Code
knowledge program (locked research:
`docs/rebuild/plans/research/persona-capability-knowledge-grounding-research.md`,
Decision D-2: α prompt-spine + corpus seed → β MCP
knowledge-server → δ Class A currency → γ optional dynamic
contributor).

α ships:

  - **Capability leverage spine** — a new top-level named
    section in the framework template
    `framework/primary-persona/templates/persona-template/prompt.md`.
    The spine carries a leverage rule paragraph (the
    declarative rule the persona runs on every plan that
    takes action — naming both Claude-Code-leverage and
    harness-leverage halves) plus a tight capability index
    (one-line entries pointing at corpus-doc paths under
    `docs/rebuild/capability-corpus/`; ≥ 8 entries; ≤ 1500
    chars total). Slotted between L's identity / archetype
    block and L's traits block, additively to L's eleven
    sections (AC.O.1).

  - **Lean on the corpus** — a new operational-rule entry,
    sibling to L's six existing rules (Lean on the harness,
    Use the right tool, Codify what repeats, Structural
    enforcement default, ODD-shaped internal model,
    Light-touch narration on choices). Names the on-demand
    fetch convention: the persona reads corpus-doc paths
    via the Read tool when the spine names a capability the
    user's prompt invokes. β replaces the Read-by-path
    convention with `mcp__knowledge__resources/read` once
    the server lands.

  - **Two-class corpus authoring guide** at
    `docs/rebuild/capability-corpus/AUTHORING.md`. Defines
    the section schemas for Class A (Anthropic-canonical
    reference, paired with `[user-intent phrasings]`),
    Class A-prime (pos-v2 harness primitives, same shape),
    and Class B (best-practice synthesis, paired with
    `Trust marker` block). Names the No-cross-class-write
    invariant — Class A's deterministic refresh (δ) never
    writes to Class B; Class B's accrual channels never
    write to Class A.

  - **Seed Class A docs** under
    `docs/rebuild/capability-corpus/claude-code/` and
    `docs/rebuild/capability-corpus/harness/`: 5 docs
    covering the highest-leverage primitives:
    `claude-code/schedule.md`, `claude-code/loop.md`,
    `claude-code/background-agents.md`,
    `claude-code/hooks.md`, `harness/scope-of-work.md`.

  - **Seed Class B docs** under
    `docs/rebuild/capability-corpus/best-practice/`: 3 docs
    synthesising owner-articulated patterns from MEMORY.md:
    `background-agents-by-default.md`,
    `scope-only-dispatch.md`,
    `verify-dispatch-before-sending.md`.

α is content + a prompt-section addition. No Python source.
No behavioural change to existing sealed components. The
spine and the operational rule are workspace-template
content (template-tree exception per v1.2 R16); the corpus
tree is framework-level reference (consumed by β + δ
programmatically; admitted via universal-paths).

BASELINE fd543fa (the #67 method-decision register
backfill commit). primary-persona's BASELINE advances per
`pos-amend apply`.
