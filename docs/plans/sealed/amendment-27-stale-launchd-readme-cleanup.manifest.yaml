schema_version: 1
amendment:
  number: 27
  slug: stale-launchd-readme-cleanup
  title: "stale launchd README cleanup — clarify historical-reference status"

# BASELINE: 006f5b9 — the current git tip (chore(seals): teardown-
# observability-retrofit seal — amendment #26). Empty-diff window at
# amendment-commit time per the established pattern. Follow-on
# chore(seals) commit advances the sidecar to the amendment SHA.
baseline: 006f5b9
plan: docs/plans/amendment-27-stale-launchd-readme-cleanup.md

# Single-component doc-hygiene amendment. Rewrites one file:
# memory-system/launchd/README.md. Admitted by memory-system's
# existing `memory-system/` allowed-prefix entry. No extra admissions
# needed. Hands-off-lifecycle is NOT touched (no narrative, no
# sidecar advance) — intentionally omitted from the manifest.
components:
  - name: memory-system
    seal_test: memory-system/tests/test_no_sealed_amendments.py
    sidecar: memory-system/tests/SEAL_COMMIT
    extra_allowed_prefixes: []

# Universal admissions — per amendment #22's ruling #3.
universal_paths:
  prefixes:
    - docs/plans/
  files:
    - CLAUDE.md
    - docs/odd-in-pos.md
    - docs/odd-methodology.md
    - docs/FUTURE_IDEAS.md
