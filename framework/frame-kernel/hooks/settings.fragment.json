{
  "_comment": "STAGED frame-kernel frame-boundary hook wiring — NOT a live-harness edit. This fragment is the authoritative, PORTABLE shape of the `hooks` block that registers BOTH frame-kernel hooks: the SubagentStart auto-context handoff (loam-realignment SLICE 1a) and the SubagentStop out-of-band frame-consistency check (loam-realignment SLICE 1b). Activation path (RF-1 CLOSED by the sealed workspace-sync settings-fragment auto-composer, 45cdf973; comment refreshed by the memory-recall cycle, AC.DMP.2): this fragment composes into a workspace's .claude/settings.json automatically on the next successful pos-sync — additively, idempotently, preserving non-loam entries, with ${LOAM_REPO} resolved to the absolute repo root. Live activation timing in an existing workspace remains DISPATCHER-TIMED (memory-recall-cycle D3): sync with --no-compose to defer, or hand-merge per the auto-composer's shape. Both hooks are fail-soft: SubagentStart exits 0 on every path and never blocks a subagent dispatch (AC.SACH.4 — a degraded bundle still lets the subagent start); SubagentStop exits 0 on every path and never blocks a subagent's return (AC.SSFC.5 — an off-frame result surfaces a NON-BLOCKING flag, never a hard abort).",
  "hooks": {
    "SubagentStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "${LOAM_REPO}/.venv/bin/python ${LOAM_REPO}/framework/frame-kernel/hooks/subagent_start_context.py",
            "timeout": 10
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "${LOAM_REPO}/.venv/bin/python ${LOAM_REPO}/framework/frame-kernel/hooks/subagent_stop_frame_check.py",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
