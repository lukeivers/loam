# post-M6 partition realignment — gate-test reclassification + corpus-gate path-list + dev-mode-manifest roots/always_loaded refresh

2026-04-29. Closes the LAST PRE-PUBLISH BLOCKER per dispatcher
recommendation analysis 2026-04-29 — three coherent surfaces, each
a "post-M6b.0 path move not fully propagated" instance.

Surface A — gate-test partition reclassification (M9 plan §7
finding #1). 12 gate-test files at framework/hands-off-lifecycle/
tests/test_AC_AG_*.py (5 files: 1_wrong_wd_dispatch +
2_method_enumerated_prompt + 3_stale_dispatch + 4_no_op_normal_use
+ 5_audit_log) + test_AC_BAG_*.py (7 files: 1_secret_commit +
2_blast_radius + 3_amend_in_subagent + 4_loam_amend_dry_run +
5_wrong_tree_write + 6_no_op_non_matching + 7_audit_log) sys.path-
probe into plugins/dev-sdlc/hooks/ (dev_only). Pre-fix they
classified dev_and_public via the broad framework/hands-off-
lifecycle/** glob; post-fix they classify dev_only via an explicit
glob entry that wins per partition-precedence rule #2 (dev_only
checked before dev_and_public). Synthesised public artefact no
longer carries gate-test files that depend on dropped plugin
modules.

Surface B — corpus_gate hook on-demand path-list update. framework/
hands-off-lifecycle/hooks/corpus_inline_session_start.py _ON_DEMAND
constant lines 135-139: first two entries rebased from docs/odd-
methodology.md + docs/odd-in-loam.md (pre-M6b.0 paths) to plugins/
dev-sdlc/docs/odd-methodology.md + plugins/dev-sdlc/docs/odd-in-
loam.md (post-M6b.0 plugin-tree paths). The hook's mode-partition
early-return (line 333-334 — mode != "dev-mode" no-ops) is the
graceful-skip mechanism for NORMAL-USE workspaces where the plugin
is dropped; no additional graceful-skip wiring needed.

Surface C — dev-mode-manifest.yaml roots: + always_loaded:
realignment (M1c-corrective plan §16 HSF#1). plugins/dev-sdlc/dev-
mode-manifest.yaml roots: block (lines 36-64) + always_loaded:
block (lines 78-110) referenced 15 top-level component dirs that
MOVED under framework/ post-M6b.0. Realignment shape:
  - 14 component refs rebased to framework/<comp>/ — cost-
    governance, hands-off-lifecycle, memory-system, objective-
    tracker, observability-aggregator, orchestrator, primary-
    persona, reversibility-primitive, safety-layer, scope-of-work,
    self-correction, self-upgrade, telegram-interface, workspace-
    bootstrap.
  - 1 component RENAME + REBASE — graceful-degradation/ → framework/
    dormancy/ (the component was renamed to dormancy/ pre-M6b.0;
    the manifest carried the old name forward). HSF#1 surfaced.
  - 1 component ADD — framework/workspace-sync/ — a missing
    component admission (workspace-sync was never in the manifest;
    original sub-plan F authoring's 15-component list predated
    the workspace-sync component and no subsequent amendment added
    it). HSF#2 surfaced.
  - tools/ → framework/tools/.
  - first-run-inventory.yaml → framework/first-run-inventory.yaml.
  - data/ STAYS top-level (workspace runtime telemetry, not a
    component source surface).
  - docs/ + CLAUDE.md + CLAUDE.dev.md + README.md STAY top-level.
  - dev_only: block NO EDITS (already realigned at M6c HSF#1 fix
    + M1c-corrective HSF#1 fix per HSF#5 finding).

AC surface (AC.PMR.1..PMR.7 + AC.PMR.S):

  - AC.PMR.1: 12 gate-test files classify dev_only via classify_path.
    Negative-control: non-gate test files in same dir continue
    dev_and_public.
  - AC.PMR.2: corpus_inline_session_start.py _ON_DEMAND first 2
    entries are plugins/dev-sdlc/docs/odd-{methodology,in-loam}.md;
    end-to-end SessionStart envelope drives main() and rendered
    on-demand block lists both plugin-relative paths.
  - AC.PMR.3: dev-mode-manifest roots: every entry resolves on disk;
    graceful-degradation/ → dormancy/ rename verified; framework/
    workspace-sync/ ADD verified.
  - AC.PMR.4: dev-mode-manifest always_loaded: every glob/path
    resolves to a non-empty match-set; data/ stays top-level;
    first-run-inventory.yaml rebased.
  - AC.PMR.5: compute_corpus_paths_required(workspace_root, "dev-
    mode") returns ≥10 paths, all of which exist on disk via the
    existing two-tier fall-through.
  - AC.PMR.6: corpus_gate sentinel state ∈ {"loaded", "partial"}
    post-realignment (NOT "missing"). The "loaded vs partial"
    branch depends on subset relationship between inline hook's
    static 3-path always-load and manifest-derived required set;
    AC asserts only that the degenerate "missing" state is gone.
  - AC.PMR.7: pre-existing tests pass — gate hook tests + corpus
    load tests + partition tests + 60 loam-mode tests.
  - AC.PMR.S: seal-diff invariant — 3 sealed components fence;
    edits land only under framework/hands-off-lifecycle/, plugins/
    dev-sdlc/, framework/tools/pos-publish-framework-only/, or
    universal admissions.

Method-level choices (builder's call per ODD §1.1):

  - D-build.PMR.1: Surface A entry shape — glob-with-pattern vs
    explicit 12-path list. Glob-with-pattern matches partition
    manifest's existing entry-shape precedent.
  - D-build.PMR.2: Surface C roots: style — per-component-glob
    (preserves granularity, matches publish-mode manifest pattern)
    vs bulk framework/** (loses surgical control).
  - D-build.PMR.3: workspace-sync/ ADD in-amendment vs deferred.
    In-amendment per scope-cohesion (single-source-of-truth pass).
  - D-build.PMR.4: AC.PMR.3+.4 test-file placement — plugins/dev-
    sdlc/tests/ (matches AC.PMR.S fence) vs plugins/dev-sdlc/tools/
    loam-mode/tests/ (co-locates with consumer).
  - D-build.PMR.5: Manifest-authoring commit ladder — separate
    docs(plans): for manifest yaml + feature commit for source/
    test (mirrors M9 + M1c-corrective precedent) vs single feature
    commit.

Out of scope (per plan §6):

  - 3 silent-swallow patterns in memory-system + 6 in orchestrator/
    supervisor.py (graceful-fallthrough-with-detection CDC
    retroactive audit; post-v0.1.0).
  - pos_orchestrator editable-install provisioning gap (HSF#3 from
    M1c-corrective; pre-existing; not pre-publish blocker).
  - Reorganising dev_only: block beyond workspace-sync ADD.
  - Adding NORMAL-USE-mode graceful-skip for missing plugin docs
    in corpus_inline_session_start.py (verified existing mode-
    partition is the skip).
  - Substitution-table extension (realigned paths are workspace-
    relative, not canonical-host).
  - Promoting data/ from always_loaded to its own root partition.
  - Anything not on Surface A / B / C.

BASELINE 699a391 (current HEAD post-M1c-corrective SHA backfill).
Each component's SEAL_COMMIT sidecar lands at the seal SHA via
`loam amend seal --scoped-sweep`. pos-publish-framework-only
admitted via universal_paths.prefixes (no per-component seal-test
sidecar — same shape M9 used; the structural surface is the
partition manifest YAML edit + 1 new test under tests/).

# M8-corrective — HC#4 byte-content rebaseline post-Apache-header insertion

2026-05-01. Foldback for M7-partition-fix HT-4 halt (HSF#1):
M8 commit `6bef03b` ("feat(public): M8 license-governance —
Apache headers on runtime .py + SECURITY.md tightening") inserted
a 14-line Apache-2.0 license header at the top of every runtime
.py file across 15 dev_and_public framework components per
AC.OSS.4, but did NOT perform the corresponding HC#4 retire-and-
rebaseline of the captured SHAs in HOL's
`test_d1_byte_content_match.py`. 13 of the test's 15 sample
files (all .py samples; the 2 pyproject.toml samples were
unchanged) diverged.

Locked decision (dispatcher directive 2026-05-01; Path A clean-
closure recommended by M7-partition-fix dispatch agent + locked
by main session per autonomy directive): recompute the 13 SHAs
on current HEAD, update the constants in
`framework/hands-off-lifecycle/tests/test_d1_byte_content_match.py`,
and add inline ODD §4 in-band retire-and-rebaseline comments
naming M8 (`6bef03b`) + AC.OSS.4 as the legitimate rebaseline
source.

Per `feedback_loose_AC_text_fix_AC_not_implementation` analog:
when implementation matches AC intent (Apache-header insertion
was the AC.OSS.4 directive) and the test's pinned bytes diverge
for a legitimate reason, rebaseline the test, not re-author the
impl. Same in-band rebaseline pattern as M-FBM applied for
primary-persona/__init__.py + session_start_emitter.py.

Structural remedy (single file edit):

  - `framework/hands-off-lifecycle/tests/test_d1_byte_content_match.py`
    — 13 SHA-256 constant updates + 13 inline narrative comments
    naming M8 + AC.OSS.4.

AC surface (AC.M8-corrective.1 + AC.M8-corrective.2 + AC.M8-corrective.S):

  - AC.M8-corrective.1: SHA-256 constants for the 13 .py samples
    match current bytes post-M8.
  - AC.M8-corrective.2: HOL's `test_d1_byte_content_match.py`
    passes 16/16 (13 rebaselined .py + 2 unchanged pyproject.toml
    + structural ≥15-samples check).
  - AC.M8-corrective.S: seal-diff invariant — only paths under
    `framework/hands-off-lifecycle/` + universal-paths.

Method-level choices (builder's call per ODD §1.1):

  - SHA recomputation tool (D1): Python `hashlib.sha256` against
    each file at current HEAD. Equivalent to `sha256sum`.
  - Comment style (D2): each rebaselined entry gains a
    per-constant inline comment naming M8 + the rebaseline
    reason (mirrors the M-FBM precedent shape on the same file).
  - Test scope (D3): narrow to HOL's `test_d1_byte_content_match.py`
    for pre-seal verification per `feedback_amendment_dispatch_speedups`.
    Cross-component seal-diff sweep at seal-time is sufficient.

Out of scope (per plan §8):

  - Re-running the M8 Apache-header script (M8 already executed
    it across 700 files; bytes are correct).
  - Re-baselining the 2 pyproject.toml samples (their bytes
    unchanged by M8; SHAs still match).
  - Edits to other HOL test files (only test_d1_byte_content_match.py
    is affected).
  - Touching M8's commit `6bef03b` (stays in audit trail; this
    corrective is an additive followup).

BASELINE `403bccd` (feature commit). HOL's SEAL_COMMIT sidecar
lands at the seal SHA via `loam amend seal`.

# M7-partition-fix — add docs/plugins/** glob to partition manifest

2026-05-01. Foldback for M11a dispatch-1 halt F-M11a.1 (synthesis
tool errors with `partition incomplete: 1 unclassified path(s);
samples: ['docs/plugins/dev-sdlc.md']`).

M7 commit `2fefd8b` ("docs(public): M7 plugins category — Dev/SDLC
plugin reference") added `docs/plugins/dev-sdlc.md` per M7 plan-doc
D-Q.M7.6 = (c) "separate `docs/plugins/<name>.md` category". M2's
partition manifest at
`framework/tools/pos-publish-framework-only/publish-mode-manifest.yaml`
was authored before M7 and never extended to admit the new
directory. M11a dispatch-1 fired §9.9 (synthesis tool errors on
partition incomplete) when run against `47cbea7`.

Locked decision (dispatcher directive 2026-05-01; no ambiguity):
add `- glob: "docs/plugins/**"` to the `dev_and_public:` block,
alongside the existing `docs/components/**` glob entry (around
line 157). Pre-flight verified `docs/plugins/dev-sdlc.md` carries
ZERO AC.OSS.3 residuals (pos-amend / loam-amend / A1-A4 /
loam-mode / docs/rebuild / pos-publish-framework-only /
odd-methodology / odd-in-loam / duration-estimation-rubric) and
ZERO AC.OSS.5 source-side residuals — public-plugin-reference is
clean for ship.

HOL is no-op narrative anchor only (per M2 + post-m6-partition-
realignment precedent) — sidecar bump + this notes file only;
no behaviour edits to HOL.

Structural remedy:

  - `framework/tools/pos-publish-framework-only/publish-mode-manifest.yaml`
    — add one glob entry under `dev_and_public:` (the new
    `docs/plugins/**` line + 6-line provenance comment).

AC surface (AC.M7-fix.1 + AC.M7-fix.2 + AC.M7-fix.S):

  - AC.M7-fix.1: partition manifest's `dev_and_public:` block
    contains `- glob: "docs/plugins/**"` alongside the existing
    `docs/components/**` glob.
  - AC.M7-fix.2: synthesis tool runs cleanly against post-fix HEAD
    (no `SynthesisError`, no `partition incomplete` error);
    `docs/plugins/dev-sdlc.md` appears in the synthesised tree.
  - AC.M7-fix.S: seal-diff invariant — only paths under
    `framework/tools/pos-publish-framework-only/` (structural) +
    `framework/hands-off-lifecycle/tests/SEAL_COMMIT*` (anchor) +
    universal-paths.

Method-level choices (builder's call per ODD §1.1):

  - Single-line glob entry under `dev_and_public:` (D1 locked by
    dispatcher; alternative shapes — per-plugin path entry, broader
    `docs/**` glob — rejected by directive).
  - HOL no-op anchor (D2 — convention per M2 precedent; tools-tree
    amendments need a sealed-component anchor since pos-publish-
    framework-only carries no test_no_sealed_amendments.py).
  - Synthesis smoke check post-fix (sanity test, not part of
    M11a's full sweep — that's M11a-2's job on re-dispatch).

Out of scope (per plan §8):

  - The full M11a sweep (AC.M11a.1..6) — re-dispatches against
    post-fix HEAD per D-Q.M11.4.
  - Other partition gaps that may surface during M11a-2 — separate
    corrective amendments.
  - Retroactive M7 plan-doc edits (M7 D-Q.M7.6 ruling was correct;
    omission was at M2-manifest-extension-time).
  - `docs/plugins/<future-plugin>.md` entries (the glob admits
    future additions under the same pattern).

BASELINE `b1dc662` (current HEAD; post-M11a-halt §14 commit). The
HOL anchor's SEAL_COMMIT sidecar lands at the seal SHA via
`loam amend seal`.

# C1 — Classes A+B partition reclassification

2026-05-02. Foldback for M11a dispatch-2 halt at AC.M11a.2 (three
named classes of public-tree dev-discipline-machinery residuals
surfaced by the M11a sweep). C1 closes Classes A + B; C2 (separate
sealed-component cycle, lands next) closes Class C.

Source surfaces (audit trail):

  - M11a dispatch-2 sweep report (per-file detail) at
    `<workspace>/.scratch/claude-output/oss-v0-1-0-publish-m11a-
    sweep-report.md`.
  - M11 plan-doc D-Q.M11.4 (always halt-and-surface; no
    auto-foldback at M11a; foldback amendments authored sealed
    before re-dispatch).
  - C1+C2 combined sub-plan at `docs/rebuild/plans/
    oss-v0-1-0-publish-public-docs-classes-abc.md`.
  - M7-partition-fix precedent (small-scope sealed-cycle + HOL
    no-op anchor): `docs/rebuild/plans/oss-v0-1-0-publish-public-
    docs-partition-fix.md`.

Locked decisions (plan §11; all 5 baked per autonomy directive):

  - D-Q.ABC.2 (Class A glob shape) = (a) framework-wide
    `**/seals/**`; verified zero false-positive matches outside
    the seven per-component `seals/` dirs at plan-time.
  - D-Q.ABC.3 (Class B glob shape) = (a) targeted exact patterns
    (`**/tests/test_no_sealed_amendments.py` +
    `**/tests/test_AC_*_seal_diff_*.py`); precision avoids
    accidentally excluding legitimate public tests.

HOL is no-op narrative anchor only (per M2 + M7-partition-fix
precedent) — sidecar bump + this notes file only; no behaviour
edits to HOL.

Structural remedy:

  - `framework/tools/pos-publish-framework-only/publish-mode-
    manifest.yaml` — add three glob entries under `dev_only:`:
    `**/seals/**`, `**/tests/test_no_sealed_amendments.py`,
    `**/tests/test_AC_*_seal_diff_*.py` (with provenance comments
    citing this plan-doc + D-Q.ABC.2 + D-Q.ABC.3).

AC surface (AC.ABC-A.1 + AC.ABC-B.1 + AC.ABC-B.2 + AC.ABC-C1.S):

  - AC.ABC-A.1: post-fix synthesis output carries zero blobs under
    any `seals/` directory across the framework tree.
  - AC.ABC-B.1: post-fix synthesis output carries zero blobs
    matching `**/tests/test_no_sealed_amendments.py`.
  - AC.ABC-B.2: post-fix synthesis output carries zero blobs
    matching `**/tests/test_AC_*_seal_diff_*.py`.
  - AC.ABC-C1.S: seal-diff invariant — only paths under
    `framework/tools/pos-publish-framework-only/` (structural) +
    `framework/hands-off-lifecycle/tests/SEAL_COMMIT*` (anchor) +
    universal-paths.

Verification empirically performed pre-seal: synthesis CLI
advanced refs/heads/framework-only against the post-feature HEAD;
`git ls-tree -r refs/heads/framework-only | grep -E '/seals/'`
returns zero hits; `... | grep -E 'test_no_sealed_amendments\\.py'`
returns zero hits; `... | grep -E 'test_AC_.*_seal_diff'` returns
zero hits. AC.ABC-A.1 + AC.ABC-B.1 + AC.ABC-B.2 PASS.

Method-level choices (builder's call per ODD §1.1):

  - Three glob entries appended to existing `dev_only:` block
    (under the `test_AC_AG_*` / `test_AC_BAG_*` HOL gate-test
    block; structural co-location with existing dev_only test
    admissions).
  - HOL no-op anchor (convention per M2 + M7-partition-fix
    precedent; tools-tree amendments need a sealed-component
    anchor since pos-publish-framework-only carries no
    test_no_sealed_amendments.py).
  - Synthesis smoke check post-fix verifies all three globs
    zero out the synthetic tree (sanity check, not part of
    M11a's full sweep — that's M11a-3's job).

Out of scope (per plan §8):

  - Class C remediation — that's C2's separate sealed-component
    cycle (multi-component fence per §5.4 of the plan-doc);
    C1 must land first because it closes Classes A + B and
    narrows what M11a-3 needs to verify.
  - The full M11a sweep (AC.M11a.1..6) — re-dispatches against
    post-C2 HEAD per D-Q.M11.4.
  - Future v0.x components inheriting the same partition shape
    (the framework-wide `**/seals/**` glob already admits any
    future component's seals/ subtree).

BASELINE `4d75385` (M11a dispatch-2 §14 register commit). The
HOL anchor's SEAL_COMMIT sidecar lands at the seal SHA via
`loam amend seal`.

# C2-bis — test-fence partition extension

2026-05-02. Foldback for C2 halt at HT-C2.4 (109-file test-residual
scope mismatch surfaced during C2 build investigation; C2 plan
§5.4's 26-file enumeration was partial — restricted to production
source/doc files only, but AC.M11a.2's literal-match grep across
the full synthetic tree includes 109 test files containing dev-
only literals as assertion strings, fixture content, comments, and
docstrings). C2-bis closes the test-residual scope mechanically
via single-glob partition extension; C2-prime (separate sealed-
cycle, lands next) closes the smaller accurate Class C population
(~21 production files).

Source surfaces (audit trail):

  - C1+C2 combined sub-plan at `docs/rebuild/plans/
    oss-v0-1-0-publish-public-docs-classes-abc.md` (§14
    D-build.ABC-C2.HALT narrative).
  - C2 halt-and-surface report at `<workspace>/.scratch/
    claude-output/oss-v0-1-0-publish-classes-abc-c2-halt.md`.
  - C2-bis sub-plan at `docs/rebuild/plans/oss-v0-1-0-publish-
    public-docs-classes-abc-bis.md`.
  - M7-partition-fix precedent (small-scope sealed-cycle + HOL
    no-op anchor): `docs/rebuild/plans/oss-v0-1-0-publish-public-
    docs-partition-fix.md`.
  - C1 amendment precedent (mechanical Classes A + B partition
    reclassification, sealed `e2cbeec`).

Locked decision (dispatcher autonomy directive):

  - Add `- glob: "**/tests/**"` to `dev_only:` block. Industry-
    convention shape — no public-shipping artefact under any
    modern Python package's `tests/` directory in v1.0 OSS
    releases (cpython, numpy, requests precedent).

HOL is no-op narrative anchor only (per M2 + M7-partition-fix +
C1 precedent) — sidecar bump + this notes file only; no behaviour
edits to HOL.

Structural remedy:

  - `framework/tools/pos-publish-framework-only/publish-mode-
    manifest.yaml` — add one glob entry under `dev_only:`:
    `**/tests/**` (with provenance comment citing the C2-bis
    plan-doc + the industry-convention rationale).

AC surface (AC.ABC-Cbis.1 + AC.ABC-Cbis.2 + AC.ABC-Cbis.S):

  - AC.ABC-Cbis.1: M2 partition manifest's `dev_only:` block
    contains `- glob: "**/tests/**"`.
  - AC.ABC-Cbis.2: post-fix synthesis output carries zero blobs
    under any `tests/` directory (`git ls-tree -r refs/heads/
    framework-only | grep '/tests/'` returns 0 hits).
  - AC.ABC-Cbis.S: seal-diff invariant — only paths under
    `framework/tools/pos-publish-framework-only/` (structural) +
    `framework/hands-off-lifecycle/tests/SEAL_COMMIT*` (anchor) +
    universal-paths.

Verification empirically performed pre-seal: synthesis CLI
advanced refs/heads/framework-only against the post-feature HEAD;
`git ls-tree -r refs/heads/framework-only | grep -E '/tests/'`
returns zero hits. AC.ABC-Cbis.1 + AC.ABC-Cbis.2 PASS.

Method-level choices (builder's call per ODD §1.1):

  - Single glob entry appended to existing `dev_only:` block
    (under the C1-locked `**/tests/test_AC_*_seal_diff_*.py`
    block; structural co-location with existing dev_only test
    admissions). The C1-locked exact globs remain in place as
    audit-trail for D-Q.ABC.3 = a precision ruling — they are
    subsumed by the broader `**/tests/**` but not conflicting.
  - HOL no-op anchor (convention per M2 + M7-partition-fix +
    C1 precedent; tools-tree amendments need a sealed-component
    anchor since pos-publish-framework-only carries no
    test_no_sealed_amendments.py).
  - Synthesis smoke check post-fix verifies the new glob zeroes
    out the synthetic tree's `tests/` files (sanity check, not
    part of M11a's full sweep — that's M11a-3's job, post-
    C2-prime).

Out of scope (per plan §8):

  - C2-prime (Class C 21-file production remediation) — separate
    sealed-cycle authored at `docs/rebuild/plans/oss-v0-1-0-
    publish-public-docs-classes-abc-prime.md`; lands next.
  - The full M11a sweep (AC.M11a.1..6) — re-dispatches against
    post-C2-prime HEAD per D-Q.M11.4.
  - Removal of the C1-locked exact globs (cosmetic; out of
    scope here — kept for audit trail).
  - Per-test classification or test-rewrite — industry-convention
    shape rules: tests don't ship publicly in v1.0 OSS releases.

BASELINE `c868618` (HEAD post-C1-§14-backfill — C1 sealed
`e2cbeec` + C2 halt narrative recorded). The HOL anchor's
SEAL_COMMIT sidecar lands at the seal SHA via `loam amend seal`.

# C2-prime — Class C 21 RW + 6 SUB/MIXED production-file remediation

2026-05-02. Foldback for M11a dispatch-2 halt at AC.M11a.2
(AC.OSS.3 literal-match grep on production-source Class C
residuals; post-C1 + post-C2-bis the remaining surface was the
27-file production-source Class C population enumerated in the
C2-prime sub-plan §5.4).

Source surfaces (audit trail):

  - C1+C2 combined sub-plan at `docs/rebuild/plans/
    oss-v0-1-0-publish-public-docs-classes-abc.md` (§14
    D-build.ABC-C2.HALT narrative; superseded by C2-prime for
    the C2 portion only — C1 sealed `e2cbeec` retained).
  - C2 halt-and-surface report at `<workspace>/.scratch/
    claude-output/oss-v0-1-0-publish-classes-abc-c2-halt.md`.
  - C2-bis sub-plan (test-fence partition extension; sealed
    `990e95c`).
  - C2-prime sub-plan at `docs/rebuild/plans/oss-v0-1-0-publish-
    public-docs-classes-abc-prime.md`.

All four §11 D-Q.ABC-prime decisions ruled to plan recommendations
per autonomy directive (each had structural grounding +
reversibility):

  - D-Q.ABC-prime.1 — per-file shape overrides for files 4, 5,
    18, 26 (revised from predecessor REWRITE-as-default to SUB
    for load-bearing constants + REWRITE-drop for file 26's
    synthesise-hint). APPROVE.
  - D-Q.ABC-prime.2 — M9 SUBSTITUTION_TABLE +8 entries (3
    D-Q.ABC.4 carry-forward + 5 D-Q.ABC-prime.2). STRIP applied
    for FUTURE_IDEAS{,_DRAFT}.md (no public counterpart;
    gate-helper handles 0-entry tuple fail-soft). APPROVE.
  - D-Q.ABC-prime.3 — File 18 SUBSTITUTE confirmed (entry 12
    ``docs/rebuild/plans/`` → ``docs/components/``).
  - D-Q.ABC-prime.4 — File 26 REWRITE-drop the synthesise-hint
    confirmed.

Surface changes (multi-component fence):

REWRITE/REMOVE — 21 files (cosmetic prose / comments /
docstrings referencing dev-only paths rewritten to public
counterparts or stripped):

  - docs/CLAUDE_CAPABILITIES.md (5 pos-amend + 1 odd-methodology)
  - framework/dormancy/docs/architecture.md
  - framework/hands-off-lifecycle/README.md
  - framework/hands-off-lifecycle/hooks/corpus_load_sentinel.py
    (loam-mode prose RW; legacy + framework_manifest path
    fallbacks restored as path-segment construction — runtime
    AC.SFR.3 binding preserved)
  - framework/hands-off-lifecycle/hooks/first_run_helper.py
    (13 loam-mode → dev-mode-session-start)
  - framework/hands-off-lifecycle/hooks/first_run_settings.py
    (8 loam-mode → dev-mode-session-start)
  - framework/hands-off-lifecycle/hooks/settings.json.fragment
  - framework/hands-off-lifecycle/hooks/statusline.py
  - framework/orchestrator/scripts/pos_session_start.py
  - framework/orchestrator/src/loam/orchestrator/__main__.py
  - framework/orchestrator/src/loam/orchestrator/orchestrator.py
  - framework/primary-persona/src/loam/primary_persona/context_composer.py
  - framework/primary-persona/src/loam/primary_persona/dispatch_wrapper.py
    (duration-estimation rubric → budget-inference rubric)
  - framework/primary-persona/src/loam/primary_persona/session_start_emitter.py
    (HC#4 SHA bump for cosmetic-prose RW)
  - framework/primary-persona/templates/persona-template/prompt.md
    (capability-index 8-entry path table DROPPED per predecessor
    §11 D-Q.ABC.5(b); FUTURE_IDEAS_DRAFT path generic-prose
    rewrite)
  - framework/safety-layer/docs/architecture.md
  - framework/safety-layer/src/loam/safety_layer/__init__.py
  - framework/safety-layer/src/loam/safety_layer/events.py
  - framework/safety-layer/src/loam/safety_layer/observability.py
  - framework/workspace-bootstrap/src/loam/workspace_bootstrap/new_workspace.py
    (REWRITE-drop synthesise-hint per D-Q.ABC-prime.4(a))
  - framework/workspace-sync/README.md

SUBSTITUTE — KEEP CANONICAL LITERALS (load-bearing source
paths/tuples/constants stay verbatim; SUBSTITUTION_TABLE
rewrites at synth time):

  - CLAUDE.md (file 1; entry 5 → docs/positioning.md)
  - framework/hands-off-lifecycle/hooks/_gate_helpers.py (file 4;
    STRIP for FUTURE_IDEAS{,_DRAFT}.md exact-admission
    entries — parent ``docs/`` prefix carve-out admits in dev;
    odd-methodology + odd-in-loam handled by entries 7+8)
  - framework/hands-off-lifecycle/hooks/corpus_inline_session_start.py
    (file 5; MIXED — SUB for _ALWAYS_LOAD + _ON_DEMAND tuples;
    RW for 2 loam-mode docstring refs; STRIP for
    FUTURE_IDEAS.md _ON_DEMAND entry + docstring references)
  - framework/primary-persona/src/loam/primary_persona/session_start_gate.py
    (file 18; STRIP for FUTURE_IDEAS.md _FALLBACK_BASELINE_PATHS
    entry; entry 12 SUB rewrites docstring ``docs/rebuild/
    plans/`` to ``docs/components/`` — function-body path-
    segment construction unchanged, runtime fail-soft)
  - framework/workspace-bootstrap/src/loam/workspace_bootstrap/adapters/first_run_scaffold.py
    (file 24; SUB-only — 3 hits handled by entries 5+7)
  - framework/workspace-bootstrap/src/loam/workspace_bootstrap/adapters/tracker_seed.py
    (file 25; SUB-only — 4+ hits handled by entries 5+6)

M9 SUBSTITUTION_TABLE +8 entries (4 M9-locked + 3 D-Q.ABC.4 +
5 D-Q.ABC-prime.2 = 12 total; framework/tools/
pos-publish-framework-only/src/loam/publish_framework_only/
substitution.py):

  - Entry 5 (D-Q.ABC.4): docs/rebuild/VALUE_PROPOSITION.md
    → docs/positioning.md.
  - Entry 6 (D-Q.ABC.4): docs/rebuild/spec/loam-objectives-
    spec.md → docs/architecture.md.
  - Entry 7 (D-Q.ABC.4): docs/odd-methodology.md
    → docs/design/odd.md.
  - Entry 8 (D-Q.ABC-prime.2): docs/odd-in-loam.md
    → docs/design/odd.md.
  - Entry 9 (D-Q.ABC-prime.2): plugins/dev-sdlc/docs/odd-
    methodology.md → docs/design/odd.md.
  - Entry 10 (D-Q.ABC-prime.2): plugins/dev-sdlc/docs/odd-
    in-loam.md → docs/design/odd.md.
  - Entry 11 (D-Q.ABC-prime.2): docs/rebuild/STATE.md
    → docs/getting-started.md.
  - Entry 12 (D-Q.ABC-prime.2): docs/rebuild/plans/
    → docs/components/. Trailing-slash entry; precedes any
    future no-slash partner.

Test rebaselines (ODD §4 in-band per
`feedback_loose_AC_text_fix_AC_not_implementation`):

  - test_AC_OSS_M9_substitution_smoke.py — TABLE size 4 → 12
    with explicit assertion of all 8 new sources +
    idempotence check.
  - test_AC_PMR_1_gate_tests_dev_only.py — negative-control
    test rebaselined to dev_only post-C2-bis test-fence
    (was previously asserting DEV_AND_PUBLIC; pre-existing
    post-C2-bis stale).
  - test_AC_CI_2_on_demand_pointer_block.py — FUTURE_IDEAS.md
    pointer no longer in on-demand block.
  - test_AC_PMR_2_corpus_gate_on_demand_paths.py — _ON_DEMAND
    tuple asserts 2 entries (was 3); FUTURE_IDEAS.md STRIP.
  - test_AC_alpha_1_capability_leverage_spine.py — capability
    index no longer enforces inline 8-entry path table; rule
    names "capability corpus" generically.
  - test_d1_byte_content_match.py — HC#4 SHA bump for
    session_start_emitter.py.

AC surface (AC.ABC-Cprime.1 + AC.ABC-Cprime.2 +
AC.ABC-Cprime.3 + AC.ABC-Cprime.S):

  - AC.ABC-Cprime.1: post-fix synthesis output carries zero
    AC.OSS.3 banned literals (pos-amend / loam-amend /
    loam-mode / docs/rebuild/ / odd-methodology / odd-in-loam /
    duration-estimation-rubric / pos-publish-framework-only).
    Verified pre-seal via ``.venv/bin/pos-publish-framework-
    only --quiet`` + per-literal ``git grep -F -l`` against
    ``refs/heads/framework-only`` — zero hits across all 8
    literals.
  - AC.ABC-Cprime.2: M9 SUBSTITUTION_TABLE size = 12; all
    replacements absent from sources (idempotence verified by
    re-running synthesis on already-synthesised tree —
    framework-only SHA stable across two runs:
    88c883de3f397dfd03053b8b304f52207fa1c08a).
  - AC.ABC-Cprime.3: SUBSTITUTE files preserve canonical-mode
    behaviour by construction (M9 SUB applied AFTER partition
    filter; canonical reads original literal at runtime).
    Per-component pytest passes on every touched component.
  - AC.ABC-Cprime.S: Sealed-component fence — 7 framework
    components + universal-paths (pos-publish-framework-only,
    docs/rebuild/plans/, top-level CLAUDE.md +
    docs/CLAUDE_CAPABILITIES.md).

Method-level choices (builder's call per ODD §1.1):

  - Single feature commit bundling per-file edits + M9 TABLE
    extension (cheaper coordination than per-component
    commits; multi-component bundle precedent per dispatch).
  - One STRIP-comment corrective NEW commit
    (`feedback_no_amend_in_agent_dispatches`) — the initial
    feature commit's STRIP-explanation comment carried the
    very ``docs/rebuild/FUTURE_IDEAS.md`` literal it was
    documenting; corrective rephrased to "FUTURE_IDEAS-style
    draft-capture surface" to clear the residual.
  - Restored corpus_load_sentinel.py's 3-path manifest probe
    after initial RW broke AC.SFR.3 single-framework-
    restructure tests; the path-segment-construction shape
    carries no textual banned literals (each of "docs",
    "rebuild", "dev-mode-manifest.yaml" is a separate
    Python token; AC.OSS.3 grep on ``docs/rebuild/`` doesn't
    match the multi-segment construction).
  - HC#4 byte-content rebaseline: 1 sample file
    (session_start_emitter.py) updated; cosmetic-prose RW
    intent matches AC.OSS.3.

Halt-and-surface findings (one surfaced; resolved):

  - HSF#1 — Pre-existing failures in
    test_AC35_6_framework_not_content.py +
    test_AC40_7_framework_not_content.py:
    ``Luke`` substring in M8-injected Apache-2.0 license
    header trips the persona-prose-constant assertion
    ``"Luke"``. Pre-existing failure (verified via stash
    +baseline pytest run); NOT introduced by C2-prime.
    OUT-OF-FENCE for C2-prime; surfaced for owner to dispatch
    a separate M8-corrective2 (tighten the persona-prose
    forbidden-substring list to exclude license-header
    ``Luke Ivers`` mentions per
    ``feedback_loose_AC_text_fix_AC_not_implementation``).
  - HSF#2 — corpus_load_sentinel.py initial RW removed
    ``legacy_manifest`` + ``framework_manifest`` fallbacks
    per plan §5.4 file 6's wording. Verification via
    per-component pytest immediately surfaced 3 AC.SFR.3
    test failures (``test_compute_corpus_paths_required_
    falls_through_to_framework`` + 2 siblings asserting on
    the framework-fallback contract). Plan-author wording
    was wrong about "the only viable location is plugins/";
    framework/docs/rebuild/dev-mode-manifest.yaml IS
    load-bearing for AC.SFR.3 (framework-only-shaped
    workspaces). Resolved by restoring the 3-path probe;
    AC.OSS.3 grep clears anyway because path-segment
    construction (``"docs" / "rebuild" / "dev-mode-manifest.
    yaml"``) carries no textual ``docs/rebuild/`` literal.
  - HSF#3 — M9 idempotence verified (re-run synthesis on
    already-synthesised tree; SHA stable
    88c883de…3a). AC.ABC-Cprime.2 + AC.OSS-M9.3 invariants
    preserved.

Out of scope (per plan §8 + ODD §2.5):

  - M11a sweep re-dispatch (M11a-3 lands next, post-C2-prime
    seal; AC.M11a.1..6 + AC.M11a.7 staging push).
  - Pre-existing M8 license-header collision tests
    (AC35.6 + AC40.7) — separate M8-corrective2 dispatch.
  - Authoring new public-shipping docs.
  - History rewrite of canonical pos-v2.

Touched-component test counts (post-C2-prime, pre-seal):

  - dormancy: 104 passed.
  - hands-off-lifecycle: 470 passed.
  - orchestrator: 102 passed.
  - primary-persona: 519 passed (2 pre-existing M8-license
    collisions, OUT-OF-FENCE per HSF#1).
  - safety-layer: 72 passed.
  - workspace-bootstrap: 252 passed, 1 skipped.
  - workspace-sync: 81 passed.
  - pos-publish-framework-only: 63 passed (admitted via
    universal-paths).

BASELINE `79b445e` (HEAD post-feature commit + STRIP-comment
corrective). hands-off-lifecycle SEAL_COMMIT sidecar advances
+ this notes file lands at the seal SHA via
``loam amend seal``.
