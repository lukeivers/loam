# strategic-compact-skill-graduation — apply ladder

2026-05-24. Sealed-component PATCH amendment graduating the
compact/clear decision-heuristic memory rule to a discoverable
loam SKILL (Wave 1 ECC absorption WI-2; second of four
sequential Wave 1 builds — predecessor: README cycle sealed
at 0a76e12; successors: token-defaults SKILL, security-hooks
bundle).

Plan: `docs/plans/strategic-compact-skill-graduation.md`.

Scope (per plan §3 / §5): two-component fence on
`plugins/loam-skills/` (load-bearing — the new
`skills/strategic-compact/` SKILL package + the five
AC.COMPACT.* tests) + `framework/workspace-bootstrap/`
(secondary — no code change; the AC.COMPACT.S outcome-altitude
smoke exercises the existing `_symlink_plugin_skills`
mechanism via test invocation only). Universal admissions:
`docs/plans/` (this plan-doc + manifest).

Single-cycle, single-seal ladder per §4 of the plan-doc.

AC families (full text in plan §2):

  - AC.COMPACT.PATH — SKILL.md exists at canonical subdirectory
    path; no flat-file predecessor (defensive regression
    catch).
  - AC.COMPACT.FRONTMATTER — valid YAML; description carries
    triggers + owner-class-only constraint; ≤1536 chars per
    Anthropic spec.
  - AC.COMPACT.BODY — three-options rubric + decision rule
    + activation triggers + composition + owner-class
    constraint named in body.
  - AC.COMPACT.BACKLINK — memory file backlink paragraph +
    rest byte-identical (skips gracefully if memory file not
    accessible).
  - AC.COMPACT.S — OUTCOME-ALTITUDE fresh-workspace
    discoverability via `_symlink_plugin_skills` against
    synthetic multi-plugin tmpfs workspace.

Method-level choices (recommendations in plan §0; rulings
backfilled at build time — all four named decisions
autonomously rule-able from existing corpus per the plan-doc's
§0 + §8 LOW-criticality determinations):

  - D-COMPACT.PATH — SUBDIRECTORY (discoverable contract).
  - D-COMPACT.BODY-SOURCE — MEMORY-RULE-PRIMARY; ECC NOT
    fetched (operational-objective test cleared).
  - D-COMPACT.TRIGGER — OWNER-CLASS ONLY encoded in
    frontmatter + body.
  - D-COMPACT.MEMORY-FATE — BACKLINK-ONLY (retain memory
    file; memory-becomes-index).
  - D-COMPACT.BACKLINK-METHOD — direct edit; halt-trigger #2
    did NOT fire (Tier-0 verified accessible).
  - D-COMPACT.ECC-ABSORPTION-DEGREE — ZERO absorption.

Halt triggers (per plan §5): none fired; all seven cleared
Tier-0 pre-source-edit per the manifest's HALT TRIGGERS
section above.

Predecessor commits (per plan §6 + Wave 1 sequence):
  - 0a76e12 — readme-ac3-synonym-list-widening corrective seal
              (Wave 1 WI-1 cycle close).
  - f26ad21 — STATE.md + plan §14 backfill (BASELINE).
  - <source-edit commit (this amendment; lands BEFORE
     `loam amend apply`)>.

BASELINE — f26ad21 (canonical-clean pre-this-amendment HEAD).
Two-component fence on `plugins/loam-skills/` +
`framework/workspace-bootstrap/`. Sidecars advance to this
amendment's seal SHA at apply time.
