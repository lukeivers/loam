---
schema_version: 3
amendment:
  # number omitted — schema v3 deprecates amendment.number per
  # AC.DPS1.10 + AC.DPS1.11. Identification by slug only.
  slug: v0-2-1-cycle-2-promotion-rubric
  title: "v0.2.1 Cycle 2 — Promotion rubric mechanism; skill-promotion-review SKILL package under plugins/dev-sdlc/skills/ with 3-signal MVP (Categorization + Quality + Conflict primary; Reusability + Tests + Usage secondary non-blocking) + 10-row decision matrix + PM-mediated owner ratification (default-to-no) + graduation amendment-cycle composition + workspace-local removal via pointer.md + on-demand `/skill-promotion-review` quarterly-trigger fallback + 4 synthetic-skill fixtures + 12 structural tests"

# BASELINE: <pending> — source-edit feat commit covering the new
# SKILL package at plugins/dev-sdlc/skills/skill-promotion-review/
# SKILL.md + 12 structural test files under plugins/dev-sdlc/tests/
# + 4 synthetic-skill fixtures under plugins/dev-sdlc/tests/fixtures/
# skill-promotion-review/synthetic-skills/.
#
# Cycle 2 is the second of three cycles in v0.2.1. Cycle 3 follows:
#   - Cycle 3: Release-level HARD smoke gate execution against
#              Eric's rd-automation (single fixture per master plan
#              §6.2 ruling 2026-05-05). NO new component code.
# After Cycle 3 seals, the v0.2.1 SHIPPED rollup commit lands on
# canonical, backfilling STATE.md + roadmap §8 + eric-final-delivery
# §2 v0.2.1 row. Tag deferred until Luke gates Eric install.
#
# Commit ladder pre-apply:
#   bbc93a7 — v0.2.0 SHIPPED rollup (predecessor; v0.2.0 release).
#   2ff444e — v0.2.1 master plan + cycle decomposition.
#   2ba7fcd — v0.2.1 master plan patch (single-fixture rd-automation).
#   9355ef2 — v0.2.1 master plan sweep (remaining two-fixture refs).
#   df447bc — FIDRAFT entry: claude-code-pasteable-survey pattern.
#   0bf33f1 — v0.2.1 Cycle 1 source-edit feat (BASELINE for Cycle 1).
#   f6b5047 — v0.2.1 Cycle 1 manifest+apply.
#   55640b1 — v0.2.1 Cycle 1 seal.
#   d7c5b2d — v0.2.1 Cycle 1 §14 backfill.
#   4c4a1d3 — v0.2.1 master plan §9 Cycle 1 SHA backfill (HEAD).
#   <plan-doc commit lands NEXT (THIS plan-doc + manifest), then
#    source-edit feat commit (BASELINE pinned to that SHA), then
#    `loam amend apply <manifest>` lands as single merged
#    manifest+apply commit per AC.DPS1.6 (v3 schema)>
#   <`loam amend seal --plan-doc <abs path> <manifest>` lands as
#    deterministic seal commit per AC.DPS2.{1,4,6}>
#   <post-seal commit backfills this plan-doc's §14 + master plan §9
#    Cycle 2 SHA row per AC.D-sa.7>
#
# Single-component fence — dev-sdlc:
#   - NEW SKILL package under plugins/dev-sdlc/skills/skill-promotion-review/:
#       SKILL.md — frontmatter (description-only, ≤1536 chars, no
#                   `name`); 6-section body (What / When / How /
#                   Graceful degradation / Composition / Out of
#                   scope); ~250-320 lines; covers 3-signal MVP +
#                   10-row decision matrix + 7-step graduation walk
#                   + demotion path + on-demand quarterly trigger.
#   - NEW tests under plugins/dev-sdlc/tests/:
#       test_AC_PROMOTE_1_skill_package.py
#                    — frontmatter validity + 6-section body presence.
#       test_AC_PROMOTE_2_three_signal_mvp.py
#                    — body mentions all 3 primary signals + secondary-
#                      non-blocking framing.
#       test_AC_PROMOTE_3_decision_matrix.py
#                    — body contains markdown table with 10 row-class
#                      labels.
#       test_AC_PROMOTE_4_walk_workspace_logic.py
#                    — body mentions walk-workspace + empty-workspace
#                      handling + scratch-output path.
#       test_AC_PROMOTE_5_owner_ratification.py
#                    — body mentions PM batch API (n=1) + default-to-no
#                      framing.
#       test_AC_PROMOTE_6_author_tests_subflow.py
#                    — body mentions sub-agent dispatch for tests +
#                      structural-test convention.
#       test_AC_PROMOTE_7_amendment_cycle_composition.py
#                    — body mentions `loam-amend-cycle` SKILL + move +
#                      feat + apply + seal sequence.
#       test_AC_PROMOTE_8_remove_workspace_local.py
#                    — body mentions delete + pointer.md replacement +
#                      duplicate-auto-load disclaimer.
#       test_AC_PROMOTE_9_demotion_path.py
#                    — body mentions demote/retire + corrective
#                      amendment + rare/explicit framing.
#       test_AC_PROMOTE_10_quarterly_trigger.py
#                    — body mentions on-demand + 90-day self-discipline
#                      + v0.2.x auto-fire deferral.
#       test_AC_PROMOTE_11_synthetic_fixtures.py
#                    — fixture-validation test for 4 fixtures.
#       test_AC_PROMOTE_12_discoverable.py
#                    — directory-scan asserts SKILL is in
#                      plugins/dev-sdlc/skills/.
#   - NEW fixtures under plugins/dev-sdlc/tests/fixtures/
#     skill-promotion-review/synthetic-skills/:
#       well-formed-harness-general/SKILL.md
#                    — minimal valid SKILL describing universal
#                      harness pattern; ~30-50 lines; expected
#                      Categorization=HARNESS-GENERAL.
#       well-formed-dev-specific/SKILL.md
#                    — minimal valid SKILL mentioning loam-amend /
#                      plan-before-code; ~30-50 lines; expected
#                      Categorization=DEV-SPECIFIC.
#       duplicate-of-existing/SKILL.md
#                    — minimal SKILL with description-keywords
#                      overlapping `loam-amend-cycle`; ~30-50 lines;
#                      expected Conflict=DUPLICATE.
#       quality-fail/SKILL.md
#                    — malformed SKILL (missing frontmatter
#                      description OR missing required body section);
#                      ~10-30 lines; expected Quality=FAIL.
#   - NO edits to existing dev-sdlc surfaces (12 sealed SKILLs +
#     all existing tests + hooks + tools + templates remain
#     unchanged).
#   - NO edits to framework/per-project-pm/ (PM batch API consumed
#     read-only; halt-trigger if edits needed).
#   - NO edits to framework/scope-of-work/ (compose-point READ-ONLY;
#     halt-trigger if non-trivial extension required for quarterly
#     trigger; on-demand fallback is the locked path).
#   - NO edits to plugins/loam-skills/ (universal-tier skills
#     unchanged; SKILL package per dev-scoped three-tier gating).
#
# AC families locked per plan-doc §3:
#   AC.PROMOTE.1  — `skill-promotion-review` SKILL package with
#                    valid SKILL.md (frontmatter ≤1536 char
#                    description; 6-section body per dev-sdlc
#                    convention).
#   AC.PROMOTE.2  — 3-signal MVP body specifies primary gates per
#                    Decision L (Categorization + Quality +
#                    Conflict primary; Reusability + Tests + Usage
#                    secondary non-blocking).
#   AC.PROMOTE.3  — Decision matrix encoded in SKILL body covering
#                    all 10 row classes from layered-skills §4.2.
#   AC.PROMOTE.4  — Walk-workspace logic: read
#                    `<workspace>/.claude/skills/`; per-skill
#                    3-signal evaluation; structured table output
#                    with recommendations; empty-workspace handled.
#   AC.PROMOTE.5  — Owner-ratification via PM batch API
#                    one-at-a-time per Decision Q. Default-to-no
#                    per Decision G; explicit Y to promote.
#   AC.PROMOTE.6  — Author-tests-for-promotions sub-flow per §4.3
#                    step 4: SKILL guides persona to dispatch
#                    sub-agent for AC-shaped test authoring.
#   AC.PROMOTE.7  — Land-in-target via amendment cycle (composes
#                    with `loam-amend-cycle` SKILL): move SKILL.md
#                    → author manifest → `loam amend apply` →
#                    `loam amend seal`.
#   AC.PROMOTE.8  — Remove workspace-local copy post-promotion per
#                    §4.3 step 6: replace with single-line
#                    "moved-to-plugin" pointer.md.
#   AC.PROMOTE.9  — Demotion path per §4.4: SKILL surfaces "skill
#                    X fired N times since promotion;
#                    demote/retire?"; corrective amendment cycle.
#   AC.PROMOTE.10 — Quarterly-review trigger via on-demand
#                    `/skill-promotion-review` (MVP fallback);
#                    90-day cadence as owner-self-discipline;
#                    scope-of-work auto-fire deferred to v0.2.x.
#   AC.PROMOTE.11 — Component-level tests against synthetic
#                    workspace-local SKILL fixtures (4 shapes)
#                    covering signal-evaluation + decision-matrix +
#                    PM ratification flow as referenced from
#                    SKILL body.
#   AC.PROMOTE.12 — Discoverable in canonical pos-v2:
#                    `/skill-promotion-review` invokable; appears
#                    in `/` menu via Anthropic native +
#                    v0.1.7 Cycle 3 layered-skill mechanism.
#
# Smoke (REALISTIC CONDITION — applicable dimensions per
# smoke-test-discipline §6 + plan-doc §4):
#   D1 cold-state — fresh tmp workspace + synthetic-skill fixtures
#     under <workspace>/.claude/skills/; SKILL body covers full
#     7-step walk; structural tests assert body coverage. Verified
#     by test_AC_PROMOTE_1 through test_AC_PROMOTE_12. Live walk
#     verified at master plan Cycle 3 HARD smoke against Eric's
#     rd-automation.
#   D2 steady-state — re-run on workspace where one SKILL graduated
#     previously: SKILL recognises pointer.md replacement (no
#     double-evaluation); skips graduated entries. Verified by
#     test_AC_PROMOTE_8 (pointer.md disclaimer) +
#     test_AC_PROMOTE_4 (walk-workspace logic mentions handling
#     pointer.md as "already-graduated, skip").
#   D5 cross-session — promoted SKILL discoverable via Anthropic
#     filesystem-discovery in next session. Verified by
#     test_AC_PROMOTE_12 (directory-scan); actual cross-session
#     behavior verified during master plan Cycle 3 HARD smoke.
#   D6 telemetry-floor — owner ratification audit-trail: each PM
#     enqueue + record_response writes to per-project-pm audit log
#     per Decision P (existing v0.1.7 Cycle 4 surface; Cycle 2
#     inherits). Verified structurally — Cycle 2 does NOT modify
#     per-project-pm; the existing audit-log shape covers
#     ratification events.
#   D3 restart — mid-review kill -TERM is recoverable from PM's
#     existing decision-queue state (queued-but-unanswered question
#     persists across kill); on restart, persona re-invokes
#     `/skill-promotion-review` and PM surfaces the pending
#     question. Verified structurally (PM batch API behavior).
#   D4 reboot — n/a structurally (stateless SKILL body; PM
#     decision-queue + filesystem state survive reboot trivially).
#   PLUS: full-suite green sweep — pre-Cycle-2 plugins/dev-sdlc/
#     tests at HEAD (4c4a1d3 or successor) all pass post-Cycle-2;
#     halt + surface on any regression.
#
# Bookkeeping note: this is the v0.2.1 second cycle (immediate
# successor to v0.2.1 Cycle 1 sealed at apply f6b5047 / seal
# 55640b1 / §14 backfill d7c5b2d / master plan §9 backfill 4c4a1d3).
# v3 fields exercised: plan_doc_ref + ac_count + smoke_outcome at
# the manifest level; single-component sweep at seal time. Universal-
# paths admit docs/rebuild/plans/ for plan-doc paper trail.
#
# Cycle 2 ships ZERO edits to existing sealed-content (12 dev-sdlc
# SKILLs unchanged; existing tests unchanged; hooks/tools/templates
# unchanged). The sealed-content invariant holds: Cycle 2 only adds
# a NEW SKILL package + NEW structural tests + NEW fixtures under
# plugins/dev-sdlc/'s existing fence.

# BASELINE pinned to the source-edit feat commit. Updated post-
# source-commit by the build agent.
baseline: "<pending>"

plan: docs/rebuild/plans/v0-2-1-cycle-2-promotion-rubric.md

# Top-level plan_doc_ref — schema v3 collapsed-narrative pointer.
# `loam amend seal` synthesizes the narrative body from this ref
# plus ac_count + smoke_outcome below.
plan_doc_ref: docs/rebuild/plans/v0-2-1-cycle-2-promotion-rubric.md

ac_count: 12
smoke_outcome: "D1+D2+D3+D5+D6 exercised; D4 n/a (stateless SKILL body); 4 synthetic-skill fixtures (well-formed-harness-general, well-formed-dev-specific, duplicate-of-existing, quality-fail); full-suite green sweep on plugins/dev-sdlc/"

components:
  - name: dev-sdlc
    seal_test: plugins/dev-sdlc/tests/test_no_sealed_amendments.py
    sidecar: plugins/dev-sdlc/tests/SEAL_COMMIT
    frozen_baseline: false
    extra_allowed_prefixes: []

# Universal admissions — per amendment #22 ruling #3 + Cycle 3 +
# Cycle 4a/4b precedent + v0.1.9 Cycle 1 precedent + v0.2.0 Cycle 1
# precedent + v0.2.1 Cycle 1 precedent.
# `docs/rebuild/plans/` admits the plan-before-code paper trail.
# Cycle 2 has NO docs/getting-started.md or other universal-doc
# edits (the SKILL ships under plugins/dev-sdlc/skills/, fully
# within the sealed component fence; install-doc updates lived in
# Cycle 1).
universal_paths:
  prefixes:
    - docs/rebuild/plans/
  files:
    - CLAUDE.md
    - docs/odd-in-loam.md
    - docs/odd-methodology.md
    - docs/rebuild/STATE.md

narrative:
  target: plugins/dev-sdlc/seals/SEAL_COMMIT.v0-2-1-cycle-2-promotion-rubric
  # body omitted — schema v3 with plan_doc_ref synthesizes a 5-15
  # line summary at seal time per AC.DPS2.{1,4}. The summary
  # surfaces title + slug + components + baseline + amendment SHA +
  # plan_doc_ref + ac_count + smoke_outcome.
