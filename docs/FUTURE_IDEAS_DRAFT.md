# FUTURE_IDEAS_DRAFT.md — no-overhead idea capture

This is a draft surface for *every* improvement idea about pos-v2 — Luke's or the assistant's — captured at point-of-occurrence with no overhead. Sibling to `FUTURE_IDEAS.md` but distinct in lifecycle:

- `FUTURE_IDEAS.md` is **curated**: each idea has a designated number, a rationale section, and a relationship to other ideas.
- `FUTURE_IDEAS_DRAFT.md` is **no-overhead capture**: append a brief bullet with rationale (~3–5 lines) the moment an idea surfaces. No numbering, no curation, no ordering.

**Lifecycle.** During the initial phase of pos-v2 development, entries accumulate. Periodic rigor reviews graduate worthy items to `FUTURE_IDEAS.md`, drop obsolete ones, and combine duplicates. Last review: 2026-04-27.

**Convention.** Agents (background or main-session) **surface to chat** — they do not write to this file directly. The parent (or owner) appends. This avoids file-write races when multiple agents run in parallel, and keeps the parent in the loop on every captured idea.

---

## Workspace-sync follow-on family (post-#56 milestone-test 2026-04-27)

Proposed consolidation: **two bundles** instead of eight follow-ons.

- **Bundle α — workspace-sync resolver cost overhaul.** Combines three attack vectors on the same problem (resolver cost dominates first-sync of long-diverged workspaces). Three internal ACs:
  - **(α.1) NN — content-vs-canonical-history ancestor detection.** If workspace path content matches some ancestor commit reachable from canonical's HEAD, fast-path accept-canonical without resolver call (workspace didn't diverge; it's just behind). Reduces first-sync cost from O(diverged-files × LLM) to O(diverged-files × git-rev-walk). For pos3 specifically: 90%+ of the 46 conflicts observed in the live-test were just-behind-canonical and could resolve without inference.
  - **(α.2) QQ-refined — deterministic-merge-with-LLM-verify-gate; LLM-as-classifier + LLM-as-verifier, never LLM-as-generator.** When merge IS needed, replace LLM-as-generator (full-file output, 60-120s+ for ~100-line files) with: (1) classify (~50 token output) "is this file structurally-mergeable?"; (2) deterministic primitive does the merge (free); (3) verify (~200 token output) "did the merge lose meaning?"; (4) verify-pass → accept; verify-fail → fall back to full-LLM-merger or halt-and-surface. 3-8× faster AND safer (deterministic merge is reproducible + audit-grade; verifier adds safety net). Generalizable meta-pattern — graduated to FUTURE_IDEAS Idea 20.
  - **(α.3) RR — workspace-sync resolver subprocess uses `claude --bare`.** One-line change in `_resolver_client.py`. `--bare` skips hooks, LSP, plugin sync, auto-memory, background prefetches, keychain reads, CLAUDE.md auto-discovery — saves 5-20s cold-start per call AND likely fixes the telegram-MCP disconnect correlation observed during pos-sync runs.
  - **Subsumes OO** (resolver timeout config + 300s default — irrelevant if cost is lower per call).

- **Bundle β — workspace-sync ergonomics.** Closes the "normal person can use pos" gap. Four internal ACs:
  - **(β.1) KK — workspace canonical-source config; `pos-sync` no-args.** Workspace stores canonical source URL/path in `<workspace>/.pos/sync-config.yaml`; `pos-sync` reads it; user runs `pos-sync` from inside workspace with no flags. Operator never has to know the canonical path.
  - **(β.2) LL — `pos new-workspace --from <repo>` bootstrap command.** Solves the chicken-and-egg: under Architecture B a fresh workspace has no framework code yet. `pos new-workspace ~/my-ws --from https://github.com/.../pos-v2` clones canonical, embeds framework via pos-sync, writes the canonical-source config. Composes with workspace-bootstrap's existing first-run plumbing.
  - **(β.3) MM — global install path for `pos` and `pos-sync`.** Today the binaries live only in canonical pos-v2's `.venv` — nothing on PATH for a normal user. Curl-bash installer / pipx / homebrew. Aligns with D-A1 ruling (Architecture A for the CLI binary).
  - **(β.4) PP — `--auto-accept` confidence-floor calibration / partial-accept tier.** Both verdicts in the live-test were 0.88 confidence (below the 0.90 default floor) yet demonstrably sharp. Either lower default to 0.85, or add a "confirm-with-reservations" tier where verdicts in [floor, 0.90) auto-stage and surface a one-line summary for human review before apply. Needs empirical sample to calibrate.

- **`pos-sync --dry-run` UX bug.** Flag spawns real LLM calls. By the name "dry-run," operators expect a free preview; this spends tokens (5-10s wall-clock per Class-C conflict). Rename current behavior to `--preview-verdicts`; make `--dry-run` truly cheap (plan-only, no LLM). Composes with cost-governance.

---

## Tooling improvements (pos-amend / dev infra)

- **Dispatch-template family extension: memory-doc + commit-message templates.** The dispatch template (#25) and plan-doc skeleton (#51) prove the pattern. Memory-doc skeleton (frontmatter + Why + How-to-apply structure) and commit-message templates per category (`feat(<comp>): ... — amendment #N`, `chore(seals): ...`, `docs(plans): record amendment #N commit SHAs`) are natural extensions. Plus `pos-amend commit-msg <category> <vars>` helper.

- **`pos-amend log-decisions <plan> <key>=<value>...`** (stretch). Mechanise §14 method-decision register's deterministic subsections — test counts, dependents-cleared list, file-touched manifests — while leaving D-build.x prose to the builder.

- **`pos-amend apply` regex misses in-function literals.** Doesn't reach `allowed_prefixes` literals declared inside function bodies (vs module-level). Caused #50 to need a corrective commit (`6c90b9c`). Fix: extend regex or switch to AST parse. OR: document the limitation so dispatchers flag it.

- **`pos-amend seal` stash-pop conflict on existing working-tree files.** Stash-and-restore can fail when stash includes a file that already exists in the working tree (collision example: #50's `personas/primary/prompt.md`). Fix: detect collisions and either three-way-merge, abort with structured diagnostic, or use `git stash apply --index` then resolve.

- **Template engine notes.** (a) One-pass `{{var}}` substitution doesn't recursively expand defaults — a default containing `{{OTHER_VAR}}` renders the literal placeholder. Caught early in #25 build. Document in dispatch-template authoring guide. (b) Templates root resolves via `__file__` parents — works for editable installs only. If pos-amend ever ships PyPI/wheel, templates need `package-data` declaration in `pyproject.toml`. Not blocking; noted for eventual packaging story.

- **Template `description` frontmatter doubles as `list` one-liner.** The introspection-surface frontmatter the engine requires gives `pos-amend template list` its descriptions for free. Useful pattern when memory-doc/commit-message families land.

- **`claude --bare` mode for clean experiments / scripted scenarios.** Document the recipe: `claude --bare --settings .claude/settings.json` for pure-harness-free Claude invocations. Useful when the full pos-v2 harness shouldn't load. (Also relevant to RR.)

- **Test conftest collision pattern across `tools/<x>/tests/` dirs.** Adding tests in two `tools/<x>/tests/` directories with the same module names triggers a pytest cross-tree collision. Resolved in #17 by dropping `__init__.py` + using fixtures instead of cross-test imports. Capture in dispatch template's "Cycle mechanics" section or pytest-conventions doc.

---

## Methodology / discipline notes

- **AC text precision sweep.** Opportunistic batch of post-seal AC tightenings (similar to AC37.5, AC40.1) — when an AC pins specific vocabulary that turns out to be method-not-objective, tighten the AC. Could batch across multiple sealed amendments in one doc-only sweep. Borderline cases not tightened in #17's sweep: AC.A1, AC.A6, AC.A7, AC.B5 (specific OTel event names + Literal types — debatable; the names ARE contracts THIS amendment authors); F's AC.F1 (the `always_loaded`/`dev_only` partition keys — borderline, names are now load-bearing). Master plan §6.3 AC3 "additionalContext payload that names the loaded persona" wording is symmetric to AC37.5 tightening. Re-evaluate in a future precision sweep.

- **Integration-test methodology gap on SQLite file inspection.** Bare `stat()` on the main `.sqlite` file mid-WAL can mislead — size doesn't reflect committed data until WAL checkpoints. Future integration-test fixtures should sample sibling `-wal`/`-shm` files AND open-then-close a `sqlite3` connection before size-checking. (Caused #3's 0-byte Finding 2 false alarm.)

- **Master-research recommendations should pre-filter through the scope-fence constraint.** #17 build's D-build.6 had to deviate when research recommended a sealed attach point but the dispatch was dev-discipline (`tools/` fence). Recurring pattern; research dispatches should explicitly filter recommendations through scope-fence at research time.

- **H19 admission-debt pattern (PARTIALLY ACTIONED via dispatch-template at `a17f1f7`).** Amendments that introduce new top-level paths should admit them in their own H19 window, not leave it for the next hands-off-lifecycle amendment. Dispatch template now includes the check; ensure the convention propagates.

- **Cross-mode reference debt: `memory-system/launchd/README.md` references dev-only path.** F's AC.F3 reference scanner found memory-system/launchd/README.md (always-loaded) references `docs/archive/component-research/true-first-run/research.md` (dev-only). Editing the README would breach memory-system's sealed-component fence. F captured as `KNOWN_CROSS_MODE_DEBT` allowlist; allowlist must shrink to empty when fixed. Resolution path: future memory-system amendment scrubs the cross-ref (preferred — minimal partition surface).

- **`source_commit` backfill pass for Phase γ records.** #17 Phase γ projection populated 99 amendment-AC records but left `source_commit` field None. A follow-on backfill pass that walks `git log --grep` for each amendment's seal commit could populate these. Going forward, the live `pos-amend seal` cycle populates `source_commit` automatically (AC.D-mig.4 verified); only historical records need backfill.

- **AC anchor regex is conservative — tightening could yield 100-200 more records.** Phase β/γ extractor catches H2/H3 + bullet-form ACs but misses prose-style criteria. ~17 placeholders out of 25 plans + 21 components. Worth tuning once the projection is in active use and missing records become visibly missed.

- **`feedback_serialize_amendment_builds` may be over-broad.** #20 empirical finding: parallel doc-edit + dev-discipline-build is safe — the rule applies specifically to sealed-component-amendment-build pairs racing on `pos-amend`, not generic git activity. Tighten the memory to clarify scope.

- **Bash-tool eval-wrapper anomalies (consolidated).** Two related quirks: (a) glob expansion fails differently than interactive zsh — `(eval):1: no matches found` errors even with `setopt nomatch` unset; use Read or `find -name` or `bash -c '...'` for glob behaviour. (b) Stderr can be filtered/dropped on some Bash-tool invocations (e.g. pos-amend halts surfaced as silent rc=0 in main session, visible in fresh agent sessions). Workarounds: `2>&1 | tee /tmp/log`. Fix is upstream Claude Code harness concern. Practical persona-level rule: don't form claims about file existence from glob failures, don't form claims about command success from rc=0 alone.

- **`pos-amend seal --plan-doc` crashes on relative-path argument.** `Path.relative_to` raises ValueError when invoked from repo root with relative `--plan-doc` arg (commit `75c4d73` worked around). Fix: normalise to absolute path inside the subcommand.

---

## Structural-enforcement candidates (O program targets)

- **End-of-turn trait-reflection — Stop-hook contributor.** Today the trait-check is content-level (a rule the persona reads from prompt.md). Higher-leverage: a Stop-hook contributor that wraps the persona's reply with a deterministic self-reflection step. Composes with O program; could become an A5 amendment after A1 substrate landed.

- **Self-evolution directive — proactive own-behavior change suggestions.** Same shape: a Stop-hook contributor runs trait-check + improvement-proposal step, surfaces concrete suggested directives to next UPS additionalContext. Active surface (not just passive FUTURE_IDEAS_DRAFT capture).

- **Workspace-level `primary_channel` config slot.** Today the directive "Telegram is workspace default conversation layer" lives in dossier prose — advisory. Structural shape: workspace config (persona contract field `primary_channel`, or `<workspace>/.pos/channel.json`) read at session-start; persona routes default replies based on its value. Multiple workspaces can have different defaults.

- **Dossier (or any persona-maintained note) shouldn't duplicate canonical-truth values.** Caught 2026-04-26: dossier "Landed amendments" carried explicit numbers + commit SHAs; drifted off-by-one because git log was the canonical source. Generalisable rule: for any value canonically elsewhere (git log, plan-doc §14, manifest), reference don't duplicate. Pruning-trait extension. Structural-enforcement candidate: a Stop-hook contributor that derives current values from canonical source and warns on dossier-claims that disagree.

- **Default action verb is "I'm doing X", not "want me to X?"** Captured 2026-04-27. Persona has a recurring pattern of surfacing discretionary-check-in framing ("Want me to patch the plugin?") on work that's clearly within Luke's broad-autonomy grant. Reserve "want me to" for genuinely ambiguous-which-path cases (multiple defensible options where owner taste matters). For uncontroversial in-scope work: announce + execute. Composes with O program.

---

## Cosmetic / specific bugs

- **Stale `error_code` + `remediation` fields in completed `first-run.state`** (gen-2→gen-3 transition; `_advance_state` doesn't clear them on completion).

- **Phase-5 confirmation sentence "twelve components"** — actual install is 13 (telegram-interface auto-discovered). Hardcoded string in `_confirmation_sentence()`.

- **`/effort` confirmation message bug** (upstream Claude Code) — `/effort auto` says "Effort level set to max" but actual state is `xhigh` (correct behaviour). UI-only mismatch. File via `/feedback`.

- **Dynamic `/effort auto`** (upstream feature request) — Luke's intuition that "auto" should scale per task, not statically reset to model default.

- **`personas/primary/` at canonical root cleanup.** Cause identified by #42 build: workspace-bootstrap test invokes `run_first_run_scaffold` without `workspace_root` set; `_resolve_workspace_root` walk-up falls through to canonical repo root. Fix is workspace-bootstrap-test-side: tests should always pass an explicit `workspace_root` (tmp dir).

---

## Reactivation / deferred cross-references

- **G-activation-first dissolves D.** If sub-plan G (shared host-level memory-graphiti instance + workspace-keying via group_id) activates first at multi-workspace reactivation time, sub-plan D (per-workspace memory-graphiti port auto-allocation) becomes moot. Flag at reactivation-time triage so we don't redundantly build D before G.

- **Lazy-projection trigger ↔ amendment #32 session-start gate composition.** When sub-plan #17 (heavy-b-phase-α/β/γ-migration) reactivates as the lazy-projection job triggered by dev_intent=yes, the cheapest available attach point is amendment #32's session-start gate. Method-level note for #17's future builder.

- **C may activate as "audit + cleanup" rather than "migrate" when multi-workspace lands.** D-MASTER.2's owner-revised mirror of `~/.claude/` collapsed C's migration burden; if any of the resolver pattern is partially in place by reactivation time, C's scope shifts from "migrate state files" to "audit existing layout for compliance + close gaps."

---

## Removed (actioned or graduated, kept as audit trail until next review)

- **Dispatch-prompt template family** — landed via #25.
- **Plan-doc skeleton template** — landed via #51.
- **`pos-amend new-plan <slug>` orchestration** — landed via #51.
- **Telegram MCP plugin watchdog refix** — patched 2026-04-27 at `~/.claude/plugins/cache/claude-plugins-official/telegram/0.0.6/server.ts:670`.
- **C closure: Stop-hook learning-extraction = (c) trust graphiti** — task closed; Stream A #48 implements (c).
- **First-run sync of long-diverged workspace burns LLM budget** — graduated as α.1 (NN ancestor detection) above.
- **LLM-as-classifier-or-verifier-not-generator meta-pattern** — graduated to FUTURE_IDEAS Idea 20.
- **SDLC objective-extraction skill for existing repos** — graduated to FUTURE_IDEAS Idea 3 sub-section.

(Audit-trail entries clear at next review — they exist to confirm nothing was lost in the cleanup.)

---

*New entries append to the top of the relevant section.*

- **Apply-step verification REQUIRES file-content-on-disk match, not output-message signal.** Captured 2026-04-27 from a major calibration miss. I declared "milestone closed + live-verified" based on `[workspace-sync] applied: <ref>` output + audit shape + state.yaml status. Subsequent inspection found pos3's framework files were STILL at pre-apply state — Bundle α's NN ancestor-detection had a `resolved_content_path: null` bug that let apply silently no-op. Bundle α's 101 tests verified verdict-shape, NOT on-disk-file-content; the bug shipped. Generalizable rule for apply-style amendments (workspace-sync, anything that mutates workspace files): **the verification-loop MUST include byte-comparison of at least one workspace file's content against canonical's expected blob**. Output messages, audit log, and state files are NECESSARY but NOT SUFFICIENT signals. Structural-enforcement candidate: add a post-apply self-check to pos-sync that byte-verifies a sample of resolved entries; emit warning + halt-and-surface on mismatch. Composes with Idea 20 meta-pattern (verifier role) — apply-step-verifier is the symmetric counterpart to merge-step-verifier (α.2 QQ-refined).

- **`feedback_verify_post_amendment_state` should sharpen to "byte-verify, don't trust output messages".** Captured 2026-04-27. The existing memory says "verify post-amendment state from code, not prior-agent reports" — but I followed it nominally (read agent reports skeptically, ran tests, code-reviewed) yet still trusted output strings (`applied: <ref>`) without actual file-content inspection. The next-level discipline: **for any state-mutating amendment, verification must include a byte-content check on the actual mutation target**. Reading test assertions tells you tests pass; reading file content on disk tells you the mutation actually happened. The latter is the ground truth.

- **Persona "want me to / awaiting your read" pattern needs STRUCTURAL fix, not discipline.** Captured 2026-04-27 from a recurring failure mode: Luke called out the autonomy-violation pattern 4+ times in one session (asking permission on uncontroversial in-scope work despite explicit broad-autonomy directives + reminders). Inference-level discipline keeps failing because the pattern is too easy to slip into. The discipline-only approach has empirically failed. Structural fixes worth pursuing: (a) Stop-hook contributor that scans persona's outbound Telegram/terminal replies for permission-asking patterns ("awaiting", "want me to", "your call", "let me know", "ruling needed", "if you confirm") and either rewrites them to action announcements OR halts with a self-correction step; (b) a deterministic post-processor at Telegram-send time that converts "awaiting your call on X" → "doing X unless you object" for in-scope work (which the persona evaluates against the autonomy directive at send time); (c) a structural pre-send check baked into the reply tool wrapper. Composes with O program (structural-enforcement substrate) + Idea 1 (three-lens enforcement). High-leverage relative to ongoing discipline cost.

- **Structural-enforcement-default rule needs the "relocate vs eliminate" sharper test.** Captured 2026-04-27 from the D vs D′ near-miss. Existing rule says "prefer structural over advisory." Misses the case where both options are nominally structural but only one eliminates the failure class. Sharper test: "Can a future code change re-introduce the same failure class without active discipline? If yes, this is rule-shaped despite using a structural mechanism." Applied to D vs D′: D′'s .gitignore patterns relocate the "developer forgot" failure to .gitignore-pattern-management; D's directory split eliminates it (different-directory = can't accidentally touch). Already promoted into dossier rule 4 inline; consider also landing in canonical's `docs/odd-methodology.md` as a methodology refinement.

- **First-principles-trigger Stop-hook contributor.** Captured 2026-04-27 — Luke confirmed (A) methodology rule is landing now (in odd-methodology.md §4.6 via corpus-edit dispatch); option (B) deferred for future evaluation. Structural-enforcement complement to the (A) methodology rule: a Stop-hook contributor that scans recent amendment history (e.g., last 14 days of git log) + the pending dispatch list for the trigger patterns named in §4.6 (N≥2 hotfixes on same component, same-root-cause across ≥2 paths in one cycle, test-discipline failure markers, operator-confusion events, estimate inflation). When a trigger pattern matches, the contributor emits a soft-warning to next session-start additionalContext naming the trigger + suggesting first-principles review before next dispatch. The persona reads + decides whether the review is needed. **Difference from Idea 21:** Idea 21 is the autonomy-pattern stop-hook (rewrites permission-asking in outbound replies); this is the architecture-decay stop-hook (warns on dispatch shape). Both compose with the O program's structural-enforcement substrate. Activation gate: A1 substrate landed + the §4.6 methodology rule has been in use for some weeks (so we know which triggers actually fire). Luke noted he's "not sure if he wants to take it up" — left as draft capture for later evaluation.

- **`KNOWN_CROSS_MODE_DEBT` allowlist drift in loam-mode partition-references audit.** Captured 2026-05-04 during v0.1.8 Cycle 5 release-level smoke. `plugins/dev-sdlc/tools/loam-mode/tests/test_partition_references.py::test_AC_F3_always_loaded_no_dev_refs` fails because the `KNOWN_CROSS_MODE_DEBT` allowlist in the test contains entries no longer present in the source files: `('framework/primary-persona/templates/persona-template/prompt.md', 'docs/FUTURE_IDEAS_DRAFT.md')` + 3× `('framework/workspace-sync/README.md', ...)` entries. Pre-existing failure at HEAD c648cf9 (Cycle 4b seal); NOT caused by Cycle 5. The test's design is correct (allowlist must SHRINK over time as debt is paid down); the failure is "good news" (debt was paid down without updating the allowlist). **Source:** Cycle 5 release-level smoke pass. **Provenance:** v0.1.8 Cycle 5 full-suite pytest sweep surfaced this as the only post-cycle failure. **Recommended next step:** `graduate` for v0.1.9 — small (≤5-line) test edit removing the stale entries from KNOWN_CROSS_MODE_DEBT. Composes with the existing "Cross-mode reference debt" entry above. **Cost-of-being-wrong:** zero (does not affect runtime; only affects the partition-audit lint surface).

- **Pos3-update first thing post-Eric ship.** Captured 2026-05-04 by main session per Luke directive ("after Eric ship tonight, first thing after... we get pos3 up to date. Some of this stuff sounds great. I want it too"). Once v0.2.1 ships and the Eric-deliverable is in Eric's hands, the FIRST follow-on work is bringing Luke's pos3 workspace up to current canonical (v0.2.1 state). New capabilities Luke gets when pos3 syncs forward: (1) per-project PM holding pos3-project state across sessions; (2) continuous codebase-watch flagging drift between sessions; (3) auto-skill-capture for the repeat patterns Luke does (3 of 6 triggers shipped at v0.2.0 MVP; 3 more deferred to v0.2.x); (4) production-safety profile (Luke would set pos3's to `dev` since pos3 IS loam's dev environment, not a production-stake context); (5) PR-safety gate + hook installers + CI templates if Luke wants automated pre-commit checks; (6) the codebase-reader pointed at pos3 itself — generates a written ODD-shaped contract for what loam is supposed to do (meta dogfood; flagged as fun first run). **Composes with:** existing Task #22 "Apply FBE.7 to pos3" (older umbrella; subsumes into this new umbrella); the pos3-sync resolver-factory bug (FIDRAFT entry); workspace-bootstrap framework-only-→-main switch already landed at amendment #132 so the sync mechanism itself is current. **Status:** queued as the first post-Eric work item; not on Eric path; activates post-v0.2.1 ship.

- **Lint pass post-Eric ship + automated lint integration into dev-sdlc plugin.** Captured 2026-05-04 by main session per Luke directive ("After the Eric release I want to do a lint pass. I want good clean python code. I also want to have this done automatically as part of SDLC plugin long term"). Two items: (1) **Immediate post-Eric (post-v0.2.1)** — manual lint sweep over framework + plugins Python source. Tools: ruff (style + import-order + unused-vars + complexity), mypy (type-checking), pytest --cov for coverage measurement. Build cycles have been writing fast; expect drift. Goal: clean readable type-checked Python at v0.2.1 ship and before any v0.3.x work starts. Surface findings + fix in a sealed amendment cycle (or split per-component if scope demands). (2) **Long-term — automated lint integration into dev-sdlc plugin's PR-safety pipeline (v0.1.9 Cycle 2)**. The PR safety gate already runs on every commit via pre-commit + pre-push hooks + 3 CI templates. Adding lint to that pipeline makes it automatic: pre-commit fires lint + gate together; CI templates run both. Composes with the production-stake profile from v0.1.6 (lint failures gate strictly under production-stake; warn-only under dev/research). Probably a v0.3.x or v0.4.x release item depending on prioritization vs other v0.3 items. **Status:** captured for post-Eric scheduling; not in current Eric pipeline (v0.1.6 → v0.2.1).


- **Claude-Code-pasteable survey as pre-install context-gathering pattern.** Captured 2026-05-05. For new-user installs of loam where the maintainer has limited synchronous time with the user, a Claude-Code-pasteable prompt that walks the user through 12 onboarding questions one-at-a-time and writes a structured markdown file the user sends back is a zero-friction async alternative to a live call. First instance: Eric onboarding (v0.2.1) — survey at `<pos3>/workspace/.scratch/claude-output/eric-onboarding-prompt-2026-05-05.md` with 8 required + 4 optional questions covering stack versions / Playwright shape / app purpose / production-stake / SOC-2 controls / authority bounds / channel / codebase shape / existing tooling / off-limits zones / first task / pain points. Companion live-walk-mode question list at `eric-onboarding-questions-call-mode-2026-05-05.md` for the case where a call materializes after all. **Generalisable shape:** prompt opens with 2-sentence greeting + one-at-a-time pacing + skippable optional questions + writes `~/loam-onboarding-<user>-<date>.md` at end + ends cleanly with file-path + send-back instruction. Pattern composes with v0.2.1 Cycle 1 install-time onboarding ritual: install-time can read the survey output as default-source if present, so the user confirms-or-overrides rather than re-answering. **Activation:** v0.2.1 Cycle 1 plan-author wires this in via a new AC. **Future use cases:** every new-user install where pre-install context-gathering matters more than first-impression UX speed (production-stake users; users with non-trivial compliance / authority / off-limits constraints).

- **`loam-amend-cycle` SKILL doc drift on `apply --plan-doc` flag.** Captured 2026-05-05 by v0.2.1 Cycle 2 build agent. SKILL body at `plugins/dev-sdlc/skills/loam-amend-cycle/SKILL.md` instructs users to invoke `loam amend apply --plan-doc <path>`; actual CLI takes positional `<manifest_path>` only (`--plan-doc` is seal-time only). Not blocking — the SKILL still works if user passes the positional manifest. Doc-only drift. Future correction: SKILL body sweep against actual CLI surface; lint-pass candidate. Composes with the post-Eric lint-pass FIDRAFT entry above (same kind of doc/code drift the lint-pass would catch).

- **Synthesis layer above structural ACs — "what should I build next?" capability.** Captured 2026-05-05 by Luke directive after Eric-survey + first-extract-on-rd-automation review. Current odd-extractor (v0.1.8) produces symbol-level structural ACs (Express routes, page objects, tests, inferred auth) banded VERIFIED/PLAUSIBLE/HYPOTHESISED. Useful for change-detection (PR-safety) + scope inventory; NOT useful for high-level "what does this app do / what constraints does it have / what should I build next." Luke's framing: "the goal is to have something where a loam user could plausibly say 'ok, what should I build next?' and based on what it has discovered, have a good suggestion on what to build." This is a synthesis layer that consumes (current per-AC extraction + repo docs + survey response + commit history) and produces (domain summary / constraint inventory / capability inventory / gap inventory / build-next recommendations). Architecturally it's a new layer above the v0.1.8 extractor — design space includes: (a) single LLM synthesis pass over ACs+docs+survey producing one narrative doc; (b) multi-pass clustering+inference (Layer 0 = symbols; Layer 1 = capability groupings; Layer 2 = behavioral inference; Layer 3 = gap detection); (c) conversational on-demand synthesis (persona reads codebase live when user asks "what to build"); (d) hybrid (pre-computed synthesis doc, persona references it conversationally). Eric's rd-automation extract demonstrated the gap: 119 PLAUSIBLE Express routes don't tell Eric what to refactor or where the priorities are. **Status:** TOP PRIORITY per Luke 2026-05-05 directive. Research/plan dispatch in flight. Activates as v0.2.2 release; Eric re-ship occurs once shipped.

- **SKILL invocation telemetry — measure whether shipped SKILLs actually change behavior.** Captured 2026-05-05 after Luke asked "is there a way to evaluate how often we're using the skills that have been generated?" Diagnosis: existing telemetry can't tell. Skill-tool-invocation count across all Claude Code projects on this machine = 4 total (`update-config`, `cost`×2, `session-wrap` — none of our 22 SKILLs). Our SKILLs are implicit-invocation (description-only frontmatter; Claude reads description from skill listing + applies body when relevant; no discrete tool call). Commit + doc-reference proxy is self-referential (counts include creation commits + dispatch briefs that name SKILLs as compose-points; doesn't measure behavioral application). **Three telemetry options:** (a) **Claude Code-side logging** of "SKILL body loaded into context" events — Anthropic-side feature, not our control. (b) **SKILL self-instrumentation** — each SKILL body, when applied, writes an audit-log entry. We control this. v0.2.0 Cycle 2's 6-event audit-log floor pattern extends naturally per-SKILL. ~1-2h work; cheapest path. (c) **Behavioral fingerprinting** — for each SKILL, define an observable artefact pattern (e.g., `loam-amend-cycle` produces a 6-commit ladder of specific shape). Mine git history for matches. More robust; harder to build. **Recommended path:** option (b) — bake `loam audit-log emit --kind skill_invoked --skill <name>` into every SKILL body. Future invocations get logged; can aggregate. Won't give retroactive data. **Activation:** post-v0.2.5 Eric ship; not blocking the ODD-rebuild path. Composes with v0.2.0 audit-log substrate + v0.2.1 promotion-rubric (telemetry data feeds promotion-rubric's Usage signal, currently layered-but-not-blocking).

## v0.2.5 yellow findings (2026-05-05 HARD smoke against rd-automation)

- **F3 — odd-extractor analyze step has no `framework/` skip.** Captured 2026-05-05 from v0.2.5 HARD smoke against rd-automation. Severity: latent gap; not load-bearing in the smoke run. The v0.2.1 corrective F2 added `framework/` skip to `framework/workspace-bootstrap/.../language_detection.py` (the `loam init` codepath). The odd-extractor's `analyze.py:_walk_repo` has its own skip-list (`_SKIP_DIR_NAMES` — `.git`, `.loam`, `.scratch`, `.venv`, `node_modules`, etc.) that does NOT include `framework/`. If `loam odd-extract` is pointed at a loam-tree (e.g., for self-extraction), it walks into `framework/` and treats files there as candidates. In the v0.2.5 smoke run the loam-tree extraction ended up with `slices: []` because no adapter's `supports()` claimed the root (no top-level Gemfile / package.json), so it didn't actually fire — but if loam ever has a top-level package.json (e.g., for a TS surface), the framework/ TS files in fixtures + venv code would leak into evidence-rows. **Suggested fix-direction:** mirror the v0.2.1 corrective F2 fix into `analyze._SKIP_DIR_NAMES` (or a more general "skip loam-internals" predicate). Cross-component-skip discipline. **Activation:** post-Eric ship; not blocking v0.2.5 since the smoke didn't hit it operationally. Composes with v0.2.1 corrective F2 (mirrors the same skip rule across the second loam codepath that walks repos).

- **F4 — v0.2.1 corrective F1 seal-text doc-drift post v0.2.3 Cycle 3.** Captured 2026-05-05 from v0.2.5 HARD smoke against rd-automation. Severity: documentation drift; no functional bug. The v0.2.1 corrective F1 seal commit message states it "writes `acs:` + `unhandled_paths:` into `contract-draft.yaml` so pr-safety gate can read full banded contract." But v0.2.3 Cycle 3 (master plan §6.2) RETIRED the legacy `acs:` field. Verified empirically during the v0.2.5 HARD smoke: the post-Stage-1 `contract-draft.yaml` carries `schema_version: 2` and lacks both `acs` and `unhandled_paths` keys. PR-safety now reads `objectives.yaml` + `backing-map.yaml` directly per AC.PRGATE.1. **No fix needed** — the F1 ground-truth IS that PR-safety reads the new sources. The drift is in the seal-commit message + the v0.2.1 corrective F1 plan-doc's framing (which talks about the retired-by-v0.2.3 field). **Suggested fix-direction:** doc-only sweep — either (a) add a "superseded by v0.2.3 Cycle 3" addendum to the v0.2.1 corrective F1 plan-doc, or (b) leave the historical record intact and rely on the v0.2.3 Cycle 3 sub-plan's authority. Optionally update any v0.2.5-era plan-doc verification spec which still references "PR-safety gate reads contract-draft.yaml fields" — that framing is outdated. **Activation:** opportunistic; not blocking. Composes with the post-Eric lint pass (FIDRAFT entry above) which will sweep code/doc drift more broadly.

- **F8 (BLOCKER) — synthesis LLM returns VERIFIED-banded objectives violating the two-source rule.** Captured 2026-05-05 from v0.2.5 corrective C3's outcome-altitude AC test running against the live API. Severity: BLOCKER for v0.2.5 HARD smoke green re-run. Symptom: `loam odd-extract <jsts-playwright-app> --live` (real anthropic SDK + real API, no monkeypatch, no pre-arrangement) fails at synthesis-validation with: `synthesis: objectives[2] ValidationError: 1 validation error for Objective; Value error, Objective: VERIFIED band requires the two-source rule — at least one of evidence.readme_excerpts or evidence.design_doc_refs in addition to tests`. The Pydantic validator in `spec.py:296-307` is correct and by-design (per v0.2.3 Cycle 1 sub-plan-doc §3 AC.OBJX.5: "malformed LLM rows are surfaced with the offending dict for triage; the response is not silently discarded"). The problem is that the LLM prompt does NOT reliably produce VERIFIED-banded objectives that satisfy the two-source rule on real-world fixtures. Three possible fix-directions: (a) prompt-engineering — strengthen the synthesis system prompt to enforce VERIFIED-band evidence requirements before returning that band; (b) auto-demote — if a VERIFIED-banded row from the LLM fails the two-source rule, downgrade to PLAUSIBLE rather than raising StageError (preserves output, weakens band fidelity); (c) per-row drop-and-continue — log the malformed row + drop it + continue (similar to AC.OBJX.5 but inverted from "raise" to "log"). Each has tradeoffs; needs design ruling. **Discovered by:** the C3.3 outcome-altitude test — exactly the procedural prevention the new SKILL was authored for. STUB-class C1 test (which monkeypatches the SDK with hand-crafted compliant payloads) cannot catch this. **Activation:** required before v0.2.5 HARD smoke can re-run GREEN; halts publish dance; halts Eric ship. **Composes with:** the new `odd-test-altitude-discipline` SKILL (worked example of OUTCOME-class test catching what STUB-class missed); v0.2.3 Cycle 1 AC.OBJX.5 (the "raise on malformed" semantics that this finding stresses); ODD §2.5 (every line of code maps to an AC — the validator is mapped; the LLM-prompt-quality side is the gap). **Suggested next dispatch:** v0.2.5 corrective C4 — synthesis-prompt hardening + auto-demote-or-drop policy ruling (one-question-at-a-time dispatcher decision: which fix-direction).

## v0.3.x+ candidates from 2026-05-08 session arc

- **Ahmad / checkmate Web as smoke target.** Captured 2026-05-08. Real Rails massive repo + real user available — natural smoke target for the Ruby/Rails reverse-ODD adapter at v0.4.x or v0.5.x scale. Parallels the rd-automation v0.2.5 smoke for JS/TS but at much larger surface and with a real user attached (so feedback loops on extraction quality + downstream code-gen quality come back faster than synthetic fixtures). **Composes with:** v0.4.0 code-gen-from-objectives (the next layer above extraction); v0.5.0 Variant B (binary-feeder mode if checkmate has compiled artefacts in addition to source). **Activation:** owner-gated; ask Ahmad before scoping. **Risk:** real user with real maintenance burden — apply the production-stake safety profile from v0.1.6 throughout. **Status:** capture-only; not promoted to roadmap. Activation gate: Luke confirms Ahmad-availability + scope.

- **Swarming × extraction composition — extracted objectives are natural partitions for swarm dispatch.** Captured 2026-05-08. Lens 5 (swarming, recursive task decomposition) ties tightly to reverse-ODD: an extracted objective set is a natural decomposition of a codebase into independently-actionable subtasks each with its own tighter acceptance criterion. The composition opens a new capability shape: rather than dispatching code-gen as one monolithic agent against a whole objectives.yaml, the swarm-planner decomposes the objectives.yaml into per-objective dispatches with EVAL_DIMENSIONS judging on each. **Composes with:** Lens 5 reference patterns (PlannerWorkerSwarm cycle, ModelOutput.rationale, EVAL_DIMENSIONS); v0.4.0 code-gen-from-objectives; the leverage discipline's question-1 sharpened rule (the swarm composition delivers user-visible improvement: "loam ships working code that maps every line to a named AC at scale"). **Status:** separate research dispatch in flight; this is the FIDRAFT capture for the composition pattern itself. Activation gate: research dispatch returns + a v0.4.x or v0.5.x candidate AC names the composition explicitly.

- **Tiered soft limits — replace hard 200-objective cap with generate-many-rank-by-impact-per-tier, surface critical-over-limit for user decision.** Captured 2026-05-08. Current odd-extractor caps objectives at 200/run as a hard limit (avoids runaway LLM cost on large repos). Hard cap is wrong altitude when impact is highly skewed: a 50K-objective codebase has 200 high-impact objectives + 49,800 long-tail; capping arbitrarily at 200 may miss the high-impact set if the LLM's enumeration order doesn't surface them first. Better shape: generate N (say 1000) candidate objectives, rank by impact-per-tier (CRITICAL / HIGH / MEDIUM / LOW), surface tier counts to user, let user decide whether to keep the auto-cap (top-200 across tiers) OR raise it for a specific tier (e.g., "show me all CRITICAL even if it's 350"). **Composes with:** the synthesis layer (v0.2.5 — already produces tier-shaped output); the gap-inventory + build-next pipeline (v0.2.4) which currently consumes whatever the cap produced. **Status:** capture-only. Activation gate: a real codebase exceeds 200 objectives and the cap demonstrably truncates the high-impact set.

- **Codebase-split recommendation — post-hoc analysis: if too many objectives for one repo, suggest decomposition into multiple codebases with dependencies.** Captured 2026-05-08. Symmetric counterpart to tiered soft limits: when extraction produces "too many" objectives (the threshold itself a calibration question), the answer may not be to cap or rank — it may be that the codebase is structurally too-big-for-one-repo and should split. Loam already has the data to make the recommendation: objective clusters from synthesis layer + capability/dependency edges. New capability shape: `loam recommend-codebase-split` produces a proposed decomposition (objective groupings + named dependencies between proposed sub-codebases + migration sketch). Composes naturally with the multi-workspace umbrella deferred under Idea 13 sub-plan C (a recommended split would presumably populate multi-workspace state). **Status:** capture-only. Activation gate: a real codebase reaches the "too many" threshold AND the user surfaces a need (rather than loam volunteering it unprompted). **Composes with:** Idea 13 multi-workspace sub-plan C; v0.2.4 gap-inventory/build-next; the synthesis layer.

- **Local-model-on-16GB-M1 viability tracking.** Captured 2026-05-08. The subscription-only-via-`claude -p` architecture is non-negotiable per the architectural commitments (`docs/leverage-discipline.md` §1, `docs/release-roadmap.md` §1). The ONE escape hatch named for relaxing the constraint: if local-model performance on commodity hardware (16GB M1-class Mac, the lower bound of what loam users would have) reaches a threshold where local inference is competitively useful for the loam workflow, the architecture revisits. Track quarterly at the strategic review (per `docs/leverage-discipline.md` §4.3): what's the best open-weight model that fits 16GB inference, what does it score against loam's workflow tasks, has the gap to Claude-Max-via-`claude -p` closed enough to matter? **Composes with:** the architectural-constraint-defending tension named at `docs/leverage-discipline.md` §9.2; the quarterly strategic review cadence. **Status:** quarterly tracker, not a roadmap item. Output is a one-paragraph note appended to the quarterly review. Activation gate: the gap closes meaningfully — at which point the methodology-amendment process (`docs/leverage-discipline.md` §3 decision 7) considers a formal architectural revisit.

- **Auto-update cadence (workspace-sync scheduling) — Claude-Code-style transparent background sync; honor in-flight session safety.** Captured 2026-05-08. Today workspace-sync runs by user invocation (`pos-sync` from inside workspace). Claude Code itself updates transparently in the background — users notice only that they have the latest. Workspace-sync could ship a similar cadence: daemon (or LaunchAgent / cron equivalent) checks canonical for new framework versions on a schedule, fetches + previews verdicts, surfaces a "ready to apply" banner at next session start. Critical safety constraint: an in-flight session must NEVER be interrupted by sync apply (mid-build agents would wedge). The daemon stages but does not apply during active sessions; apply happens at session-start. **Composes with:** Bundle β (workspace-sync ergonomics) — auto-sync is the next layer above β.1 canonical-source config + β.2 bootstrap + β.3 global install; the workspace-sync resolver cost overhaul (Bundle α) — auto-sync amplifies the cost-of-resolver-call problem so Bundle α is a prerequisite. **Status:** capture-only. Activation gate: Bundle α + β land + a real user surfaces the "I forget to run pos-sync" friction.

- **Semantic-merge for non-dev users — line-diff merge is wrong altitude.** Captured 2026-05-08. The current workspace-sync resolver does line-diff-style merging (deterministic primitive + LLM-as-classifier+verifier per Bundle α.2). For dev users this is the right altitude — they reason about line-diffs naturally. For non-dev users (the v0.6.0 audience) line-diffs are the wrong altitude entirely: a non-dev user can't reason about whether a line-diff merge "lost meaning" because they don't read the lines. The correct altitude for non-dev users is semantic-merge: an LLM-pass that extracts the objectives + capabilities + constraints from BOTH the user's customizations AND the incoming framework version, checks logical/functional consistency between them, and surfaces conflicts at the OBJECTIVE level ("your customization adds objective X; the new framework version also adds objective Y; X and Y contradict on constraint Z") rather than the line level. Composes naturally with reverse-ODD: the same pipeline that extracts objectives from a codebase can extract objectives from BOTH sides of a merge and reason about consistency. **Composes with:** v0.6.0 non-tech-user surface (where this becomes load-bearing); reverse-ODD pipeline (v0.1.8+); Idea 20 LLM-as-classifier+verifier meta-pattern (the verifier here checks objective-level consistency, not line-level). **Status:** capture-only. Activation gate: v0.6.0 plan time — when the non-tech-user surface is being shaped, the line-diff merge is one of the surfaces that needs altitude review. **Risk:** semantic-merge cost will be substantially higher per call than line-diff merge; production-stake calibration needed.

- **odd-extract chain dropoff — `--interview` / `--gaps` / `--build-next` invisible to users after default extraction.** Captured 2026-05-08 from Eric user-feedback (Telegram 10375): "stopped after generating the objectives." Cold-run reproduction on rd-automation: success-path stdout terminates with "Contract draft / Sidecar / AC count / Unhandled" and exits — no next-step pointer. Three compounding gaps: (1) extract command's success-path stdout never names `--interview` (the next step), (2) user-facing docs (README, getting-started, dev-mode-getting-started) don't document the chain at all (lives only in cycle plan-docs + verbose `--help` text), (3) v0.2.4 §6.3 ruling made the persona-pull contract documentation-only with no plumbing (SKILL contribution / Stop-hook nudge / greeting-template hook) — Eric's session is empirical disproof of that bet. **Recommendation:** v0.3.0.1 PATCH — Path A (stdout next-step hint at end of every chain stage; ~30-45 min) + Path B (document chain in `getting-started.md` + onboarding ritual Q4 follow-on; ~45-60 min). Path C (SKILL.md for persona-pull; revisits §6.3 ruling) deferred to v0.4.0 plan-time pending post-A+B observation. **Side findings surfaced:** (a) `loam project new` / `loam project advance` referenced in `start-project.md` SKILL but not registered as top-level verbs (separate honesty gap); (b) default-dry-run produces `AC count: 0` with no warning message explaining `--live` exists (UX-cleanup candidate); (c) v0.3.0 feature-honesty audit framework's "100% match" verdict is honest under claim-vs-reality scope but missed design-intent-vs-user-outcome gap (audit-framework-altitude gap to close at v0.4.0 plan-time per `feedback_test_outcome_altitude_required`). **Composes with:** v0.3.0 feature-honesty audit (this is exactly the kind of finding the audit was designed for, missed by altitude); v0.4.0 code-gen-from-objectives (chain becomes 5-step at v0.4.0; persona-pull revisit naturally lands then); `feedback_locked_design_not_license` (v0.2.4 §6.3 docs-only ruling is revisitable now that outcome data exists); `feedback_test_outcome_altitude_required` (Eric's session is the outcome-altitude probe the audit lacked). **Status:** v0.3.0.1 PATCH candidate; investigation report at `<workspace>/.scratch/claude-output/loam-extract-dropoff-investigation.md`. Activation gate: Luke ratifies the version-fit recommendation (v0.3.0.1 PATCH) + the bundle (A + B; C deferred).

- **HeavySwarm 4-role decomposition pattern (post-v0.1.0).** Captured 2026-05-02 by swarms research agent (kyegomez/swarms deep-dive at `workspace/.scratch/claude-output/swarms-research-deep-dive-2026-05-02.md`). Pattern: high-uncertainty research tasks decompose into 4 parallel role-agents (Research / Analysis / Alternatives / Verification) → Synthesis aggregator. Direct prior art for the Swarming principle's high-uncertainty-task pattern. Different shape from PlannerWorkerSwarm (which fits well-bounded execution); HeavySwarm fits the "we don't yet know what shape the answer is" case. **Composes with:** Swarming principle (named decomposition recipe for the high-uncertainty bucket); Scope↔Confidence (HeavySwarm IS a low-confidence-of-exact-outcome → broad-scope-per-subagent shape). v0.1.x amendment after Swarming principle codifies in v0.1.0 foundation work.

- **LLMCouncil with anonymized peer ranking + Chairman synthesis (post-v0.1.0).** Captured 2026-05-02 by swarms research agent. Pattern (Karpathy-inspired per the swarms repo): N agents independently produce candidate answers; each agent ranks the OTHERS' answers anonymized (own-answer hidden); a Chairman agent synthesises the ranked field into the final output. Reduces single-model bias + groupthink in judgment tasks. **Use cases for loam:** A/B-eval judge bias mitigation (a council instead of a single judge); ODD §2.5 violation triage when context is ambiguous; principle-conflict resolution (M5) when scope-confidence and other signals disagree. **Composes with:** A/B-eval pattern (council-as-judge replaces single-judge for high-stakes evals); M5 conflict resolution (council-as-arbiter as a fallback when context-signals don't converge). v0.1.x amendment after A/B-eval pattern lands in F1b.

- **SequentialWorkflow drift_detection semantic-alignment scoring (post-v0.1.0).** Captured 2026-05-02 by swarms research agent. Pattern: in long-horizon multi-step workflows, a judge agent computes semantic drift between original goal and current intermediate output; if drift exceeds a threshold, surface a warning. Direct application to loam: long-running amendment cycles + multi-stage swarms (Stream A + Stream B parallel work) where the work might wander from the original objective over time. **Composes with:** Ruthless Feedback (drift IS a form of compromise-without-approved-scope-reduction); ODD methodology (the runtime-delivery probe could include a drift check); Swarming (intermediate aggregator stages include drift-check). v0.1.x amendment after Ruthless Feedback codification in F1a.

- **MessageTransforms middle-out compression with metrics (post-v0.1.0).** Captured 2026-05-02 by swarms research agent. Pattern: when agent context approaches limits, middle-out compression (preserve start + end of conversation; summarise middle) with explicit TransformResult metrics (tokens before/after, compression ratio, what got summarised). loam currently uses Claude's built-in /compact; this pattern is finer-grained and observable. **Composes with:** dispatch-template authoring discipline (large-context dispatches benefit); Scope↔Confidence (compression IS scope-narrowing under context-pressure); cost-governance + caching (compressed prefixes interact with prompt caching — careful design needed). Lens 1 question: is this already in Claude Code's compaction pipeline, or do we need to author? Research first. v0.1.x or later depending on Lens 1 finding.

- **Per-run autosave directory layout (post-v0.1.0).** Captured 2026-05-02 by swarms research agent. Pattern: every agent run writes a triplet `{workspace_dir}/swarms/{ClassName}/{name}-{ts}/{config.json, state.json, metadata.json}` capturing the run's input config, output state, and metadata. Auditability primitive — replay/audit any past run from disk. (This was the actual "reproducibility" mechanism in swarms — survey caveat that the repo doesn't have RNG seeding / VCR / cassettes; what it HAS is auditability via this layout.) **Composes with:** observability-aggregator (canonical home for structured run records); M-FBM (per-turn markdown layout is a parallel pattern — both are "structured records on disk per atomic operation"); ModelOutput.rationale field (the swarms model-selection-audit primitive — auditability complement). v0.1.x; potentially fold into observability-aggregator's surface widening rather than a standalone amendment.

- **Pre-dispatch WD-discipline structural enforcement — pattern-match brief contents for literal `cd <abs path> && pwd` first action.** Captured 2026-05-09 from the pos3-framework dirty-state reconciliation discovery: the wrong-tree-write pattern (work authored into pos3/framework/ instead of into canonical pos-v2) recurred 6+ times across the May 2 → May 9 window despite `feedback_always_specify_wd_in_dispatches.md` and `feedback_dispatch_cd_literal_first_action.md` being in the memory corpus. Advisory rules in feedback files are demonstrably not load-bearing for this failure class — the discipline relies on the dispatcher remembering at brief-authoring time, and the dispatcher does not always remember. **Recommended structural shape:** a Claude Code PreToolUse hook (or a wrapper around Agent/TaskCreate dispatch) that pattern-matches the prompt body for: (a) literal `cd /abs/path && pwd` as the first action instruction, (b) absence triggers a halt with a one-line corrective prompt to the dispatcher. Implementation lives in framework/ as a default-on hook; can be opt-out via settings.json for users who don't run multi-tree workspaces. **Composes with:** `feedback_always_specify_wd_in_dispatches` + `feedback_dispatch_cd_literal_first_action` (this entry's structural form supersedes the advisory shape per `Structural enforcement default` Lens); ODD §2.5 (the hook IS the structural check that catches the violation class); v0.6.0 non-tech-user surface (multi-tree workspaces will be more common when v0.6.0 ships, raising the cost of recurrence). **Status:** capture-only. Activation gate: next reconciliation triggered by the wrong-tree-write pattern OR a v0.5.x window with hook-system surface widening already planned.

- **Rebrand-residue sweep — `pos_*` / "pos v2 *" → `loam_*` / "loam *" across canonical.** Captured 2026-05-09 after Telegram 10516 owner observation: the `pos_session_start.py` hook still emitted "pos v2 ready" + "pos v2 session-start" diagnostics post-rename-to-loam (M1c label rebrand was 2026-04-29 via `loam-migrate-launchd-labels` migration). Cosmetic strings fixed in same session via small docs commit (5 occurrences in `framework/orchestrator/scripts/pos_session_start.py`). **Broader scope NOT in this fix:** ~32 files in canonical reference `pos_session_start` / `pos_orchestrator` / "pos v2 *" patterns — script filename itself (`pos_session_start.py` not `loam_session_start.py`), test fixture names, `.claude/settings.json` template references, README + plan-doc historical references. **Sweep shape:** rename script files (with care — test files import them by filename), update all references via grep + structural edit, regenerate workspace `.claude/settings.json` template, sync rule. AC families: AC.REBRAND.1 (script-file renames), AC.REBRAND.2 (reference updates), AC.REBRAND.3 (settings.json template + sync regeneration), AC.REBRAND.4 (idempotent re-run safety + back-compat for older workspace settings.json). **Composes with:** existing `loam-migrate-launchd-labels` (sibling structural migration; rebrand-residue sweep would extend to script-name rebrand); `loam-migrate-host-config` (~/.pos → ~/.loam already shipped); v0.7.0 META-FRAMEWORK (substrate work would touch the same hook surface; opportunity to bundle). **AI-time band:** 1-2 hr build (rename + reference sweep + settings.json template + back-compat handling) + ~30 min HARD smoke against the rebrand. **Status:** v0.5.0 PATCH candidate OR v0.5.0 plan-time bundle. Activation gate: owner ratifies version-fit (PATCH soonest vs bundle with v0.5.0).

## v0.4.3 deferred items (2026-05-09 — file-based memory retrieval BM25-bypass + grep-length-bias closure)

- **Stopword list expansion beyond the in-tree minimal set.** Captured 2026-05-09 from v0.4.3 patch §6 deferred items (per `feedback_durable_capture_for_planned_work`). Current `_FTS_STOPWORDS` in `framework/primary-persona/src/loam/primary_persona/file_memory.py` is 20 entries (English-question-shaped — `what / how / the / was / did / does / is / are / a / an / of / to / in / on / for / at / by / be / this / that / it`). Excludes high-signal loam-corpus terms (`loam`, `pos`, `claude`, `eric`, version strings, AC IDs, etc.) deliberately. Larger stoplist (full NLTK English ~180 entries; or curated harness-aware ~50 entries) may improve retrieval relevance further but introduces tradeoffs: (a) corpus drift — if loam adopts new vocabulary the curated list goes stale; (b) cross-language surface — if non-English prompts surface, English-only stoplist becomes wrong-altitude; (c) configuration burden if stoplist becomes user-tunable. **Recommendation:** stay at minimal v0.4.3 set until empirical AC.V043.5 verdict drops below GREEN OR a corpus-shape change makes the minimal set demonstrably insufficient. Track with quarterly retrieval-quality probe (rerun AC.V043.5 against current corpus). **Composes with:** AC.V043.5 outcome-altitude probe (the empirical signal); `feedback_locked_design_not_license` (the minimal-set decision is revisitable when outcomes change). **Status:** capture-only. Activation gate: AC.V043.5 verdict drops to YELLOW OR a stranger-clone reveals a stopword-shape gap.

- **Task-notification block stripping on episode write.** Captured 2026-05-09 from v0.4.3 patch §6 deferred items. The Stop-hook writes one episode per turn including the full assistant reply body. Replies often include `<system-reminder>` blocks, task-list dumps, MCP server announcements, etc. that are operational metadata not meant to be retrieval-relevant. These blocks bloat episode size (~2-5x in some cases) AND seed retrieval matches on operational vocabulary that has nothing to do with the conversational topic. v0.4.3's length-normalization + token-sanitization mitigates the operational-vocabulary noise but doesn't eliminate it. **Stripping shape:** pre-write filter in `memory_write_worker._build_episode_args` that strips known operational blocks (regex on `<system-reminder>`, `## Available skills`, etc.) before persisting. Idempotent + back-compat (older episodes stay as-written; only new writes are stripped). **Composes with:** AC.MFBM.1 episode-write contract (changes the body shape on writes); v0.4.3 retrieval surface (improves the signal-to-noise ratio FTS5 ranks across); `feedback_specific_claims_verified_or_marked_guess` (would let AC.V043.5 probes pin specific signal/noise ratio improvements). **AI-time band:** 30-60 min for the strip filter + tests; outcome-altitude probe via re-running AC.V043.5 against a new write surface. **Status:** v0.5.0+ candidate. Activation gate: AC.V043.5 quarterly verdict shows operational-noise-driven false positives in top-3 results.

- **Recency boost in BM25 ranking — multiply score by `1 + α/age_days`.** Captured 2026-05-09 from v0.4.3 patch §6 deferred items. Current ranker is recency-blind: an episode from 2 weeks ago and an episode from 2 minutes ago compete on the same length-normalized BM25 score. Conversational continuity strongly prefers recent episodes (the user is more likely asking about a current topic than something from 2 weeks ago). **Boost shape:** post-rank multiplier `score * (1 + α / max(age_days, 1))` with `α` tuned empirically (probably 0.5-2.0 range). Composes with linear length-normalization (path b) — the multiplier is independent of doclen normalization. Verify v0.4.3 baseline first; recency might not move the needle if the corpus is already churn-rich. **Composes with:** AC.V043.{1,2} (the surface this would extend); `feedback_locked_design_not_license` (current recency-blind ranker is revisitable based on outcomes); ODD §2.5 (the boost coefficient α needs an AC anchor). **AI-time band:** 30 min for the multiplier + tunable + tests; ~1 hr for AC.V043.5 re-run with vs without. **Status:** v0.5.0+ candidate. Activation gate: AC.V043.5 quarterly verdict shows old-episode false positives in top-3 OR users surface a "I asked about something current and got something old" friction.

- **FastMCP `service.py` rip-out.** Captured 2026-05-09 from v0.4.3 patch §6 deferred items. The pre-FBE.7 `framework/memory-system/src/loam_memory_system/service.py` (FastMCP-based memory server stub) is no longer on the runtime path — file-based memory is the canonical floor at v0.4.3 + earlier. FastMCP service.py + the kuzu_db dir + graphiti vendor lib are all dead-code-on-disk. **Rip-out shape:** delete the FastMCP service file + clean dependency declarations + remove dormant launchd plists + update memory-system component README to note the file-based store is canonical and the FastMCP path is fully retired. AC families: AC.RIPOUT.1 (file deletion + import-cleanup), AC.RIPOUT.2 (no production callsite imports the deleted symbols), AC.RIPOUT.3 (back-compat — older workspaces with FastMCP plists still load gracefully). **Composes with:** v0.1.0 OSS publish (substrate cleanliness for stranger-clone); v0.7.0 META-FRAMEWORK (substrate work would touch the same surfaces); `feedback_durable_capture_for_planned_work` (this is the durable capture). **AI-time band:** 1-2 hr for the rip-out + grep + tests + smoke. **Status:** v0.5.0+ refactor. Activation gate: post-OSS-publish + a v0.5.x window when substrate-cleanup is in scope.

## v0.5.0 deferred items (2026-05-09 — subagent-personas routing + priming consumption-gap closure)

- **Subagent-routing structural-enforcement hook.** Captured 2026-05-09 from v0.5.0 patch §6 deferred items (per `feedback_durable_capture_for_planned_work`). v0.5.0 ships SOFT enforcement: the `subagent-routing` SKILL recommends `subagent_type: <persona>` based on work-shape; the dispatcher rules. HARD enforcement — a Claude Code PreToolUse hook on `Task` matcher that REJECTS dispatches with `subagent_type == general-purpose` when the work-shape clearly maps to a typed persona — is the v0.7.0 META-FRAMEWORK structural-enforcement substrate territory. The hook would pattern-match the brief body against the work-shape rubric in the routing SKILL, surface a halt + corrective prompt to the dispatcher, and let the dispatcher override only by explicitly naming the boundary condition (cross-persona work, tool-surface mismatch, persona-constraint override). **Composes with:** `subagent-routing` SKILL (the rubric the hook would consume); v0.7.0 META-FRAMEWORK (structural-enforcement is the v0.7.0 outcome shape); pre-dispatch WD-discipline hook (sibling structural-enforcement entry above; both are PreToolUse on Task matcher). **AI-time band:** 1-2 hr for the hook + tests + a real typed-dispatch fixture. **Status:** capture-only. Activation gate: v0.7.0 plan-time when the META-FRAMEWORK substrate is being shaped.

- **Per-persona model-tier defaults.** Captured 2026-05-09 from v0.5.0 patch §6 deferred items. Each v0.1.7 persona declares `model: inherit` (uses the dispatcher's model). A more sophisticated default could pin model-tier per persona based on cycle complexity: Opus for `loam-plan-author` (complex architectural decisions) + `loam-reviewer` (deep ODD §2.5 walks); Sonnet for `loam-builder` (the default); Haiku for `loam-routing` decisions if a future routing-decider persona ships. The model-rationale pattern from F3 swarming would auto-document the choice in every dispatch brief. **Composes with:** v0.1.7 persona surface (this is a per-file edit; not v0.5.0 fence); F3 swarming `model-rationale` rule; cost-governance work (per-cycle cost forecasting needs model-tier as input). **AI-time band:** 30-60 min for the per-persona model declaration + the dispatch-brief-authoring SKILL extension to omit `model-rationale` line when the persona-default suffices. **Status:** capture-only. Activation gate: a v0.5.x window with v0.1.7-persona file amendment in scope OR cost-governance work surfaces a per-persona cost variance signal.

- **Subagent-persona prompt versioning for in-flight dispatches.** Captured 2026-05-09 from v0.5.0 patch §6 deferred items. Persona prompts are loaded at dispatch-start time from disk (Anthropic's documented behavior). In-flight dispatches inherit whatever persona file content was on disk at the dispatch-start moment; if a persona file is edited mid-flight, no version-pinning currently surfaces the drift. For long-running dispatches (≥15 min wall-clock) this is a real-but-rare drift class. **Pinning shape:** capture the persona file's git SHA at dispatch-start; record in the dispatch's metadata (post-task report); surface drift to the dispatcher if a follow-on dispatch loads a different SHA for the same persona. **Composes with:** v0.1.7 persona files (the read surface); cost-governance per-dispatch metadata (the write surface for the SHA capture); observability-aggregator (canonical home for per-dispatch metadata). **AI-time band:** 1-2 hr for the SHA capture + drift detection + a real probe across a persona-edit boundary. **Status:** capture-only. Activation gate: when persona prompt edits cause observable in-flight drift OR a v0.6.x window with observability-aggregator surface widening already planned.

- **Persona-level halt-trigger consolidation review.** Captured 2026-05-09 from v0.5.0 patch §6 deferred items. Each v0.1.7 persona body names its halt triggers explicitly in a Halt-and-surface section: builder names 7 triggers; plan-author names 4; researcher names 4; reviewer names 5 FAIL-band conditions; documenter names 4. Some triggers overlap across personas (e.g., "WD drifts from the plan-doc's named working directory" is in builder; sibling shapes appear elsewhere); some have subtle differences that may not be intentional. A consolidation review would cross-walk all 5 personas' halt triggers, identify overlap + intentional differences, and surface where a shared halt-trigger SKILL might reduce duplication. v0.5.0 does not modify persona files; this is a separate amendment surface. **Composes with:** v0.1.7 persona surface (the read surface); `subagent-routing` SKILL (boundary cases between personas surface here); F2 RUTHLESS FEEDBACK (the consolidation review IS a self-applied F2 RF on the persona corpus). **AI-time band:** 30-45 min for the cross-walk + a recommendation report; if recommendation is to ship a halt-trigger SKILL bundle, +1-2 hr build. **Status:** capture-only. Activation gate: next persona-prompt revision cycle OR an observed dispatch where two personas would have halted on the same condition with materially different surfaces.

- **Implementation-tier picker for non-tech user expectation-matching.** Captured 2026-05-09 (Telegram 10575 owner observation). Working with non-tech users, the hardest part is judging the level of implementation effort to engage in without betraying their expectations — both over-building (delivering a full SaaS when a one-time script was wanted) and under-building (delivering a one-time script when they expected a sustainable system). **Proposed shape:** loam ships a predefined, plain-English ladder of implementation tiers that the persona can use to discuss scope with the user before authoring anything. Candidate tiers (rough): (1) **one-time on-thread** — generate output in chat, no artefact persists; (2) **reusable script** — saved file the user can re-run; (3) **local file-based** — script + persistent local data files; (4) **local service-based** — long-running local daemon / scheduled job; (5) **external service** — cloud-deployed (Vercel / Render / etc), accessible from internet, requires auth + security. **Critical risk band on tier 5:** EXCEPTIONALLY clear surfacing of the risks (data exposure, auth + security obligations, ongoing operational liability). Most non-tech users should NEVER need tier 5; tier 5 selection requires an explicit conversation about the data / privacy / security implications. **Show-over failure mode:** one instance of exposing customer data online when the user didn't expect it = brand-killing for loam-as-consulting-tool. The tier-picker is structural prevention. **Composes with:** v0.6.0 non-tech-user surface (this is part of the onboarding-time conversation shape); `light-touch education` AC family (V060.1) — the tier discussion IS one of the educations. v0.1.6 production-safety mode + safety_profile field (the tier-5 floor maps onto production-stake floors); cost-governance (each tier has a different cost shape; the picker should surface cost-band along with capability). **AI-time band:** 1-2 hr for the tier-ladder doc + persona-prompt SKILL section + 1 example onboarding flow exercising it. **Status:** capture-only. Activation gate: v0.6.0 plan-time when non-tech-user surface is being shaped. **Sub-objective alignment:** Goal #2 (loam-as-consulting-tool); critical for loam-consulting credibility.

- **AC.DBT propagated-principle coverage in v0.1.7 personas — RESOLVED 2026-05-09.** Original capture noted v0.1.7 persona bodies didn't carry every AC.DBT propagated principle uniformly. **Resolution:** v0.5.0 corrective `a95dfb9d` (2026-05-09) added §"Reporting + escalation discipline" to all 5 typed personas covering AC.DBT.{3,5,6} + TIME-CLAIMS-DISCIPLINE. Cross-walk SKILL flipped 4 of 6 propagated rows to OMIT-OK. AC.V050.5 verdict: GREEN under the corrected ≥20% threshold (-22.0% midpoint achieved; original ≥30% target was set against an idealized assumption the post-corrective audit invalidated, math-impossible without consolidating non-AC.DBT brief structure). The full-omission shape (which would hit ≥30%) requires a v0.6.0+ amendment that consolidates non-AC.DBT brief structure (mission + authorization + fence + ACs + halt triggers + bookkeeping + model rationale). Captured below as a separate FIDRAFT entry for that follow-on.

- **Non-AC.DBT brief-structure consolidation for typed dispatches.** Captured 2026-05-09 from v0.5.0 corrective halt-and-surface (build agent `a6c45e005fb42f67c`). The 84-line typed-brief floor is set by non-AC.DBT brief structure that AC.DBT propagation cannot reduce. Hitting the originally-targeted ≥30% reduction requires consolidating those structural slots — collapse Authorization + Halt triggers + Bookkeeping by referencing persona/SKILL surfaces rather than restating per-brief. **Composes with:** v0.5.0 substrate (the typed personas already carry discipline — the brief can lean on them harder); `dispatch-brief-authoring` SKILL (the consolidation lives here); v0.6.0+ window. **AI-time band:** 60-90 min midpoint ~75 min. **Status:** capture-only; activation gate: a downstream cycle where brief-length is itself a friction (e.g., context-budget-tight dispatches) OR Anthropic-positioning where consolidated typed dispatches are part of the loam-as-product story.

- **v0.5.0 typed-personas need session-restart to be available; mid-session symlink install doesn't refresh subagent registry.** Captured 2026-05-09 from main-session attempt to dispatch `subagent_type: loam-builder` for the v0.5.0 corrective. Failed with "Agent type 'loam-builder' not found" because Claude Code's subagent registry is loaded at session-start; symlinks at `<workspace>/.claude/agents/` created mid-session aren't picked up. **Implication:** v0.5.0 shipped the typed personas at canonical `plugins/dev-sdlc/agents/` but the workspace install (symlinks at `.claude/agents/`) wasn't part of any AC family. First-use of `subagent_type: loam-*` requires either: (a) install at workspace-bootstrap time so a fresh session has them; (b) `/clear` + reload mid-session if installed late. **Closure shape:** v0.6.0+ amendment that adds workspace-bootstrap step "symlink loam-* personas into `<workspace>/.claude/agents/`" + a session-start hook that detects + warns if symlinks are missing. **Composes with:** v0.1.7 typed personas (the source); `framework/workspace-bootstrap/` (the install surface); session-start hook (the detection surface). **AI-time band:** 30-60 min midpoint ~45 min for bootstrap-step amendment + verification. **Status:** capture-only. Activation gate: next workspace-bootstrap amendment OR next instance of "tried to use typed dispatch and it failed."

- **No public push to loam/main without an accompanying version tag — structural enforcement.** Captured 2026-05-09 (Telegram 10623) after the v0.5.0 path-fixup commit `7363cfb` was pushed to public main as a bare untagged commit. Owner directive: every public push corresponds to a version, even small patches. Memory-rule discipline alone is rule-shape and demonstrated to fail in this very session — Luke caught the bare push before any memory rule fired. **Recommended structural shape:** PreToolUse hook on `Bash` matcher pattern-matching `git push <remote> main` (or any public-branch push) that REFUSES the push if `HEAD` does not point to a tagged commit OR a tag isn't being pushed in the same invocation. Hook surfaces a corrective: "tag with `git tag -a v<X.Y.Z> -m '<objective sentence>'` + push tag together." Override possible only via explicit `--no-version-tag` env var or similar opt-out for the rare legitimate exception (e.g., commit-message-only fix, gh-pages, etc). **Composes with:** the existing `_gate_helpers.py` protected-branch regex (sibling structural-enforcement surface for force-push deny); v0.6.0 release-process CLI (the `loam release` verb already gates on tag presence — this hook is the bypass-blocker for the case when someone uses raw `git push` instead of the verb). **AI-time band:** 30-45 min midpoint ~38 min for the hook + tests + a real-push-without-tag failure-mode probe. **Status:** capture-only. Activation gate: v0.7.0 META-FRAMEWORK structural-enforcement substrate window OR next instance of bare-commit-push-without-version (whichever is sooner).

- **`system-binary-operational` release-gate — structural CLI implementation (v0.7.1 follow-on).** Captured 2026-05-10 from v0.7.1 build-time. AC.READY.7 added gate 7 (`system-binary-operational`) to `docs/release-process.md` §1 as a documentation-only addition; the structural CLI implementation in `framework/tools/loam/src/loam_cli/release/gates.py` was deferred per ODD-no-non-objective-code (a new gate function is MINOR-class — extends release-process capability — out-of-scope for the v0.7.1 PATCH defect-closure). **Proposed shape:** add `system_binary_operational_gate()` function that runs `which loam` + `loam --help` + parses the help output to verify every documented subcommand is registered (the 7-verb list at v0.7.1 ship: `init / amend / release / odd-extract / onboard / pr-safety / project`). Returns RED + corrective hint when `which loam` resolves to a binary whose import fails (e.g., editable installs pointing at a deleted source tree per the v0.5.1 split-worktrees migration root cause that gate 7 exists to catch). Hint text names the canonical fix (`pip uninstall -y` of stale loam packages + `pip install -r install-from-source.txt`). **Composes with:** v0.6.0 release-process CLI (the gate-functions live alongside the existing 6); v0.7.1 install-from-source.txt extension (which is what `pip install -r` will pick up); v0.7.1 public-surface-manifest §2.8 (the gates table is part of the public contract). **AI-time band:** 60-120 min midpoint ~90 min for the gate function + tests + a regression test seeded with a deliberately-broken-system-binary fixture. **Status:** capture-only. Activation gate: v0.8.0 minor (release-process capability extension) OR earlier if v0.7.1 + v0.8.0 publish reveals the gate-7 manual verification is structurally insufficient.

- **Public-surface-manifest structural enforcement (v0.7.1 follow-on).** Captured 2026-05-10 from v0.7.1 AC.READY.9. The `docs/public-surface-manifest.md` is informative until structural enforcement lands — a CI test that fails when any listed surface drifts from the actual exposed surface. **Proposed shape:** test at `framework/tools/loam/tests/test_public_surface_manifest_no_drift.py` that (a) parses the manifest's §2 sections; (b) asserts each listed CLI verb is registered (via `loam --help` parse); (c) asserts each entry-point group name is discoverable via `importlib.metadata.entry_points()`; (d) asserts each manifest field exists in the workspace-bootstrap manifest schema; (e) asserts each on-disk path convention exists at the documented path within a freshly-initted workspace. Test is informational-only (the manifest is contract-frozen; the test catches accidental drift, not intentional revision). **Composes with:** v0.7.1 AC.READY.9 manifest itself (the test reads the manifest as input); v0.7.1 cold-venv install probe (the test runs against the same install surface); v1.0 ship gate (the test must be GREEN before v1.0 ship). **AI-time band:** 90-150 min midpoint ~120 min for the test + manifest-section parser + each-AC-source verification. **Status:** capture-only. Activation gate: v0.8.0+ (release-process structural-enforcement family) OR pre-v1.0 sweep (whichever is closer).

- **v0.7.3 auto-backfill spec was incomplete — 4 residual gaps surfaced at v0.7.3's own publish dogfood.** Captured 2026-05-10 from v0.7.3 publish dogfood + owner directive Telegram 10680 (option A — full structural elimination). **RESOLVED at v0.7.4** — closed by AC.BACKFL2.{1-6,S} per `docs/plans/v0-7-4-auto-backfill-completeness.md`. The v0.7.3 auto-backfill applied 3 edits / 2 files at commit `88964cb` but missed: (1) leading row-title `**v0.7.3 PATCH SHIPPED LOCAL**` was never flipped to `**SHIPPED PUBLIC**` — only the trailing sentence got the flip, leaving the eye-grabbing title misleading; (2) STATE.md row's `seal TBD-AT-SEAL` placeholder was untouched (only the roadmap §2 row got TBD-AT-* backfill); (3) `TBD-AT-COMMIT` (source-edit SHA) left manual per v0.7.3 D-BACKFL.1.b deferral; (4) `TBD-AT-APPLY` (apply SHA) same. Manual touch-up at commits `cb71ca5` + `5c3f7ac` closed the gaps for v0.7.3; v0.7.4 makes them structural. **Resolution shape (v0.7.4):** three new helpers in `post_publish_backfill.py` — `_backfill_state_md_leading_title` flips the bolded leader; `_backfill_state_md_placeholders` mirrors the v0.7.3 roadmap-row TBD-AT-* helper to STATE.md; `_discover_source_edit_and_apply_shas` walks the seal commit's git log message back to the apply commit (`chore(amend): <slug> manifest+apply — ... BASELINE+sidecar bump to <source-edit-sha>` canonical form) + extracts the source-edit SHA. Path-B ruling per AC.BACKFL2.3 (single-component release-CLI extension; path-A — extend `loam amend seal` contract — declined). 8 new tests at `test_AC_BACKFL.py`. Outcome-altitude probe runs the function against the live `/Users/lukeivers/loam/` state for v0.7.3 — confirms all 4 gap-edits are correctly identified + the commit-graph walk discovers the real apply (`527698b`) + source-edit (`01e0883`) SHAs.

- **Post-publish state-staleness — recurring SHIPPED-LOCAL → SHIPPED-PUBLIC manual backfill defect.** Captured 2026-05-10 from owner directive (Telegram 10675) after the v0.7.2 manual backfill commit `f0ae00c` (the LAST manual cycle). **RESOLVED at v0.7.3** — closed by AC.BACKFL.{1-6,S} per `docs/plans/v0-7-3-release-cli-auto-backfill.md`. The recurring-defect shape: post-publish, the version's `**vX.Y.Z ... SHIPPED LOCAL**` rows in `docs/STATE.md` and `docs/release-roadmap.md` continued to claim "SHIPPED LOCAL — owner gates publish" until a human ran a manual backfill commit. Bit at every loam publish since v0.6.0 (commits `5c0d272` v0.6.0, `0f0d4b3` v0.7.0, `af73a69` v0.7.1, `f0ae00c` v0.7.2). The v0.6.0 release-process documented "STATE.md updated" as a publish gate but did not enforce post-publish state-update. Downstream agents reading STATE.md surfaced the staleness as F2 RUTHLESS FEEDBACK each cycle. **Resolution shape (v0.7.3):** new module `framework/tools/loam/src/loam_cli/release/post_publish_backfill.py` exports `apply_backfill(repo_root, version, tag, tag_sha, *, dry_run=False)` → `BackfillResult`. Wired into `runner.run` between step 4 (push branch + tag) and step 6 (post-ship review). The function: (a) flips the STATE.md trailing `<version> SHIPPED LOCAL — owner gates publish.` sentence to `**<version> SHIPPED PUBLIC YYYY-MM-DD at tag \`<tag>\` (annotated \`<sha7>\`)**.`; (b) appends the SHIPPED-PUBLIC marker to the §2 row's third pipe-cell; (c) backfills `TBD-AT-SEAL` / `TBD-AT-TAG` placeholders from known SHAs (`TBD-AT-COMMIT` / `TBD-AT-APPLY` not discoverable from runner inputs — left alone + surfaced); (d) updates the `**Total shipped:** N minor + M patches. v<latest> published.` aggregate-count summary line; (e) appends a new bold entry to §3 Active version. Commits as `docs(release): vX.Y.Z post-publish backfill — SHIPPED PUBLIC`; pushes to `origin main`. Idempotent on re-run (no commit if no edits). 11 new tests at `framework/tools/loam/tests/test_AC_BACKFL.py` cover positive/negative/idempotence/dry-run/runner-integration. Outcome-altitude probe (`loam release v0.7.3 --dry-run`) emits a `DRY-RUN: would apply post-publish backfill` summary block naming the file edits.

- **`loam release` `acs-verified` gate parser scoping (v0.7.1 publish-time finding).** Captured 2026-05-10 from v0.7.1 publish-prep. **RESOLVED at v0.7.2** — closed by AC.READYP.{1-4,S} per `docs/plans/v0-7-2-release-cli-parser-fix.md`. The `acs-verified` gate now scopes its AC-ID scan to the plan-doc `## §4 — Acceptance criteria` section body (between the §4 heading and the next `## §<n>` boundary). Cross-references in §6 / §8 / §11 / §13 are no longer flagged as missing-from-§status. AC.READYP.2 restored AC-ID precision in the v0.7.1 plan-doc cross-references (AC.NTU.6 in §6 + AC.V060.7 in §8). Three new tests at `framework/tools/loam/tests/test_AC_V060_2_pre_publish_gates.py` cover the scoped scan (positive ignore-cross-refs; negative names-only-§4-ACs; negative §4-heading-absent). [Original capture text retained for audit history:] The `acs-verified` gate scans the entire plan-doc for any `AC.<scope>.<n>` token + asserts each is GREEN-marked in §status. The scan is too greedy: it picks up cross-reference AC IDs in §6 (out-of-scope explanations) + §8 (predecessor dependencies) and treats them as in-scope ACs to verify. v0.7.1 publish failed RED on `AC.NTU.6` (referenced in §6 as out-of-scope follow-on from v0.7.0) and `AC.V060.7` (referenced in §8 as v0.6.0's dogfood AC the v0.7.1 publish ritual itself uses). Worked around by rewriting the cross-references to use prose — preserves meaning, satisfies parser, but loses the AC-ID precision in the cross-reference. **Proposed structural fix:** restrict the AC-ID scan to a single named section (the plan-doc convention defines §4 as "Acceptance criteria" — that's the canonical location for in-scope ACs). Cross-references in §6/§8/elsewhere should not be treated as ACs the parser must verify. Implementation in `framework/tools/loam/src/loam_cli/release/gates.py`: parse plan-doc for the `## §4 — Acceptance criteria` heading, scan AC IDs only between that heading and the next `## §<n>` boundary. **Composes with:** the existing §13 §status header convention (per v0.7.0 publish: parser requires the literal `## §13 — §status` heading); the broader release-CLI parser brittleness family (this is the second parser-too-literal finding). **AI-time band:** 30-60 min midpoint ~45 min for the section-scoped parser + tests + a regression test seeded with a plan-doc that has cross-reference AC IDs in §6/§8. **Status:** RESOLVED at v0.7.2 (sealed local 2026-05-10).

## v0.8.0 deferred items (2026-05-10 — honesty cleanup; reviewer findings F7 + F8 + AC.HONEST.6 triage residuals)

- **F7-PLUGIN-VERSION — Plugin contract `api_version` field on `ContributionMetadata`.** Captured 2026-05-10 from external-reviewer report 2026-05-10 (Axis-5 LOW = Plugin/extensibility contract MEDIUM, Named weaknesses #5). The plugin contract is real where it matters (entry-point discovery, Pydantic-validated metadata, named phases, ordering constraints) but unreal where third parties would actually trip on it: no `api_version: int` field on `ContributionMetadata`; bare-name dep declarations (`loam-workspace-bootstrap` without version constraint between plugin + core); `host: Any` duck-typed without a `BootstrapHost` Protocol. Plugin authors targeting "loam 0.7" have no way to declare that — they declare against `loam-workspace-bootstrap`, which is at 0.1.0 (now 0.8.0 after v0.8.0). **Proposed shape:** add `api_version: int = 1` required field on `ContributionMetadata` (bootstrap rejects on mismatch); add `BootstrapHost` Protocol typing the host attribute surface; add upper-bound version constraints in `install-from-source.txt` for inter-component deps. **Composes with:** AC.HONEST.1 (per-component-version discipline establishes the substrate this builds on); reviewer's Axis-5 verdict (this is the path to MEDIUM → HIGH); v1.0 ship gate (plugin contract version surface is a v1.0-readiness component). **AI-time band:** 90-150 min midpoint ~120 min for the field + Protocol + version constraints + bootstrap-side rejection + tests. **Status:** capture-only. Activation gate: v0.8.x or v0.9.0 plugin-contract-hardening cycle. Out-of-scope at v0.8.0 per plan §6.

- **F8-CRITERION-2-THIRD-PARTY — v1.0 criterion #2 ("one real user has shipped real software with loam, not a smoke-test fixture") remains unmet by maintainer's own definition.** Captured 2026-05-10 from external-reviewer report 2026-05-10 (Axis-12 LOW + Named weaknesses #6). BallotPath is the maintainer's own project per `docs/release-roadmap-dependency-map.md` lines 49 + 58 + 62 — maintainer dogfood, not third-party shipping. The reviewer correctly noted: "Maintainer-shipping-on-maintainer-tooling is dogfood, not the criterion's intent." **The honest position:** v0.8.0 doesn't redefine criterion #2; the criterion remains explicitly unmet (already named honestly in STATE.md per the v0.7.0 row's own self-assessment). Closing this requires a real third party (NOT BallotPath) shipping software using loam. **Proposed shape:** there is no maintainer-controllable closure. The path-forward is owner-gated: identify a third-party developer (someone other than Luke) who would adopt loam for a real project; help them onboard; ship the project under loam. **Composes with:** v1.0 ship gate (criterion #2 is unmet); AC.HONEST.7 (this is one of the 2 of 8 reviewer findings explicitly OUT-OF-SCOPE / FIDRAFT-deferred at v0.8.0). **AI-time band:** N/A — owner-gated wall-clock event. **Status:** capture-only with the honest acknowledgment. Activation gate: an actual third-party shipping event.

- **F-RETIRE-MIGRATE-TOOLS — Retire `framework/tools/loam-migrate-*` + `heavy-b-migrate` + `orphan-plist-cleanup` + `pos-publish-framework-only` (workspace-sync test cleanup ride-along).** Captured 2026-05-10 from external-reviewer report (Axis-2 weakness #8) + AC.HONEST.6 triage F-TF-1 finding. The 4 `loam-migrate-*` tools (launchd-labels / host-config / dormancy-config) + `heavy-b-migrate` + `orphan-plist-cleanup` are one-time migration scripts that have run. They ship in the repo as runtime components creating `framework/tools/` overflow. The `pos-publish-framework-only` tool was already retired (`framework/workspace-sync/tests/test_AC_D_5_5_bare_paths_absent.py:132` asserts a stale path expectation that fails). **Proposed shape:** retire the 6 migration tools (delete src + tests + remove from install-from-source.txt) + update `framework/workspace-sync/tests/test_AC_D_5_5_bare_paths_absent.py` to reflect the post-retirement structural surface. Single multi-component cleanup cycle. **Composes with:** reviewer's Axis-2 verdict (retire-them is the path to "Architecture clarity" HIGH); AC.HONEST.6 F-TF-1 (the workspace-sync test-fixture stale-path fix); v1.0 ship gate (the migrate-tools-overflow is a v1.0-readiness cleanup). **AI-time band:** 60-120 min midpoint ~90 min for the 6-tool retirement + workspace-sync test update + structural-surface verification + FIDRAFT-resolve at this entry. **Status:** capture-only. Activation gate: v0.8.x cleanup cycle.

- **F-FUNC-1 — Extend `_backfill_state_md_leading_title` to handle date-in-title variant.** Captured 2026-05-10 from v0.8.0 build-time AC.HONEST.4 halt-and-surface. The v0.4.2 STATE.md row uses the form `**v0.4.2 SHIPPED LOCAL 2026-05-09**` (date in bolded title) — non-canonical for the v0.7.4 helper's regex `\*\*<version>\s+(MINOR|PATCH|minor|patch)\s+SHIPPED LOCAL\*\*`. The function correctly surfaces a hint and skips; manual touch-up resolved v0.4.2 in the v0.8.0 cycle. **Proposed shape:** extend the regex to `\*\*<version>(\s+(MINOR|PATCH|minor|patch))?(\s+SHIPPED LOCAL.*?)\*\*` with a backreference for the SHIPPED-LOCAL portion + corresponding replacement that flips LOCAL → PUBLIC + appends the at-tag-annotated suffix. **Composes with:** v0.7.4 BACKFL2.1 helper (this extends it); v0.8.0 AC.HONEST.4 (this is the named follow-on for the v0.4.2 manual touch-up). **AI-time band:** 20-40 min midpoint ~30 min for the regex extension + 2 new test cases (date-in-title positive + already-public no-op for the variant) + verification. **Status:** RESOLVED 2026-05-13 by v0.10.2 PATCH (`state-md-leading-title-date-variant`). Implementation tightened the originally-proposed shape per D-SMLTV.1 / `feedback_loose_AC_text_fix_AC_not_implementation`: the leading-title flip is a pure LOCAL → PUBLIC token swap with date preserved verbatim, NOT an append of the at-tag-annotated suffix (the at-tag/annotated detail is owned by the sibling trailing-sentence helper `_backfill_state_md`, not the leading-title helper). Helper regex extended to alternation between canonical CLASS-keyword shape and date-in-title variant via named groups (`cls` / `date`); replacement uses whichever group is populated. AC.SMLTV.{1,2,3,4,S} all GREEN. 25/25 BACKFL tests pass (22 existing preserved + 3 new). Sub-plan: `docs/plans/state-md-leading-title-date-variant.md`. Smoke: `docs/experiments/v0-10-2-hard-smoke.md`.

- **F-FUNC-2 — Extend `apply_backfill` with retroactive interim-SHIPPED-LOCAL-sentence removal mode.** Captured 2026-05-10 from v0.8.0 build-time AC.HONEST.5 halt-and-surface. The v0.7.4 function's idempotent design correctly skips the trailing-claim flip when a SHIPPED-PUBLIC marker already exists (e.g., v0.5.0's case where the publish-public-marker landed manually before v0.7.3's auto-backfill existed). But this leaves any stale interim "SHIPPED LOCAL — owner gates publish" sentence in place. v0.8.0 manually removed this for v0.5.0; the retroactive cleanup pattern would benefit from automation. **Proposed shape:** extend `apply_backfill` with an optional `--remove-interim-shipped-local-sentence` mode (or equivalent kwarg) that detects + removes the stale interim sentence when a SHIPPED-PUBLIC marker already exists for the version. **Composes with:** v0.7.4 BACKFL2.{1,2} helpers; v0.8.0 AC.HONEST.5 (this is the named follow-on for the v0.5.0 manual touch-up). **AI-time band:** 20-40 min midpoint ~30 min for the kwarg + helper + 2 test cases (positive: stale interim removed; negative: no-marker-no-removal preserves SHIPPED-LOCAL trailing-claim) + verification. **Status:** RESOLVED 2026-05-14 by v0.10.3 PATCH (`release-backfill-helpers-completeness-batch`). Implementation tightened the originally-proposed kwarg-mode shape per D-RBHCB.1 / `feedback_loose_AC_text_fix_AC_not_implementation`: the cleanup runs unconditionally inside `_backfill_state_md` whenever the trigger condition holds (already-public marker present AND stale-LOCAL-sentence still present); no kwarg gate. The kwarg in the original capture was an implementation suggestion, not an outcome requirement. New helper `_remove_stale_interim_sentence(body, version)` extracted for readability; called from `_backfill_state_md` after the existing `_already_public_in_state_md` short-circuit. Idempotent: re-run on already-cleaned body returns None edit_summary. AC.RBHCB.1 GREEN (positive removal + idempotence tests). Sub-plan: `docs/plans/release-backfill-helpers-completeness-batch.md`. Smoke: `docs/experiments/release-backfill-helpers-completeness-batch-hard-smoke.md`.

- **F-FUNC-3 — Scope `_backfill_state_md_placeholders` to known trailing-position placeholders only (narrative-safety extension).** Captured 2026-05-13 from `historical-post-publish-completeness-backfill` PATCH plan-time empirical recheck. **Empirical finding:** `_backfill_state_md_placeholders` at `framework/tools/loam/src/loam_cli/release/post_publish_backfill.py:415-416` uses non-boundary-aware `new_row.replace("TBD-AT-SEAL", f"\`{seal_sha[:7]}\`")` (and the same shape for `TBD-AT-TAG` / `TBD-AT-COMMIT` / `TBD-AT-APPLY`). **Corruption shape:** STATE.md rows whose prose narrative contains literal `TBD-AT-SEAL` / `TBD-AT-TAG` strings (e.g., the v0.7.3 STATE.md row at `docs/STATE.md:133`, whose body describes what the v0.7.3 auto-backfill helper itself does — specifically the prose `` backfills `TBD-AT-SEAL` / `TBD-AT-TAG` placeholders from known SHAs `` and `` (`TBD-AT-COMMIT` / `TBD-AT-APPLY` not discoverable from runner inputs — left alone) ``) cannot be retroactively backfilled via the top-level `apply_backfill(...)` without corrupting the prose narrative. Path-A of the `historical-post-publish-completeness-backfill` PATCH (which would have invoked `apply_backfill` retroactively) halted at the dispatcher level after this empirical finding surfaced; Path-B (manual surgical edit at the trailing placeholder only) shipped instead. **Proposed safety extension:** scope `TBD-AT-*` replacement to specific known-placeholder positions — e.g., trailing `seal TBD-AT-SEAL.` only (the canonical placement the helper itself emits at publish-time, immediately before the SHIPPED-PUBLIC marker sentence). Concretely: replace the non-boundary-aware `str.replace` calls with regex matches anchored to the trailing-position context (e.g., `r"seal TBD-AT-SEAL\.(?=\s*\*\*v[\d.]+\s+SHIPPED PUBLIC)"`). Same shape for `TBD-AT-COMMIT` / `TBD-AT-APPLY` / `TBD-AT-TAG` — each placeholder has a canonical surrounding context the helper emits; the regex anchors should match only that context. **Composes with:** F-FUNC-1 (regex-shape extension to `_backfill_state_md_leading_title` — sibling helper, same family); F-FUNC-2 (interim-cleanup-mode extension — different shape, same module); F-NFCLEAN-FOLLOWON (this PATCH closes 2 of 3 deferred items strictly via manual edits; the 3rd — v0.7.3 STATE.md + roadmap §2 row `TBD-AT-COMMIT` / `TBD-AT-APPLY` placeholders — is precisely the case F-FUNC-3 would unblock; closing it requires the narrative-safety extension first OR the same surgical-edit treatment per-placeholder). **AI-time band:** 30-60 min midpoint ~45 min for the regex anchors + 4 new test cases (positive: trailing placeholder backfilled correctly; negative: prose narrative containing literal `TBD-AT-*` strings preserved; idempotence: re-run no-op when placeholder already backfilled; mixed-row: prose contains TBD-AT-* literals AND trailing placeholder present — only trailing one is backfilled) + verification against the live v0.7.3 row. **Status:** RESOLVED 2026-05-14 by v0.10.3 PATCH (`release-backfill-helpers-completeness-batch`). Implementation per D-RBHCB.3: each TBD-AT-* placeholder gets a regex matcher with a positive lookbehind for its canonical preceding token — `TBD-AT-SEAL ← seal `, `TBD-AT-COMMIT ← source-edit `, `TBD-AT-APPLY ← apply `, `TBD-AT-TAG ← tag `. Pattern shape `(?<=seal )TBD-AT-SEAL\b` (and analogously for the other three). Backtick-wrapped prose-narrative occurrences (`` `TBD-AT-SEAL` `` etc.) lack the canonical prefix and are skipped. Slight scope tighten vs FIDRAFT proposal: lookbehind anchors don't constrain trailing context (no `(?=\s*\*\*v[\d.]+\s+SHIPPED PUBLIC)` lookahead) — the canonical-preceding-token is a sufficient anchor for the corruption pattern observed at v0.7.3 STATE.md row, and a tighter lookahead would over-constrain the roadmap-row context where SHIPPED-PUBLIC isn't always immediately following. AC.RBHCB.3 GREEN (positive narrative-preserving + negative prose-only + canonical-pre-image regression tests). Sub-plan: `docs/plans/release-backfill-helpers-completeness-batch.md`. Smoke: `docs/experiments/release-backfill-helpers-completeness-batch-hard-smoke.md`. Note: this PATCH ships the helper extension; the residual v0.7.3 / v0.7.x `TBD-AT-COMMIT` + `TBD-AT-APPLY` placeholder closure (the 3rd F-NFCLEAN-FOLLOWON item) MAY now be unblocked, but that closure is a separate cycle.

- **F-TF-1 — `framework/workspace-sync/tests/test_AC_D_5_5_bare_paths_absent.py` retired-tool path expectation.** Captured 2026-05-10 from v0.8.0 AC.HONEST.6 triage. The test asserts `framework/tools/pos-publish-framework-only/pyproject.toml` exists — the tool was retired. **Proposed shape:** update the `expected` list to drop the retired tool path; composes with F-RETIRE-MIGRATE-TOOLS. **AI-time band:** 15-30 min midpoint ~22 min. **Status:** capture-only. Activation gate: F-RETIRE-MIGRATE-TOOLS cycle.

- **F-TF-2 — `framework/objective-tracker/tests/test_d7_otel_emission.py` collection error.** Captured 2026-05-10 from v0.8.0 AC.HONEST.6 triage. Likely pytest-asyncio downstream effect; verify post-install. **Proposed shape:** verify post-pytest-asyncio install whether the collection error resolves. If yes, no closure needed. If no, capture the actual root cause + closure path. **AI-time band:** 5-15 min verification + TBD if real defect. **Status:** capture-only. Activation gate: post-v0.8.0 publish dogfood verification.

- **F-TF-3 — Per-component asyncio test verification post-pytest-asyncio install.** Captured 2026-05-10 from v0.8.0 AC.HONEST.6 triage. Post v0.8.0 publish + fresh install with pytest-asyncio resolved, run the full-test-suite smoke + report the failure count drop. **Proposed shape:** per-component test sweep + root-cause assignment per remaining failure. **AI-time band:** 30-60 min midpoint ~45 min. **Status:** capture-only. Activation gate: post-v0.8.0 publish.

- **F-TF-4 — Drive remaining test failures to zero pre-v1.0.** Captured 2026-05-10 from v0.8.0 AC.HONEST.6 triage. v1.0 ship gate requires zero-known-failures. **Proposed shape:** for each test-failure remaining post-pytest-asyncio install + F-TF-1/2/3 closures, capture root cause + closure path; commit to zero-known-failures by v1.0. **AI-time band:** depends on F-TF-3 verification result. **Status:** capture-only. Activation gate: v1.0 prep.

- **F-OTEL-VERSION-BUMP — OTel tracer version strings still hardcoded `0.1.0`.** Captured 2026-05-10 from v0.8.0 AC.HONEST.1 in-cycle finding. Each component's `observability.py` has `_TRACER = trace.get_tracer("loam.<component>", "0.1.0")` — the second arg is the tracer's instrumentation version, distinct from the package version but still a documented-vs-actual drift. v0.8.0 deliberately scoped to pyproject.toml + `__version__` strings (the user-visible surfaces); OTel tracer versions touch the trace-data layer (telemetry semantics). **Proposed shape:** import `__version__` from each component's package + use it as the tracer-version arg, OR bump the literal to `0.8.0` everywhere. **Composes with:** AC.HONEST.1 per-component-version discipline (extends to telemetry). **AI-time band:** 30-60 min midpoint ~45 min for the per-component sweep + tests if any assert on tracer version + verification. **Status:** RESOLVED 2026-05-14 by v0.10.4 PATCH (`otel-tracer-version-honesty`). Implementation per D-OTVH.1: Path B (current literal "0.10.0") ruled over Path A (`__version__` import) — none of 6 affected components export `__version__`; adding 6 new exports would be scope-creep. Path A captured as new FIDRAFT F-OTEL-VERSION-DYNAMIC-IMPORT for follow-on. 12 sites swept (7 production observability/supervisor + 5 test-fixture monkeypatch installers); runtime dogfood probe verified `instrumentation_scope.version == "0.10.0"` across all 7 production tracers. AC.OTVH.{1,2,3,4,S} GREEN. Sub-plan: `docs/plans/otel-tracer-version-honesty.md`. Smoke: `docs/experiments/otel-tracer-version-honesty-hard-smoke.md`.

- **F-WALKER-1 — `_classify_row` pipe-in-description robustness.** Captured 2026-05-10 from v0.8.1 AC.NFCLEAN.2 build-time investigation. The walker reads `third_cell_split[3]` (the 4th element of `row.split("|")`) as the classification cell. Rows whose description (cell [2]) contains backtick-wrapped pipes (e.g., v0.4.2's `\`Y\` → \`Union[X, Y]\` / \`Optional[X]\``) cause the split to exceed 5 elements; cell [3] becomes the SECOND segment of the description rather than the actual class cell. v0.8.1 added a version-pattern fallback (X.Y.0 = MINOR; rest = PATCH) which incidentally produces the correct answer for v0.4.2 (it IS a patch), but the explicit-class detection is still wrong for the same reason. **Proposed shape:** split on a pipe-row-aware tokenizer that respects backtick-bounded pipes — use a regex like `r"(?<!\`)\|(?![^\`]*\`(?!\|))"` or a state-machine tokenizer that tracks backtick parity. **Composes with:** v0.7.3 AC.BACKFL.2 (this fixes the same walker); v0.8.1 AC.NFCLEAN.2 (extends the version-pattern fallback to the explicit-class path too). **AI-time band:** 25-45 min midpoint ~35 min for the tokenizer + test cases (pipe-in-backticks positive; multi-pipe-in-backticks; nested-backtick edge case) + verification. **Status:** RESOLVED 2026-05-14 by v0.10.3 PATCH (`release-backfill-helpers-completeness-batch`). Implementation per D-RBHCB.2: state-machine tokenizer that walks the row character-by-character tracking backtick parity (toggle a boolean on each backtick; emit a cell on each `|` only when parity is 0). Single-pass, no regex backtracking surprises. New helper `_split_pipe_row_backtick_aware(row) -> list[str]` lives next to `_classify_row` for visibility; replaces `row.split("|")` callsites in BOTH `_classify_row` and `_extract_objective_sentence` (the same tokenizer bug affected both). Existing version-pattern fallback in `_classify_row` retained as defense-in-depth. AC.RBHCB.2 GREEN (backtick-aware split + classification-via-tokenizer-not-fallback contradiction shape + objective-extraction preservation + regression for fallback path). Sub-plan: `docs/plans/release-backfill-helpers-completeness-batch.md`. Smoke: `docs/experiments/release-backfill-helpers-completeness-batch-hard-smoke.md`.

- **F-PCV-1 — Per-component pyproject patch-number bumps for PATCHes.** Captured 2026-05-10 from v0.8.1 D-NFCLEAN.4 ruling. The v0.8.0 per-component-version discipline established that component pyproject.toml versions advance with shipped MINORs (all 30 pyprojects bumped 0.1.0 → 0.8.0). v0.8.1 (PATCH) does NOT bump pyprojects — they stay at 0.8.0 per the predecessor MINOR. Question: should PATCHes bump component patch-numbers (`0.8.0 → 0.8.1`) so pyproject.toml versions reflect the actual repo-tag? Tradeoff: SemVer purity (every released tag has matching pyproject version) vs cleanup overhead (every PATCH cycle now touches 30 pyprojects). **Proposed shape:** option A — bump per-component patch-numbers per PATCH cycle (full SemVer alignment); option B — bump only the touched-component's patch-number per PATCH (per-component fence respect); option C — keep current discipline (PATCHes share predecessor MINOR's version; releases identified by repo tag only). **Composes with:** v0.8.0 AC.HONEST.1 (extends the discipline to PATCH cycles); F7-PLUGIN-VERSION (the api_version surface that needs per-component patch granularity to be useful). **AI-time band:** 5-10 min decision + 15-30 min implementation per cycle if option A or B. **Status:** decision-needed. Activation gate: v0.9.0 cycle prep (decide before next MINOR).

- **F-NFCLEAN-FOLLOWON — Side-effect cleanups skipped during v0.8.1 strict-scope build.** Captured 2026-05-10 from v0.8.1 AC.NFCLEAN.1 HARD HALT #7 ruling. Build-time research showed `apply_backfill(...)` (top-level) on v0.7.1/v0.7.2/v0.7.3 produces 9 total edits (vs the 3 leading-title flips AC.NFCLEAN.1 names). The additional 6 edits are LEGITIMATE axis-12 honesty closures of the same drift class: (a) v0.7.3 STATE.md TBD-AT-{COMMIT,APPLY} placeholders backfilled (residual from v0.7.4's manual touch-up at `cb71ca5` that didn't fully sync to STATE.md); (b) v0.7.3 roadmap §2 row TBD-AT-{COMMIT,APPLY} placeholders backfilled; (c) v0.7.2 §3 Active version entry appended (was missing — v0.7.2's publish didn't append at the time). v0.8.1 deferred these per HARD HALT #7 strict-scope ruling. **Proposed shape:** invoke `apply_backfill(...)` against v0.7.1 / v0.7.2 / v0.7.3 with full side-effect application; commit as `docs(state): historical post-publish-completeness backfill — v0.7.1/v0.7.2/v0.7.3` single atomic cleanup. **Composes with:** v0.7.4 BACKFL2.* helpers (this exercises the full helper set); v0.8.1 AC.NFCLEAN.1 (this is the named follow-on for the side-effect deferral). **AI-time band:** 10-20 min midpoint ~15 min for the apply_backfill calls + verification + commit. **Status:** RESOLVED-BY-COMPOSITION 2026-05-14. Items (a) + (b) (v0.7.3 STATE.md + roadmap §2 row TBD-AT-{COMMIT,APPLY} placeholders) closed empirically — investigation under `residual-tbd-at-placeholder-closures` cycle (agent halt-and-surface) confirmed the v0.7.3 row commit-metadata cells are fully populated as of HEAD `816d264`. Resolution chain: v0.10.1 surgical edit (`fabdf76`, AC.HPPCB.1) closed the v0.7.3 STATE.md trailing seal placeholder; v0.7.4's `apply_backfill` walker handled the remaining commit-metadata cells across the v0.7.x publish chain; v0.10.3's narrative-safe `_backfill_tbd_placeholders` extension (`f3f6cf1`, AC.RBHCB.3) makes any future residual-placeholder closure narrative-safe by construction. Item (c) (v0.7.2 §3 Active version entry) was closed inline at the v0.10.1 PATCH. No additional cycle needed; only TBD-AT-* tokens remaining in the v0.7.3 row are intentional backtick-wrapped narrative-prose (correctly skipped by the lookbehind anchors).

- **F-EXTRACT-CONVERGE — Multi-source provenance convergence as objective-quality confidence signal (continuum extractor).** Captured 2026-05-11 from owner Telegram 10846 during ProgramBench-derivative experiment Step 1 (extractor adds subprocess_help + subprocess_version + subprocess_strings channels). Step 1 ships per-Objective provenance tagging (each Objective carries which channel(s) it was sourced from). The owner observation extending this: when an objective is sourced from MULTIPLE convergent channels (e.g., README mentions a flag AND `--help` lists it AND `strings` finds it embedded), the convergence is itself a strong confidence signal — much higher quality than a single-source objective. **Proposed shape:** (a) dedup function on the merged objective set that detects functionally-equivalent objectives across channels (semantic similarity, not exact text match — likely an LLM-judge pass on candidate pairs OR a heuristic on shared keywords + structure); (b) confidence field on Objective populated from convergence-count + channel-weights (e.g., README+help+strings = HIGH, README+help = MEDIUM-HIGH, README-only = MEDIUM, help-only = MEDIUM, strings-only = LOW); (c) downstream composer can use confidence to weight ordering / surface lower-confidence objectives for ambiguity dialogue with non-tech users. **Composes with:** Step 1 provenance tagging (the data substrate this builds on); the broader continuum architecture's "objective stack with altitude + kind"; the non-tech-user value-prop (low-confidence objectives are exactly what should surface for clarification dialogue). **AI-time band:** dedup function 90-180 min (LLM-judge pass is the expensive part); confidence field 30-60 min once dedup is in place; total 120-240 min midpoint ~180 min. **Status:** capture-only. Activation gate: post-Step-4 (after compile-loop test results land) OR earlier if the Step 2 benchmark re-run reveals that confidence-signaling is needed to disambiguate which objectives drove which test-pass behavior. Owner explicitly said "not sure if this is worth a significant effort to implement now."

- **F-VERIFIER-LAYERS — Multi-layer verifier substrate (compile-success is necessary but not sufficient).** Captured 2026-05-12 from ProgramBench-derivative top-up Batch 3 yj rep3 (Telegram 10941 surfacing). Empirical finding: compile-loop fired 2 attempts, succeeded (binary verifier passed → code compiles), but downstream test-pass-rate was 8.24% (vs siblings rep0/rep2 at 37%/39%). The substrate's verifier is too weak — "does it compile" doesn't entail "is it semantically correct." Proposed shape: extend the iteration-loop family from a single verifier to a layered stack of verifiers in cost-ascending order: (1) compile-success (current; cheapest); (2) README-example self-tests (run documented examples against the produced binary, compare outputs to README-stated expectations); (3) behavioural-equivalence probes (run produced binary alongside reference on constructed inputs, diff outputs); (4) LLM-judge against rubric (most expensive). Each layer gates the next; the substrate halts at the first failure and either retries with that layer's feedback OR escalates to a richer layer. Submission ships only when ALL configured layers pass within their retry budgets. **Composes with:** the iteration-loop family from `objective-continuum-architectural-reframe-2026-05-11.md` extension #6 (this is the family's depth dimension); F-EXTRACT-CONVERGE (the convergence-confidence pattern — verifier-layers-passed is the analogous confidence signal on the output side); F-MEMORY-RELEVANCE-VERIFIER (LLM-judge as one verifier; the architectural pattern generalises). Empirical evidence inline: yj rep3 8.24% with all-green compile-loop is the worked counterexample showing the single-verifier substrate is insufficient. **AI-time band:** 240-480 min midpoint ~360 min for the framework + 2-3 reference verifiers + layer-composition logic + integration with existing compile-loop. **Status:** capture-only. Activation gate: post-v1.0 architectural cycle, or sooner if a benchmark-context use case forces it.

- **F-RETRY-REGRESSION — Detect when iteration-substrate retry regresses semantic correctness vs the original attempt.** Captured 2026-05-12 from same yj rep3 incident. Empirical finding: compile-loop's retry took a non-compiling attempt 1 and produced compiling attempt 2, but the compiling code scored 8.24% — substantially worse than the corpus-typical 37-40% range for the same task with substrate. The retry's "success" (verifier passed) masked a quality regression. Proposed shape: the substrate maintains a baseline expectation (per-task historical mean across recent reps), and when a retry's downstream score falls below baseline by N stddev, the substrate flags `retry_quality_regression: true` in run.json telemetry. Downstream consumers (the dispatcher, the persona, the report agent) treat this as a warning signal — the substrate "succeeded" but produced a likely-bad sample. Alternative escalation: re-roll generation entirely rather than accept a verifier-pass-but-test-fail output. **Composes with:** F-VERIFIER-LAYERS (this is the failure-mode the verifier-layers idea addresses architecturally — both flag the same gap, attacked from different angles); the existing compile-loop telemetry (this extends the telemetry surface, not the loop itself). Requires per-task baseline data — could come from rolling historical aggregation or from a fresh "baseline rep" run at the start of each benchmark cycle. **AI-time band:** 120-240 min midpoint ~180 min for the baseline tracker + regression detector + telemetry integration + test. **Status:** capture-only. Activation gate: post-v1.0 OR sooner if iteration-substrate retries start producing systematically worse outputs in production use.

- **F-INVERTED-FRAME — v0.9.0 ODD methodology paper inverts top-level objective vs operational paths; corrective experiment needed.** Captured 2026-05-14 (Luke F2 via Telegram 11104 on 2026-05-13). The v0.9.0 published paper at `docs/papers/odd-methodology.md` frames behavioral-equivalence as Layer 4 of an iteration substrate ("engineering-derived") with compile-loop + README-self-test as separate methodology layers above it. Correct framing: behavioral-equivalence is the TOP-LEVEL OBJECTIVE; compile-loop + README-self-test are operational paths a properly-functioning ODD extraction should DERIVE AS SUB-OBJECTIVES from the prompt. The ProgramBench-derivative `build_prompt` was verified at `/Users/lukeivers/pos3/workspace/experiments/programbench-derivative/harness/run_agent.py` to contain behavioral-equivalence language 4× ("matches the documented behavior", "against expected outputs", "Match the documented CLI shape", "pass behavioral tests against the original binary's documented behavior"). Pre-building compile-loop + README-self-test as named substrate machinery, then running the experiment, doesn't measure what the v0.9.0 paper claims to measure — it measures pass-rate improvement from inserting specific operational paths the methodology should produce, not paths that were baked in. The whole non-tech-user value-prop depends on naturally-emergent operational paths; if loam can't derive these from "match documented behavior," it can't help a non-tech user shipping software either. **Corrective experiment named:** PLAN-A at `<pos3>/workspace/.scratch/claude-output/programbench-v2-plan-2026-05-14.md` — give loam the same `build_prompt` raw LLM gets, observe what objectives + sub-objectives it naturally extracts, see whether compile-loop + README-self-test emerge as derived operational paths WITHOUT being pre-built. 3 arms (raw / loam ODD-only / optional loam + explicit-extract). LLM-judge per-rep captures derivation. **Cost band:** $3-6 total (4× cheaper than v1's 4-layer phase). 13-17 reps. 90-150 min critical path. **Corrective shapes for the published paper:** three paths surfaced to Luke at Telegram 11105 — (1) yank + redo via v0.11.0 with placeholder note; (2) addendum + leave paper; (3) leave + capture privately. Dispatcher leaned (1); owner ruling pending. **Composes with:** PLAN-F at `<pos3>/workspace/.scratch/claude-output/programbench-leverage-plan-2026-05-14.md` (L2 natural-derivation lesson marked PROVISIONAL pending v2 outcome); PLAN-LICENSING at `<pos3>/workspace/.scratch/claude-output/claude-p-licensing-research-2026-05-14.md` (cost-per-rep budget context). **AI-time band:** PLAN-A experiment execution 90-150 min + ~30 min findings memo. Paper corrective (whichever shape Luke picks) 30-90 min. **Status:** OPEN; pending Luke's ruling on the corrective shape (pos3 task #75).

- **F-NEXT-SCOPE-EMPTY-§4 — `loam release` post-ship next-scope-proposal returns "(no entries in §4)" after v0.10.0 emptied §4 of pre-numbered candidates.** Captured 2026-05-14 from v0.10.1 publish output. Empirical finding: after v0.10.0's §4 priority-queue restructure stripped pre-numbered version slots and replaced them with the candidate queue (which lives in a different section structure now), the release CLI's `post_ship_review.py` (or equivalent) walks `docs/release-roadmap.md` §4 looking for `### vX.Y.Z` heading entries to propose as next-scope. Post-restructure §4 carries the queue header + candidate slugs rather than version-pinned headings → walker returns zero entries → output reads `(no entries in §4)` + `Class hint: (no entries in §4)` + `Fence: (no entries in §4)`. Functionally harmless (publish completes; the post-ship section is informational); structurally regresses a useful operator-facing surface from v0.10.0 forward. **Proposed shape:** update the next-scope walker to read from the §4 candidate-queue structure that the v0.10.0 restructure introduced — pick the queue's top-priority candidate by slug, surface objective + class + fence as before. Composes with v0.10.0 priority-queue-restructure (the change that surfaced this; was anticipated as "Note (2026-05-09 Q3 ratification): until that restructure ships, the §4 numbers are placeholder" but the post-ship hook wasn't updated when the restructure DID ship). **AI-time band:** 30-60 min for the walker update + tests against the new §4 structure + dogfood probe. **Status:** capture-only. Activation gate: pre-v1.0 release-CLI consistency sweep OR sooner if Luke uses next-scope-proposal operationally + the empty output is friction.

- **F-PAPER-HTML-REGEN — Regenerate `docs/papers/odd-methodology.html` to match the v19 markdown source-of-truth.** Captured 2026-05-13 from the ODD paper publish build-time decision D-ODDPAPER.5.2 Path C. The previously-staged HTML (committed `cfcb03f`, 32KB, custom-CSS-styled with `prefers-color-scheme` dynamic theme) is from an earlier paper iteration — title reads "Outcome-Altitude Acceptance for LLM-Authored Software" while v19's title is "Methodology Description and Case-Study Observations from LLM-Authored Software." Body content is also substantively different. Build-time path C dropped the stale HTML entirely from the publish (deleted from git) and shipped markdown-only — GitHub renders the markdown natively for external readers. **Proposed shape:** install pandoc (or write a small markdown-to-HTML conversion script in `framework/tools/loam/`) + apply the existing custom CSS template from the deleted `odd-methodology.html` (preserve the dynamic theming) + regenerate from `docs/papers/odd-methodology.md` source-of-truth. Commit the regenerated HTML alongside any subsequent paper edits. **Composes with:** `feedback_dynamic_theme_for_generated_documents` (the regen must preserve the prefers-color-scheme adapt); the broader docs/papers/ category established at this publish (HTML rendering is the canonical reader surface for papers, even though markdown is the source of truth). **AI-time band:** pandoc install + first regen 30-60 min; if writing the conversion script in framework/tools/loam/ for reproducible regen across future paper updates 120-240 min. **Status:** capture-only. Activation gate: next paper-edit cycle OR pre-v1.0 surface cleanup.

- **F-REMOVED-VERDICT-GATE — `acs-verified` gate should accept `REMOVED` as a valid build-time verdict alongside `GREEN`.** Captured 2026-05-13 from v0.8.2 AC.SDPD.4 dogfood probe. **RESOLVED at v0.8.3 (2026-05-13).** Narrow shape shipped: `check_acs_verified` verdict-loop extended with a second proximity-pattern (`re.escape(ac) + r".{0,240}?REMOVED"`) per D-RVG.1.a. Per D-RVG.2.b the gate verifies only the verdict token; verdict-justification (the `D-<plan-id>.<n>` reference) discipline stays plan-doc-author + reviewer responsibility, not parser-enforced — narrower than this FIDRAFT's originally-proposed `(GREEN|REMOVED.{0,160}?D-[A-Z]+\.\d+...)` shape. Per D-RVG.2.a, DEFERRED + N/A + other-verdict expansions are explicitly out of scope for v0.8.3; if those verdicts surface in real plan-docs, capture as new FIDRAFT for a follow-on patch. AC.RVG.4 outcome-altitude dogfood probe verified all 6 gates GREEN against the live paper-publish artefacts post-patch. 4 new tests at `framework/tools/loam/tests/test_AC_RVG_removed_verdict.py`; 86/86 release-CLI tests GREEN. See `docs/plans/v0-8-3-acs-verified-removed-verdict-parser.md` for the full plan-doc.

- **F-AGENT-EMPIRICAL-RECHECK-BEFORE-HALT — Agents must empirically re-verify an "impossible" conclusion before halting on it.** Captured 2026-05-12 from the figlet behavioral-equivalence incident (owner Telegram 10980). **RESOLVED at 2026-05-14:** graduated from FIDRAFT capture to canonical memory rule at `feedback_agent_empirical_recheck_before_halt.md`. Trigger: re-fired tonight against the persona-registry investigation where I concluded "by-design lightweight" before empirical testing; 5-minute test falsified the conclusion. Memory rule encodes the 4-step discipline (state conclusion + evidence; generate 3 alternative hypotheses; empirically test each; halt only after confirmation) + dispatch-brief integration pattern + future structural-enforcement-hook FIDRAFT extension if needed. Original capture text retained below for audit history. Empirical pattern: the build agent ran `./executable Hi` against the figlet reference binary, observed "Unable to open font file" (because the binary's hard-coded `DEFAULTFONTDIR=/usr/local/share/figlet` doesn't exist in the Docker container), concluded "behavioral-equivalence is structurally impossible for figlet because the reference can't be made to find fonts," and SKIPped the verifier for that task. Owner caught the framing: "That implies it is literally impossible for anyone to ever pass that test. That is definitely not the case. There has to be a solution you're missing because you're not empirically evaluating all your assumptions here." 30-second empirical recheck inside the container confirmed: standard figlet implementations respect the `FIGLET_FONTDIR` env var; running `FIGLET_FONTDIR=/workspace/fonts ./executable Hi` produces the expected ASCII art. The fonts physically exist at `/workspace/fonts/` in the container. The reference works fine; the agent never tried the standard convention. **Proposed shape:** when an agent reaches a "this is impossible" or "this fundamentally can't work" conclusion that would cause it to halt or SKIP a meaningful path, the agent must (a) state the conclusion explicitly with its supporting evidence, (b) generate at least 3 alternative hypotheses for why the apparent impossibility is in fact resolvable (env vars, alternate invocation patterns, fixture conventions, framework defaults, search the community/web for how others solve it), (c) empirically test each hypothesis in the same environment where the "impossibility" surfaced, (d) only halt-and-SKIP after the empirical tests confirm the impossibility. This is the agent-side mirror of the owner-side discipline "agents trust their halt-and-surface without re-evaluating." **Composes with:** the broader research-depth-scaling principle (when first-pass-impossible meets high-prior-plausibility, the agent should broaden its search before declaring impossible — same shape as the ccusage discovery earlier today); the multi-channel extraction principle (sometimes the answer is in a channel/probe the agent didn't think to check); ODD's outcome-altitude rule (an agent declaring "impossible" against a load-bearing AC must verify the impossibility at outcome-altitude before accepting it). Cost of the rule: ~1-2 extra minutes per halt-decision; benefit: catches structural false-impossibilities that would otherwise lock out entire features. **AI-time band:** authoring rule + integrating into dispatch-brief templates 30-60 min; structural enforcement (e.g., a hook that detects "impossible"/"cannot work"/"structurally infeasible" phrases in agent halts and requires the agent to attach empirical-recheck evidence) 60-120 min. **Status:** capture-only. Activation gate: post-v1.0 architectural cycle OR sooner if another structural-false-impossibility incident occurs.

- **F-CYCLE-ARTEFACT-SLUG-NAMING — Agents default to slug-naming, not version-naming, for cycle artefacts (smoke writeups, plan-docs, manifests).** Captured 2026-05-14 from v0.10.2 publish: the F-FUNC-1 cycle agent named the HARD smoke writeup `docs/experiments/v0-10-2-hard-smoke.md` (version-prefix) but the `hard-smoke` release gate expects `docs/experiments/<plan-doc-slug>-hard-smoke.md` per the v0.8.2 plan-doc-slug convention (`feedback_version_numbers_at_release_time`). Trivial git mv + commit fix, but this is the second cycle in a row where the version prefix snuck back in despite the slug being explicit in the dispatch brief. **Empirical pattern:** agents trained on dev-conventions historically named artefacts after versions; the autopilot persists even when dispatched with the slug. The plan-doc itself was correctly slug-named (`state-md-leading-title-date-variant.md`); only the smoke writeup drifted. **Proposed shape:** (a) dispatch-brief template addition explicitly enumerating "all cycle artefacts MUST follow `<plan-doc-slug>-*` naming, not `v<version>-*`"; (b) optional structural enforcement — extend the `hard-smoke` gate to fall back to the version-named path with a WARN verdict + corrective hint, OR a pre-commit hook that detects version-prefixed files in `docs/experiments/` and surfaces the slug-name. **Composes with:** `feedback_version_numbers_at_release_time` (this is a structural extension of the version-derivation principle to all cycle artefacts, not just the plan-doc itself); v0.8.2 plan-doc-slug surface; the broader pattern of conventions-as-discipline-vs-conventions-as-enforcement (slug-naming in dispatch briefs is discipline; the gate accepting only slug-named files would be enforcement). **AI-time band:** dispatch-brief template addition 5-10 min; gate fallback + WARN verdict 30-60 min; pre-commit hook 30-60 min. **Status:** capture-only. Activation gate: next dispatch-brief authoring OR if a third version-prefix slip occurs (third-strike triggers the structural enforcement path).

- **F-FIDRAFT-AC-RATIFICATION-PATTERN — Standardize "FIDRAFT proposes implementation; build-time AC ratifies outcome" as an explicit FIDRAFT-capture-template convention.** Captured 2026-05-14 from v0.10.2 (D-SMLTV.1) + v0.10.3 (D-RBHCB.1) cycles consecutively producing the same shape: the FIDRAFT entry's "Proposed shape" line described an implementation-shaped target (e.g., "append at-tag suffix" / "opt-in `--remove-interim-shipped-local-sentence` kwarg-mode"), and the build-time AC tightened to outcome-shape (e.g., "pure token swap with date preserved" / "unconditional cleanup-when-triggered"). The build-time tightenings were correct (per `feedback_loose_AC_text_fix_AC_not_implementation`) but the FIDRAFT capture template doesn't currently signal that the proposed-shape line is a method hint, not a contract. **Empirical finding:** two cycles in a row, same pattern; the F2-surface from the v0.10.3 agent explicitly flagged it as worth standardizing. **Proposed shape:** add a "Proposed implementation hint (NOT contract)" header label to the FIDRAFT capture template's "Proposed shape" section, OR split into "Proposed outcome" (contract-shaped) + "Implementation hint" (method-shaped, agent-overridable). Either signal makes the agent-vs-contract distinction explicit at capture-time. **Composes with:** `feedback_loose_AC_text_fix_AC_not_implementation` (this is the upstream capture-side convention; loose-AC-fix is the build-side convention); ODD §3 (method is the builder's call); the FIDRAFT no-overhead capture pattern (the convention must NOT add capture overhead — header rename is OK; required filling-out of two fields is not). **AI-time band:** template tweak + propagation to existing FIDRAFT entries 30-60 min (template-only edit + sweep); enforcement via FIDRAFT-capture skill 60-120 min. **Status:** capture-only. Activation gate: third occurrence of the FIDRAFT-vs-AC tightening pattern in a single cycle — at which point the enforcement path activates.

- **F-PLAN-DOC-TEMPLATE-AUTO-BUMP-FENCE — Plan-doc template should pre-include `plugins/dev-sdlc/tests/test_no_sealed_amendments.py` in the default scope-fence allow-list.** Captured 2026-05-14 from v0.10.3 AC.RBHCB.S allow-list slip (caught at first dry-run gate verification + tightened doc-only post-seal at commit `5271cbd`). **Empirical finding:** every dev-sdlc-anchored amendment auto-bumps `test_no_sealed_amendments.py`'s BASELINE-pointer at seal-time as part of the standard apply/seal workflow. The v0.10.2 plan-doc happened to include it; the v0.10.3 plan-doc omitted it; both are equally valid plan-doc authoring choices but the gate verification fails-then-tightens for the omission case. The fix-up is small (one-line allow-list addition + commit) but the rework is preventable by template-default-inclusion. **Proposed shape:** if a plan-doc-template exists in `framework/tools/loam/`, add `plugins/dev-sdlc/tests/test_no_sealed_amendments.py` to the AC.<scope>.S default allow-list with a note that "the dev-sdlc seal workflow auto-bumps this file at seal-time; remove only if your scope explicitly does not seal." If no template exists, capture this convention in `feedback_*` discipline. **Composes with:** F-FIDRAFT-AC-RATIFICATION-PATTERN (both are capture-time convention upgrades that prevent build-time fix-up rework); the broader dev-sdlc seal workflow (this auto-bump is a specific consequence). **AI-time band:** template-edit OR feedback-write 15-30 min; structural enforcement via plan-doc-author skill 60-120 min. **Status:** capture-only. Activation gate: third occurrence of the allow-list slip OR next plan-doc-template revision — whichever comes first.

- **F-RESOLVED-BY-COMPOSITION-VOCABULARY — Add explicit `RESOLVED-BY-COMPOSITION` status to the FIDRAFT vocabulary alongside `capture-only` / `RESOLVED`.** Captured 2026-05-14 from the `residual-tbd-at-placeholder-closures` cycle halt-and-surface. **Empirical finding:** the F-NFCLEAN-FOLLOWON 3rd item was closed not by a single dedicated PATCH but by composition of three prior cycles (v0.10.1 surgical edit + v0.7.4 walker + v0.10.3 narrative-safe helper). The existing two-state FIDRAFT vocabulary forces a binary attribution that doesn't match the actual resolution shape. Today's update flips the entry to `RESOLVED-BY-COMPOSITION 2026-05-14` with an explicit chain pointer; that was a free-form invention of the moment, not a vocabulary-backed status. **Proposed shape:** standardize `RESOLVED-BY-COMPOSITION <date>` as a third terminal status. Convention: the entry's status line names each contributing cycle's slug + commit SHA (chain pointer); the post-status text explains why the composition closes the item. Capture-template addition: a `composition-cycles:` field for tracking. **Composes with:** F-FIDRAFT-AC-RATIFICATION-PATTERN (both are FIDRAFT-vocabulary upgrades that prevent build-time framing inheritance from a stale entry); F-FIDRAFT-FLIP-ON-UNBLOCK-PATCH (the discipline that would auto-trigger the status-flip — captured below); the broader pattern of capture-side discipline preventing build-time fix-up. **AI-time band:** vocabulary documentation + sweep of existing entries for retroactive composition-resolved status 30-60 min; capture-template field addition 15-30 min. **Status:** capture-only. Activation gate: third entry that needs the composition-resolved status — at which point the formal vocabulary change activates.

- **F-FIDRAFT-FLIP-ON-UNBLOCK-PATCH — When a PATCH structurally unblocks a deferred FIDRAFT item, the PATCH's source-edit MUST flip every dependent entry's status in the same commit.** Captured 2026-05-14 from the `residual-tbd-at-placeholder-closures` cycle halt-and-surface. **Empirical finding:** v0.10.3's F-FUNC-3 helper extension structurally closed F-NFCLEAN-FOLLOWON's 3rd item by making narrative-safe placeholder backfill possible AND by composition with v0.10.1 + v0.7.4 already having populated the actual commit-metadata cells. The F-FUNC-3 entry was correctly flipped to RESOLVED at v0.10.3 seal-time, BUT F-NFCLEAN-FOLLOWON's status was NOT flipped — the entry stayed `capture-only` with stale-deferral framing for ~3 hours. Result: a downstream cycle dispatcher (me) inherited the stale framing + dispatched 30 min of agent wall-clock that correctly halted but burned tokens that wouldn't have been spent if the FIDRAFT had been kept in sync. **Proposed shape:** discipline rule — when a PATCH closes a FIDRAFT item AND that item's RESOLVED entry text mentions another FIDRAFT entry as "blocked-by-this-helper" / "unblocked-by-this-helper" / "depends-on-this-helper" / "the case [other-entry] would unblock", the source-edit MUST grep for the dependent entry by ID + flip its status to RESOLVED-BY-COMPOSITION (per F-RESOLVED-BY-COMPOSITION-VOCABULARY) in the same commit. Structural enforcement option: a pre-commit hook that scans the commit's diff for FIDRAFT status-flip-to-RESOLVED + greps the diff'd entry text for "[unblock|unblocked-by] F-[A-Z-]+" patterns + warns if any matched referenced entries are still `capture-only` in the working tree. **Composes with:** F-RESOLVED-BY-COMPOSITION-VOCABULARY (this discipline produces composition-resolved entries; the vocabulary makes them recordable); F-CYCLE-ARTEFACT-SLUG-NAMING + F-PLAN-DOC-TEMPLATE-AUTO-BUMP-FENCE (all three are capture-side conventions preventing build-time fix-up rework); ODD §2.5 (FIDRAFT entries are part of the project's source-of-truth; stale FIDRAFTs are documentation drift in the same family as STATE.md drift). **AI-time band:** discipline-rule documentation + dispatch-brief template addition 15-30 min; pre-commit hook 60-120 min. **Status:** capture-only. Activation gate: second occurrence of stale-FIDRAFT-causing-null-cycle-dispatch — at which point the structural enforcement activates.

- **F-OTEL-VERSION-DYNAMIC-IMPORT — Replace literal tracer-version arg with `importlib.metadata.version("loam-<component>")` import to eliminate future drift.** Captured 2026-05-14 from D-OTVH.1 Path A deferral inside the `otel-tracer-version-honesty` PATCH (v0.10.4). **Empirical context:** v0.10.4 closed F-OTEL-VERSION-BUMP via Path B (current literal "0.10.0" hardcoded across 12 sites — 7 production observability/supervisor + 5 test-fixture monkeypatch installers). Path A (`__version__` import) was declined at plan-time because none of the 6 affected components (cost-governance / self-correction / dormancy / reversibility-primitive / safety-layer / orchestrator) export `__version__` from their `__init__.py`; adding 6 new exports would have been scope-creep beyond the F-OTEL-VERSION-BUMP capture's scope. Path B closes the immediate drift but reintroduces the same drift surface at every future MINOR (v0.11.0 will again need 12 sites bumped manually). **Proposed shape:** replace `_TRACER = trace.get_tracer("loam.<component>", "0.10.0")` with `_TRACER = trace.get_tracer("loam.<component>", version("loam-<component>"))` (using `from importlib.metadata import version`); handle `PackageNotFoundError` for development installs by falling back to a sentinel (e.g., `"0.0.0-dev"` or string-cast of the metadata) so the import never fails. Eliminates future drift entirely; tracer-version always reflects the installed package version. **Composes with:** F-OTEL-VERSION-BUMP (the PATCH this would supersede via structural elimination); AC.HONEST.1 per-component-version discipline (would extend the discipline to "discipline at install-time, not at edit-time"); F7-PLUGIN-VERSION (similar dynamic-version-import pattern for plugin api_version field — could share infrastructure). **AI-time band:** sweep 12 sites + add ImportError fallback per site + verify across `pip install -e .` AND wheel-installed scenarios + verify across pyproject-changed-but-not-reinstalled developer-iteration scenarios + new tests 30-50 min. **Status:** capture-only. Activation gate: (a) next cycle that's already touching the 6 components' `__init__.py` for other reasons (no incremental scope cost), OR (b) structural drift signal — e.g., 3+ MINORs in a row where the literals were forgotten to bump, indicating Path-B discipline isn't sticking.
